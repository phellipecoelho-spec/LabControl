---
phase: 06-estoque
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/app/Http/Controllers/Api/V1/InventoryItemController.php
  - backend/app/Http/Controllers/Api/V1/InventoryCategoryController.php
  - backend/app/Http/Controllers/Api/V1/InventoryMovementController.php
  - backend/app/Http/Requests/StoreInventoryItemRequest.php
  - backend/app/Http/Requests/UpdateInventoryItemRequest.php
  - backend/app/Http/Requests/StoreInventoryCategoryRequest.php
  - backend/app/Http/Requests/UpdateInventoryCategoryRequest.php
  - backend/app/Http/Requests/StoreInventoryMovementRequest.php
  - backend/app/Http/Resources/InventoryItemResource.php
  - backend/app/Http/Resources/InventoryCategoryResource.php
  - backend/app/Http/Resources/InventoryMovementResource.php
  - backend/routes/api.php
autonomous: true
requirements: [INVT-01, INVT-02]
user_setup: []

must_haves:
  truths:
    - Inventory items can be created, read, updated, and soft-deleted via REST API
    - Inventory categories can be created, read, updated, and soft-deleted via REST API
    - Stock movements can be recorded and listed (immutable — no update/delete)
    - Permission middleware restricts access per estoque.* and movimentacoes.* permissions
    - Movement creation validates balance sufficiency via InventoryMovementService
    - FormRequest validation enforces required fields, unit list, existing FKs
    - API responses include computed balance and critical stock status via Resource
  artifacts:
    - 3 Controllers (InventoryItem, InventoryCategory, InventoryMovement)
    - 5 Form Requests (Store/Update for items, Store/Update for categories, Store for movements)
    - 3 API Resources (Item, Category, Movement)
    - Routes registered under /api/v1/ with Sanctum middleware
  key_links:
    - InventoryItemController::store() must create initial purchase movement for initial_quantity > 0 (D-10)
    - InventoryMovementController::store() must delegate to InventoryMovementService for transactional integrity
    - StoreInventoryMovementRequest must validate reason required_if:adjustment,disposal (D-08)
    - All controllers use HasMiddleware static method pattern per EquipmentController
---

<objective>
Create the complete REST API layer for the Inventory module.

**Purpose:** Expose CRUD endpoints for inventory items, categories, and movements with permission-based access control, input validation via FormRequests, and computed response data via API Resources. Movement recording uses the transactional service layer for data integrity.

**Output:**
- 3 Controllers with permission middleware
- 5 Form Requests with complete validation rules
- 3 API Resources with whenLoaded for relationships
- Routes registered in api.php under auth:sanctum group
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estoque/06-CONTEXT.md
@.planning/phases/06-estoque/06-RESEARCH.md

# Backend Patterns
@backend/app/Http/Controllers/Api/V1/EquipmentController.php
@backend/app/Http/Requests/StoreEquipmentRequest.php
@backend/routes/api.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Form Requests with validation rules</name>
  <read_first>
    - `backend/app/Http/Requests/StoreEquipmentRequest.php` — FormRequest pattern: authorize() returns true, rules() returns validation array, messages() returns Portuguese error messages
    - `06-CONTEXT.md` D-14, D-15, D-16 — required fields (nome, categoria, fornecedor, quantidade inicial, unidade, estoque mínimo), optional fields (lote, validade, localização, código), fixed unit list
    - `06-RESEARCH.md` lines 459-462 — conditional validation (reason required_for adjustment/disposal), unit as varchar with application validation instead of enum
  </read_first>
  <files>
    backend/app/Http/Requests/StoreInventoryItemRequest.php
    backend/app/Http/Requests/UpdateInventoryItemRequest.php
    backend/app/Http/Requests/StoreInventoryCategoryRequest.php
    backend/app/Http/Requests/UpdateInventoryCategoryRequest.php
    backend/app/Http/Requests/StoreInventoryMovementRequest.php
  </files>
  <action>
    Create 5 Form Request files in `backend/app/Http/Requests/`, each extending `FormRequest` with `authorize() → true` and Portuguese `messages()`:

    **A. `StoreInventoryItemRequest.php`** (D-14, D-15, D-16)
    - `name` required|string|max:255
    - `code` nullable|string|max:100|unique:inventory_items,code
    - `description` nullable|string
    - `category_id` required|exists:inventory_categories,id
    - `supplier_id` nullable|exists:suppliers,id
    - `unit` required|string|in:UN,KG,L,CX,M,M2,M3,PC,PCT,CJ (use the values from D-16; note M² and M³ use M2/M3 in the validation since Unicode superscripts in DB values cause encoding issues)
    - `min_stock` required|integer|min:0
    - `batch_lot` nullable|string|max:100
    - `expiry_date` nullable|date
    - `physical_location` nullable|string|max:255
    - `initial_quantity` required_without:id|integer|min:0 (used for initial purchase movement creation in the controller)
    - Messages in Portuguese matching StoreEquipmentRequest pattern

    **B. `UpdateInventoryItemRequest.php`**
    - Same rules as StoreInventoryItemRequest EXCEPT:
      - `name` sometimes|string|max:255 (nullable on update)
      - `code` nullable|string|max:100|unique:inventory_items,code,{id} — ignore current item's own code
      - `category_id` sometimes|exists:inventory_categories,id
      - `unit` sometimes|string|in:UN,KG,L,CX,M,M2,M3,PC,PCT,CJ
      - `min_stock` sometimes|integer|min:0
      - NO `initial_quantity` (initial qty only on create)
    - Override `prepareForValidation()` and set `$data['id'] = $this->route('inventory_item')` for the unique:code exclusion

    **C. `StoreInventoryCategoryRequest.php`**
    - `name` required|string|max:255
    - `slug` nullable|string|max:255|unique:inventory_categories,slug (auto-generated from name if not provided)
    - Portuguese messages

    **D. `UpdateInventoryCategoryRequest.php`**
    - `name` sometimes|string|max:255
    - `slug` nullable|string|max:255|unique:inventory_categories,slug,{id}
    - Override prepareForValidation to set id from route parameter

    **E. `StoreInventoryMovementRequest.php`** (D-07, D-08, D-09)
    - `item_id` required|exists:inventory_items,id
    - `type` required|string|in:purchase,consumption,adjustment,disposal,return (D-07)
    - `quantity` required|integer|min:1 (always positive per D-09, direction determined by type)
    - `reason` nullable|string|required_if:type,adjustment|required_if:type,disposal (D-08 — motivo obrigatório para adjustment/disposal)
    - `notes` nullable|string
    - Portuguese messages including custom messages for required_if conditions
  </action>
  <verify>
    <automated>php -l backend/app/Http/Requests/StoreInventoryItemRequest.php 2>&1</automated>
    <automated>php -l backend/app/Http/Requests/StoreInventoryMovementRequest.php 2>&1</automated>
  </verify>
  <done>
    5 Form Request files created with complete validation rules in Portuguese. StoreInventoryMovementRequest enforces reason required for adjustment/disposal. Update requests ignore current record in unique validation.
  </done>
  <acceptance_criteria>
    - StoreInventoryItemRequest validates all required fields per D-14
    - StoreInventoryItemRequest validates unit against fixed list per D-16 (UN,KG,L,CX,M,M2,M3,PC,PCT,CJ)
    - StoreInventoryMovementRequest validates type against 5 fixed values per D-07
    - StoreInventoryMovementRequest requires reason when type is adjustment or disposal
    - UpdateInventoryItemRequest makes fields optional (sometimes) for partial updates
    - UpdateInventoryItemRequest excludes current item's own code from unique check
    - All requests have Portuguese error messages
    - All requests have authorize() returning true (permission handled by controller middleware)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create API Resources</name>
  <read_first>
    - `06-RESEARCH.md` lines 541-561 — InventoryItemResource example with computed current_balance, is_critical, whenLoaded for category/supplier
  </read_first>
  <files>
    backend/app/Http/Resources/InventoryCategoryResource.php
    backend/app/Http/Resources/InventoryItemResource.php
    backend/app/Http/Resources/InventoryMovementResource.php
  </files>
  <action>
    Create 3 API Resource files in `backend/app/Http/Resources/`, each extending `JsonResource`:

    **A. `InventoryCategoryResource.php`**
    - id, name, slug
    - created_at, updated_at

    **B. `InventoryItemResource.php`** (per RESEARCH.md lines 544-561)
    - id, name, code, description
    - unit, min_stock, batch_lot, expiry_date, physical_location
    - current_balance (computed attribute from model)
    - is_critical (computed attribute from model)
    - category → `new InventoryCategoryResource($this->whenLoaded('category'))`
    - supplier → `new SupplierResource($this->whenLoaded('supplier'))` (SupplierResource exists from Phase 5)
    - created_at, updated_at

    **C. `InventoryMovementResource.php`**
    - id, type, quantity (always positive in payload), quantity_display (signed integer: negative for consumption/disposal/adjustment, positive for purchase/return)
    - balance_after, reason, notes
    - item → `new InventoryItemResource($this->whenLoaded('item'))` — light load, only id and name
    - user → `$this->whenLoaded('user', fn() => ['id' => $this->user->id, 'name' => $this->user->name])`
    - created_at
  </action>
  <verify>
    <automated>php -l backend/app/Http/Resources/InventoryItemResource.php 2>&1</automated>
  </verify>
  <done>
    3 API Resource files created with computed attributes and whenLoaded for relationship data
  </done>
  <acceptance_criteria>
    - InventoryItemResource exposes current_balance and is_critical computed attributes
    - InventoryItemResource uses whenLoaded for category and supplier relationships
    - InventoryMovementResource computes quantity_display with sign (+/-) based on movement type
    - All resources return standard JSON structure matching existing API pattern
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Controllers and Register Routes</name>
  <read_first>
    - `backend/app/Http/Controllers/Api/V1/EquipmentController.php` — Full CRUD pattern with static middleware(), index with search/filter, store/update/destroy with user_id/updated_by, load relationships
    - `backend/routes/api.php` — Route structure: prefix v1, Sanctum group, apiResource for resources, nested routes for sub-resources
    - `06-RESEARCH.md` lines 470-537 — Recommended route structure, controller methods, permission middleware patterns (estoque.* for items/categories, movimentacoes.* for movements)
    - `06-CONTEXT.md` D-08, D-10, D-14, D-22 — movement fields, initial balance creation, permission assignments
  </read_first>
  <files>
    backend/app/Http/Controllers/Api/V1/InventoryItemController.php
    backend/app/Http/Controllers/Api/V1/InventoryCategoryController.php
    backend/app/Http/Controllers/Api/V1/InventoryMovementController.php
    backend/routes/api.php
  </files>
  <action>
    Create 3 controllers and update routes: a) Create controller files at the paths above; b) Edit `backend/routes/api.php` to add routes inside the existing `auth:sanctum` group.

    **A. `InventoryItemController.php`** (per D-17, pattern = EquipmentController)
    - static middleware():
      - auth:sanctum on all methods
      - permission:estoque.view on index, show
      - permission:estoque.create on store
      - permission:estoque.edit on update
      - permission:estoque.delete on destroy
    - `index(Request $request)`:
      - Filters: search (name/code ilike), category_id, supplier_id, unit, critical (boolean — filters items where current_balance <= min_stock; use a subquery to get latest balance per item)
      - Load: category, supplier
      - Order: created_at desc
      - Paginate: 15
      - Return: InventoryItemResource::collection()
    - `show(InventoryItem $item)`:
      - Load: category, supplier, movements (latest 50 for detail page)
      - Return: new InventoryItemResource($item)
    - `store(StoreInventoryItemRequest $request)`:
      - Get validated data
      - Wrap in DB::transaction:
        - Create the item with user_id = auth()->id()
        - If initial_quantity > 0 (from request), create initial purchase movement via `$item->movements()->create([...])` with balance_after = initial_quantity, reason = 'Saldo inicial', user_id = auth()->id() (per D-10 and RESEARCH Pitfall 4)
      - Load category, supplier
      - Return 201 with new InventoryItemResource
    - `update(UpdateInventoryItemRequest $request, InventoryItem $item)`:
      - Get validated data, set updated_by = auth()->id()
      - Update item
      - Load category, supplier
      - Return new InventoryItemResource
    - `destroy(InventoryItem $item)`:
      - Set deleted_by = auth()->id(), save, then softDelete()
      - Return 204 no content
    - `byItem(InventoryItem $item)` — additional method: returns movements for this item (used by frontend detail page; delegates to InventoryMovementResource::collection)
      - Return movements for this item: latest 50, paginated 15, ordered by created_at desc

    **B. `InventoryCategoryController.php`**
    - static middleware(): auth:sanctum + permission:estoque.view/index,show; permission:estoque.create/store; permission:estoque.edit/update; permission:estoque.delete/destroy
    - index: list all, search, order by name
    - show: single category
    - store(StoreInventoryCategoryRequest): create, return 201
    - update(UpdateInventoryCategoryRequest): update, return resource
    - destroy: soft delete with deleted_by, return 204

    **C. `InventoryMovementController.php`** (D-06 — immutable, only store/index/show)
    - static middleware():
      - auth:sanctum on all methods
      - permission:movimentacoes.view on index, show (per RESEARCH — movimentacoes.view already seeded)
      - permission:movimentacoes.create on store
    - `index(Request $request)`:
      - Filters: item_id, type, from (date), to (date), user_id
      - Load: item (id, name), user (id, name)
      - Order: created_at desc
      - Paginate: 15
      - Return: InventoryMovementResource::collection()
    - `store(StoreInventoryMovementRequest $request)`:
      - Delegate to `app(InventoryMovementService::class)->recordMovement($request->validated())` for transactional movement creation with row lock
      - After successful movement creation, check if item is now critical: `$item->refresh()->is_critical`
      - Return response with the movement resource AND `is_critical` flag in `meta` for the frontend to display toast alert (per D-11)
      - On InsufficientStockException: catch and return 422 with user-friendly Portuguese error message
      - Return 201
    - `show(InventoryMovement $movement)`:
      - Load: item, user
      - Return resource

    **D. Routes — Edit `backend/routes/api.php`**
    - Add the following INSIDE the existing `Route::middleware('auth:sanctum')->group(function () { ... })` block, after the equipment module section (around line 69, before the closing parenthesis):
    ```php
    // Inventory Module
    Route::apiResource('inventory-items', InventoryItemController::class);
    Route::get('inventory-items/{item}/movements', [InventoryMovementController::class, 'byItem'])
        ->name('inventory-items.movements');

    // Inventory reference tables
    Route::apiResource('inventory-categories', InventoryCategoryController::class)
        ->only(['index', 'store', 'update', 'destroy']);

    // Movements (immutable — no update/destroy)
    Route::apiResource('inventory-movements', InventoryMovementController::class)
        ->only(['index', 'store', 'show']);
    ```
    - Place AFTER the existing equipment-related routes block and BEFORE the closing parenthesis of the auth:sanctum group
    - Add the use statements at the top of api.php:
    ```php
    use App\Http\Controllers\Api\V1\InventoryItemController;
    use App\Http\Controllers\Api\V1\InventoryCategoryController;
    use App\Http\Controllers\Api\V1\InventoryMovementController;
    ```
    - Verify no duplicate imports exist
  </action>
  <verify>
    <automated>php -l backend/app/Http/Controllers/Api/V1/InventoryItemController.php 2>&1</automated>
    <automated>php -l backend/routes/api.php 2>&1</automated>
    <automated>cd backend && php artisan route:list --name=inventory 2>&1</automated>
  </verify>
  <done>
    3 controllers created with permission middleware, complete CRUD logic for items/categories, and append-only movements. Routes registered. Output of `route:list --name=inventory` shows 12+ routes.
  </done>
  <acceptance_criteria>
    - InventoryItemController has all 5 CRUD methods + byItem for movements
    - InventoryItemController::store creates initial purchase movement when initial_quantity > 0
    - InventoryCategoryController has index, store, update, destroy with soft delete
    - InventoryMovementController has only store, index, show (immutable — no update/destroy)
    - InventoryMovementController::store catches InsufficientStockException and returns 422
    - All controllers have static middleware() with permission checks
    - Routes registered: /api/v1/inventory-items (full CRUD), /api/v1/inventory-categories (except show), /api/v1/inventory-movements (no update/destroy), /api/v1/inventory-items/{item}/movements
    - php artisan route:list shows all inventory routes with correct middleware
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API (HTTP) | All routes behind auth:sanctum — unauthenticated requests rejected |
| API → Database (PostgreSQL) | Eloquent ORM with parameterized queries — no raw SQL in controllers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | InventoryItemController middleware | high | mitigate | Static middleware() enforces permission:estoque.{view,create,edit,delete} per action (EquipmentController pattern) |
| T-06-04 | Tampering | StoreInventoryMovementRequest | high | mitigate | FormRequest validates type against fixed whitelist, quantity >= 1, reason required for adjustment/disposal |
| T-06-05 | Elevation of Privilege | InventoryMovementController middleware | high | mitigate | Uses permission:movimentacoes.{view,create} — already seeded in RolePermissionSeeder for all appropriate roles |
| T-06-06 | Denial of Service | List endpoints without pagination | medium | mitigate | All index() methods use paginate(15) — no unbounded queries |
| T-06-07 | Spoofing | UUID generation | low | accept | gen_random_uuid() produces v4 UUIDs; enumerable but no sensitive data tied to UUID guessability |
| T-06-SC | Tampering | composer packages (no new) | high | mitigate | No new package installs in this plan — slopcheck N/A |
</threat_model>

<verification>
```bash
cd backend && php artisan migrate:fresh --seed --seeder=InventorySeeder
cd backend && php artisan route:list --name=inventory
```
Expected: 12+ routes (inventory-items.* with full CRUD, inventory-categories.* with 4 methods, inventory-movements.* with 3 methods, inventory-items.movements).
</verification>

<success_criteria>
- [ ] All 5 FormRequests validate correctly per D-14, D-15, D-16
- [ ] StoreInventoryItemRequest accepts initial_quantity for initial stock creation
- [ ] StoreInventoryMovementRequest enforces reason for adjustment and disposal types
- [ ] InventoryItemController full CRUD with permission middleware
- [ ] Store action creates initial purchase movement when initial_quantity provided
- [ ] InventoryMovementController has only store, index, show (immutable ledger)
- [ ] Store action delegates to InventoryMovementService and returns is_critical in meta
- [ ] API Resources include computed current_balance and is_critical
- [ ] All routes registered and visible in route:list
</success_criteria>

<output>
Create `.planning/phases/06-estoque/06-02-SUMMARY.md` when done
</output>
