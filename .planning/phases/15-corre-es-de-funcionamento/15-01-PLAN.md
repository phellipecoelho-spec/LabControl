---
number: 15-01
title: RBAC Enforcement e Correções de Controllers (BUG-02)
slug: corre-es-de-funcionamento
status: draft
phase: 15-corre-es-de-funcionamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/Http/Controllers/Api/V1/CategoryController.php
  - backend/app/Http/Controllers/Api/V1/ManufacturerController.php
  - backend/app/Http/Controllers/Api/V1/SupplierController.php
  - backend/app/Http/Controllers/Api/V1/EquipmentController.php
  - backend/app/Http/Controllers/Api/V1/EquipmentPhotoController.php
  - backend/app/Http/Controllers/Api/V1/InventoryCategoryController.php
  - backend/app/Http/Controllers/Api/V1/InventoryItemController.php
  - backend/app/Http/Controllers/Api/V1/InventoryMovementController.php
  - backend/app/Http/Controllers/Api/V1/LoanController.php
  - backend/app/Http/Controllers/Api/V1/CalibrationController.php
  - backend/app/Http/Controllers/Api/V1/CalibrationCertificateController.php
  - backend/app/Http/Controllers/Api/V1/VerificationController.php
  - backend/app/Http/Controllers/Api/V1/MaintenanceOrderController.php
  - backend/app/Http/Controllers/Api/V1/DashboardController.php
  - backend/app/Http/Controllers/Api/V1/ReportController.php
  - backend/app/Http/Controllers/Api/V1/RoleController.php
  - backend/database/seeders/RolePermissionSeeder.php
  - backend/routes/api.php
  - backend/tests/Feature/RbacRegressionTest.php
  - backend/tests/Feature/Auth/RateLimitTest.php
autonomous: true
requirements: [BUG-02]
created: 2026-08-09

# Goal-backward verification
must_haves:
  truths:
    - "Usuário autenticado SEM permissão recebe 403 em endpoints de módulo (não 200)"
    - "GET /api/v1/reports não retorna mais 500"
    - "GET /api/v1/verifications/pending aponta para VerificationController (não LoanController)"
    - "RateLimitTest verde (RateLimiter::clear() com chave correta)"
    - "RoleController exige permission:roles.manage para mutações (sem escalada)"
  artifacts:
    - backend/tests/Feature/RbacRegressionTest.php (Wave-0, T-15-01)
    - "14 controllers com implements HasMiddleware + new Middleware()"
    - "ReportController sem formato array legado"
    - "RolePermissionSeeder com permissão roles.manage"
  key_links:
    - "controller middleware() -> routes/api.php -> CheckPermission (alias permission)"
    - "RolePermissionSeeder -> RoleController::middleware() -> permission:roles.manage"
---

<read_first>
@C:/Users/phell/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/phell/.config/opencode/gsd-core/templates/summary.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-corre-es-de-funcionamento/15-RESEARCH.md
@.planning/phases/15-corre-es-de-funcionamento/15-VALIDATION.md
@backend/app/Http/Controllers/Api/V1/UserController.php
@backend/app/Http/Controllers/Api/V1/ActivityLogController.php
@backend/app/Http/Middleware/CheckPermission.php
@backend/app/Providers/AppServiceProvider.php
</read_first>

<objective>
Aplicar a correção CRITICAL do bypass de RBAC (14 controllers com middleware() ignorado por não implementarem HasMiddleware), corrigir o 500 do ReportController (formato legado de middleware removido no Laravel 13), fechar a escalada de privilégio no RoleController, corrigir a rota /verifications/pending e o RateLimitTest, e criar a rede de regressão RbacRegressionTest que comprova o 403 para usuário sem permissão.

Purpose: Hoje qualquer usuário autenticado acessa qualquer módulo. Este plano torna o controle de permissões real e comprovado por teste automatizado, atendendo ao critério BUG-02 "bugs da varredura corrigidos e validados".

Output: 14 controllers convertidos para o padrão Laravel 13 de middleware, ReportController/RoleController corrigidos, rotas e RateLimitTest corrigidos, RbacRegressionTest verde.
</objective>

<acceptance_criteria>
Critérios de aceite derivados de REQUIREMENTS.md (BUG-02):

1. Usuário autenticado sem permissão recebe **403** (não 200) ao acessar endpoints de qualquer módulo — comprovado por RbacRegressionTest.
2. `GET /api/v1/reports` retorna **200/403** (nunca 500) — ReportControllerTest verde.
3. Nenhum controller de `Api/V1` usa o formato array legado `['middleware' => ..., 'options' => ...]`.
4. Mutações de roles (criar/editar/excluir/sincronizar permissões) exigem `permission:roles.manage` — sem escalada por usuário comum.
5. RateLimitTest verde (bloqueio após 5 tentativas → 429).
</acceptance_criteria>

<execution_context>
- Repo: `D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl`
- Teste rápido: `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=<teste-da-tarefa>`
- Suite completa ao fim do wave: `docker compose -f docker/docker-compose.yml exec -T php php artisan test`
- Ambiente de teste: PHPUnit ^12.5.12, Laravel 13.20.0, sqlite :memory: (CACHE_STORE=array), ~120s
- EXISTEM mudanças não commitadas na working tree relacionadas a BUG-02 (EquipmentController guards de exclusão, ReportController edição parcial, Models/Services, VerificationUatFixTest parcial, frontend). Integrar/verificar — NÃO refazer. Em particular o ReportController.php JÁ tem edição parcial (`'options' => ['only' => ...]`) que AINDA é o formato legado — deve ser convertida para `new Middleware()`.
</execution_context>

## Context / Estado Verificado

- **14 controllers** declaram `middleware()` mas NÃO implementam `HasMiddleware`: CategoryController, ManufacturerController, SupplierController, EquipmentController, EquipmentPhotoController, InventoryCategoryController, InventoryItemController, InventoryMovementController, LoanController, CalibrationController, CalibrationCertificateController, VerificationController, MaintenanceOrderController, DashboardController. O router IGNORA `middleware()` sem `HasMiddleware` → RBAC bypass.
- **Padrão correto** (UserController/ActivityLogController): `use Illuminate\Routing\Controllers\HasMiddleware;` + `use Illuminate\Routing\Controllers\Middleware;` + `implements HasMiddleware` + `new Middleware('permission:x', only: [...])`.
- **ReportController**: `implements HasMiddleware` ok, mas retorna arrays legados → 500. Converter para `new Middleware('auth:sanctum')`, `new Middleware('permission:relatorios.view', only: ['index'])`, `new Middleware('permission:relatorios.export', only: ['download'])`.
- **RoleController**: `implements HasMiddleware` ok, mas só `new Middleware('auth:sanctum')` → qualquer usuário autenticado pode criar/editar/excluir roles e sincronizar permissões (escalada). Métodos: index(25), show(36), store(44), update(55), destroy(66), syncPermissions(81).
- **RolePermissionSeeder**: idempotente (updateOrInsert), roles/perms via DB. Admin recebe TODAS as permissões automaticamente (`pluck('slug')->all()`). NÃO existe permissão `roles.manage` — será adicionada.
- **CheckPermission**: admin (slug 'admin') → bypass; senão exige `hasPermission($permission)`; lança AuthorizationException → 403.
- **routes/api.php**: linha 131 `Route::get('/verifications/pending', [LoanController::class, 'pending'])` — **BUG**: LoanController NÃO tem método `pending` (métodos: index/show/store/update/destroy/activate/returnItem/cancel). VerificationController TEM `pending()` (linha 56). As rotas canônicas `/equipments/{id}/verifications` (134) e `/equipments/{id}/maintenance` (143) JÁ existem e funcionam; as duplicatas legado `/verifications/by-equipment/{id}` (137) e `/maintenance-orders/by-equipment/{id}` (140) serão removidas no plano 15-02 (atômico com a migração dos testes).
- **RateLimitTest** (backend/tests/Feature/Auth/RateLimitTest.php:19): `RateLimiter::clear()` sem argumento → no Laravel 13 `clear($key)` exige chave. O limiter registrado é `RateLimiter::for('auth', ...)` (AppServiceProvider.php:22) e as rotas usam `throttle:auth`. VALIDATION.md row 15-01-03 menciona `'api'` — usar `'auth'` (nome do limiter registrado) e validar com o teste; se persistir falha, alternar para `'api'`.
- **RbacRegressionTest** é teste Wave-0 (❌ W0 na VALIDATION.md): deve ser CRIADO neste plano antes da implementação e validar 403 para usuário sem permissão.

### Tasks

<tasks>

<task type="auto">
<name>Converter controllers de Equipamentos/Estoque para HasMiddleware + new Middleware()</name>
<files>
- backend/app/Http/Controllers/Api/V1/CategoryController.php
- backend/app/Http/Controllers/Api/V1/ManufacturerController.php
- backend/app/Http/Controllers/Api/V1/SupplierController.php
- backend/app/Http/Controllers/Api/V1/EquipmentController.php
- backend/app/Http/Controllers/Api/V1/EquipmentPhotoController.php
- backend/app/Http/Controllers/Api/V1/InventoryCategoryController.php
- backend/app/Http/Controllers/Api/V1/InventoryItemController.php
</files>
<action>
Para cada um dos 7 controllers listados, aplicar a conversão EXATA do padrão já usado em UserController/ActivityLogController:

1. Adicionar imports: `use Illuminate\Routing\Controllers\HasMiddleware;` e `use Illuminate\Routing\Controllers\Middleware;`.
2. Alterar a declaração da classe para `class X extends Controller implements HasMiddleware` (ou `class X implements HasMiddleware` se o controller não estende Controller).
3. Converter o corpo de `middleware()` do formato array legado para objetos `new Middleware(...)`:
   - `['middleware' => 'auth:sanctum']` → `new Middleware('auth:sanctum')`
   - `['middleware' => 'permission:xxx', 'options' => ['only' => [...]]]` → `new Middleware('permission:xxx', only: [...])`
   - `['middleware' => 'permission:yyy']` (sem only) → `new Middleware('permission:yyy')`
4. MANTER EXATAMENTE os slugs de permissão já declarados (não inventar nem alterar):
   - CategoryController/ManufacturerController/SupplierController → `equipamentos.view/create/edit/delete`
   - EquipmentController → `equipamentos.view/create/edit/delete`
   - EquipmentPhotoController → `equipamentos.edit`
   - InventoryCategoryController/InventoryItemController → `estoque.view/create/edit/delete`
5. Atualizar o docblock `@return array<int, array<string, mixed>>` para refletir `array<int, \Illuminate\Routing\Controllers\Middleware>`.
6. NÃO alterar a lógica dos métodos; NÃO tocar em outras partes do arquivo. EquipmentController já tem guards de exclusão não commitados na working tree — preservar intactos.
</action>
<verify>
<automated>docker compose -f docker/docker-compose.yml exec -T php php artisan route:list --path=api/v1; docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=EquipmentApiTest</automated>
</verify>
<done>7 controllers implementam HasMiddleware, usam somente `new Middleware()`, e EquipmentApiTest segue verde (ou falha apenas por pendências conhecidas do 15-02).</done>
<acceptance_criteria>
- BUG-02: módulos Equipamentos/Estoque passam a exigir permissões reais (403 para sem-permissão).
</acceptance_criteria>
</task>

<task type="auto">
<name>Converter controllers de operações para HasMiddleware; corrigir ReportController (500) e RoleController (escalada)</name>
<files>
- backend/app/Http/Controllers/Api/V1/InventoryMovementController.php
- backend/app/Http/Controllers/Api/V1/LoanController.php
- backend/app/Http/Controllers/Api/V1/CalibrationController.php
- backend/app/Http/Controllers/Api/V1/CalibrationCertificateController.php
- backend/app/Http/Controllers/Api/V1/VerificationController.php
- backend/app/Http/Controllers/Api/V1/MaintenanceOrderController.php
- backend/app/Http/Controllers/Api/V1/DashboardController.php
- backend/app/Http/Controllers/Api/V1/ReportController.php
- backend/app/Http/Controllers/Api/V1/RoleController.php
- backend/database/seeders/RolePermissionSeeder.php
</files>
<action>
Parte A — Converter os 7 controllers de operações para `implements HasMiddleware` + `new Middleware()` (mesmo padrão da Task 1), mantendo os slugs já declarados:
- InventoryMovementController → `movimentacoes.view/create`
- LoanController → `emprestimos.view/create/edit/finalizar`
- CalibrationController → `calibracoes.view/create/edit/concluir/cancel`
- CalibrationCertificateController → `calibracoes.edit`
- VerificationController → `afericoes.view/create/edit`
- MaintenanceOrderController → `manutencoes.view/create/edit/concluir`
- DashboardController → `dashboard.view` (controller invocável `__invoke`; `new Middleware('permission:dashboard.view')` sem `only`)

Parte B — ReportController (500): substituir TODAS as entradas de array legado por `new Middleware()`:
- `['middleware' => 'auth:sanctum']` → `new Middleware('auth:sanctum')`
- `['middleware' => 'permission:relatorios.view', 'options' => ['only' => ['index']]]` → `new Middleware('permission:relatorios.view', only: ['index'])`
- `['middleware' => 'permission:relatorios.export', 'options' => ['only' => ['download']]]` → `new Middleware('permission:relatorios.export', only: ['download'])`
- A edição parcial não commitada da working tree faz parte deste arquivo — concluí-la (não reverter).

Parte C — RoleController (escalada de privilégio):
- Em `RolePermissionSeeder.php`, adicionar ao array `$permissions` a entrada: `['name' => 'Gerenciar Perfis', 'slug' => 'roles.manage', 'group' => 'usuarios']` (o seeder é idempotente via updateOrInsert; admin recebe automaticamente por `pluck('slug')->all()`).
- Em `RoleController::middleware()`, substituir o retorno por:
  - `new Middleware('auth:sanctum')`
  - `new Middleware('permission:roles.manage', only: ['store', 'update', 'destroy', 'syncPermissions'])`
  - Index/show ficam com `auth:sanctum` apenas (a tela de usuários carrega a lista de roles; manter index/show abertas).
</action>
<verify>
<automated>docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=ReportControllerTest</automated>
</verify>
<done>7 controllers de operações com HasMiddleware; ReportControllerTest verde (sem 500); RoleController exige roles.manage para mutações; RolePermissionSeeder contém roles.manage.</done>
<acceptance_criteria>
- BUG-02: `/api/v1/reports` não retorna 500; usuário não-admin não consegue criar/editar/excluir roles (403).
</acceptance_criteria>
</task>

<task type="auto">
<name>Rede de regressão RBAC (RbacRegressionTest), correção do RateLimitTest e da rota pending</name>
<files>
- backend/tests/Feature/RbacRegressionTest.php
- backend/tests/Feature/Auth/RateLimitTest.php
- backend/routes/api.php
</files>
<action>
1. Criar `backend/tests/Feature/RbacRegressionTest.php` (Wave-0):
   - `use RefreshDatabase;` + `$this->seed(\Database\Seeders\RolePermissionSeeder::class);` no setUp.
   - Criar usuário SEM roles (`User::factory()->create()`) e autenticar via `actingAs($user)`.
   - Para cada endpoint de módulo, `assertStatus(403)` com `getJson`/`postJson`:
     - `GET /api/v1/dashboard`
     - `GET /api/v1/equipments` e `POST /api/v1/equipments`
     - `GET /api/v1/categories`, `/api/v1/manufacturers`, `/api/v1/suppliers`
     - `GET /api/v1/inventory-items`, `/api/v1/inventory-categories`, `/api/v1/inventory-movements`
     - `GET /api/v1/loans` e `POST /api/v1/loans`
     - `GET /api/v1/calibrations` e `POST /api/v1/calibrations`
     - `GET /api/v1/verifications` e `GET /api/v1/verifications/pending`
     - `GET /api/v1/maintenance-orders`
     - `GET /api/v1/reports` e `GET /api/v1/reports/equipments?format=csv`
     - `GET /api/v1/users`, `GET /api/v1/roles` e `POST /api/v1/roles`
     - `GET /api/v1/activity-logs`
   - Observação: CheckPermission dá bypass ao admin; usuário sem role → 403 em todos os módulos.
2. Corrigir `RateLimitTest.php:19`: `RateLimiter::clear();` → `RateLimiter::clear('auth');` (nome do limiter registrado em AppServiceProvider.php:22; as rotas usam `throttle:auth`). Rodar o filtro; se continuar vermelho, trocar a chave para `'api'` (documentada na VALIDATION.md row 15-01-03) e validar de novo. Manter a chave que deixa verde.
3. Corrigir `routes/api.php:131`: `[LoanController::class, 'pending']` → `[VerificationController::class, 'pending']`. NÃO remover ainda as rotas legado `/verifications/by-equipment/{id}` e `/maintenance-orders/by-equipment/{id}` — sua remoção é atômica com a migração dos testes (plano 15-02, Task 2).
4. Rodar a suite BUG-02: filtros RbacRegressionTest, ReportControllerTest, RateLimitTest; depois suite completa e anotar as falhas restantes conhecidas (VerificationUatFixTest, MaintenanceVerificationTest, ReportServiceTest, SeederIdempotencyTest — pendentes do 15-02).
</action>
<verify>
<automated>docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=RbacRegressionTest; docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=RateLimitTest</automated>
</verify>
<done>RbacRegressionTest verde (403 para sem-permissão em todos os módulos); RateLimitTest verde (429 após 5 tentativas); rota /verifications/pending aponta para VerificationController.</done>
<acceptance_criteria>
- BUG-02: bypass de RBAC eliminado e comprovado por regressão automatizada; RateLimitTest e rota pending corrigidos.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Filtros por tarefa: `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=RbacRegressionTest` (15-01-01), `--filter=ReportControllerTest` (15-01-02), `--filter=RateLimitTest` (15-01-03).
- Suite completa ao fim do wave: `docker compose -f docker/docker-compose.yml exec -T php php artisan test` — registrar falhas restantes pendentes do 15-02 (não são regressões novas).
- Verificação estrutural: nenhum controller em `backend/app/Http/Controllers/Api/V1/` sem `HasMiddleware` quando declara `middleware()`; nenhuma ocorrência de `'options' =>` restante nos controllers.
</verification>

<success_criteria>
- RbacRegressionTest, ReportControllerTest e RateLimitTest verdes.
- 14 controllers com `implements HasMiddleware` + somente `new Middleware()`.
- ReportController sem 500; RoleController com gate `roles.manage`; seeder com a nova permissão.
- Rota `/verifications/pending` corrigida.
- Nenhuma falha nova na suite além das pendências explícitas do 15-02.
</success_criteria>

<output>
Criar `.planning/phases/15-corre-es-de-funcionamento/15-01-SUMMARY.md` ao concluir.
</output>

## Artifacts this phase produces
- `backend/tests/Feature/RbacRegressionTest.php` — rede de regressão RBAC (wave 0).
- 14 controllers convertidos para `implements HasMiddleware` + `new Middleware()`.
- `backend/app/Http/Controllers/Api/V1/ReportController.php` — middleware em formato Laravel 13 (sem 500).
- `backend/app/Http/Controllers/Api/V1/RoleController.php` — gate `permission:roles.manage`.
- `backend/database/seeders/RolePermissionSeeder.php` — permissão `roles.manage` adicionada.
- `backend/routes/api.php` — rota `/verifications/pending` apontando para VerificationController.
- `backend/tests/Feature/Auth/RateLimitTest.php` — `RateLimiter::clear()` com chave.
</content>


</content>
