---
phase: 09-afericoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database/migrations/2026_07_25_000001_create_verifications_tables.php
  - backend/app/Models/Verification.php
  - backend/app/Models/VerificationTemplate.php
  - backend/app/Models/VerificationParam.php
  - backend/app/Models/Equipment.php
  - backend/app/Models/Category.php
  - backend/app/Enums/VerificationResult.php
  - backend/app/Exceptions/VerificationException.php
  - backend/app/Services/VerificationService.php
  - backend/database/factories/VerificationFactory.php
  - backend/database/factories/VerificationTemplateFactory.php
  - backend/database/factories/VerificationParamFactory.php
  - backend/database/seeders/VerificationSeeder.php
  - backend/database/seeders/DatabaseSeeder.php
autonomous: true
requirements: [VERF-01, VERF-02]
user_setup: []

must_haves:
  truths:
    - "verification_templates table exists with FK to equipment_category_id"
    - "verifications table exists with FK to equipment_id and operator_id"
    - "verification_params table exists with FK to verification_id and template_id"
    - "Equipment model has verification_frequency field and verifications() relationship"
    - "Category model has verificationTemplates() relationship"
    - "VerificationResult enum exists with within_range, outside_range, not_measured"
    - "VerificationService.create() creates verification + params in transaction with auto-calculated results"
    - "Factories and seeder produce test data"
    - "Permissions afericoes.view/create/edit exist in seeder"
  artifacts:
    - backend/database/migrations/2026_07_25_000001_create_verifications_tables.php
    - backend/app/Enums/VerificationResult.php
    - backend/app/Models/Verification.php
    - backend/app/Models/VerificationTemplate.php
    - backend/app/Models/VerificationParam.php
    - backend/app/Exceptions/VerificationException.php
    - backend/app/Services/VerificationService.php
    - backend/database/factories/VerificationFactory.php
    - backend/database/factories/VerificationTemplateFactory.php
    - backend/database/factories/VerificationParamFactory.php
    - backend/database/seeders/VerificationSeeder.php
  key_links:
    - "Equipment ↔ Verifications: hasMany relationship (D-06)"
    - "Category ↔ VerificationTemplates: hasMany relationship (D-01)"
    - "VerificationService tolerance calculation must match template tolerance_min/max (D-05)"
    - "Result enum values must match D-04 exactly: within_range, outside_range, not_measured"
    - "No scheduled command is built for this alert (D-13) — notification is synchronous in the controller layer (Plan 09-02)"
---

<objective>
Database schema, models, enums, exception, service layer, factory, and seeder for the Aferições module.

**Purpose:** Establish all backend data foundations so that Plan 09-02 can build the API and frontend on top.

**Output:** 3 migration files, 3 Eloquent models, 1 enum, 1 exception class, 1 service class, 2 factories, 1 seeder, modifications to Equipment and Category models, permission seeding.
</objective>

<execution_context>
@C:/Users/phell/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/phell/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-afericoes/09-CONTEXT.md
@.planning/phases/09-afericoes/09-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

# Model and migration patterns
@backend/app/Models/Calibration.php
@backend/app/Models/CalibrationCertificate.php
@backend/app/Models/Equipment.php
@backend/app/Models/Category.php
@backend/app/Enums/CalibrationStatus.php
@backend/app/Exceptions/CalibrationException.php
@backend/app/Services/CalibrationService.php
@backend/database/migrations/2026_07_25_000001_create_calibrations_tables.php
@backend/database/seeders/RolePermissionSeeder.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for verifications tables (D-01, D-02, D-03, D-04)</name>
  <files>
    backend/database/migrations/2026_07_25_000001_create_verifications_tables.php
  </files>
  <action>
    Create a single compound migration file following the exact pattern of `2026_07_25_000001_create_calibrations_tables.php`.

    This migration MUST create three tables in a single `up()` method — one `Schema::create()` call per table, each representing the logical groupings per D-01/D-02/D-03/D-04. The down() method MUST drop all three in reverse order.

    **Table 1 — `verification_templates`** (D-01, D-02):
    - `id` UUID PK with `gen_random_uuid()` default
    - `equipment_category_id` FK constrained to `categories` (note: the model is `Category`, table is `categories`)
    - `parameter_name` string(255)
    - `parameter_unit` string(50) nullable
    - `tolerance_min` decimal(16,6) nullable
    - `tolerance_max` decimal(16,6) nullable
    - `sort_order` integer default 0
    - `created_by` UUID nullable (FK users)
    - `updated_by` UUID nullable
    - `deleted_by` UUID nullable
    - `timestamps()` + `softDeletes()`
    - Index on `equipment_category_id`
    - Index on `(equipment_category_id, sort_order)` for ordered loading

    **Table 2 — `verifications`** (D-03):
    - `id` UUID PK with `gen_random_uuid()` default
    - `equipment_id` FK constrained to `equipments`
    - `verified_at` timestamp not null (default: now)
    - `operator_id` FK constrained to `users`
    - `notes` text nullable
    - `created_by` UUID nullable (FK users)
    - `updated_by` UUID nullable
    - `deleted_by` UUID nullable
    - `timestamps()` + `softDeletes()`
    - Index on `equipment_id`
    - Index on `(equipment_id, verified_at)` for history queries
    - Index on `verified_at` for pending detection

    **Table 3 — `verification_params`** (D-04):
    - `id` UUID PK with `gen_random_uuid()` default
    - `verification_id` FK constrained to `verifications` onDelete cascade
    - `template_id` FK constrained to `verification_templates`
    - `value` decimal(16,6) nullable
    - `result` string(20) not null default 'not_measured' (values: within_range, outside_range, not_measured per D-04)
    - `notes` text nullable
    - `created_by` UUID nullable (FK users)
    - `updated_by` UUID nullable
    - `deleted_by` UUID nullable
    - `timestamps()` + `softDeletes()`
    - Index on `verification_id`
    - Index on `template_id`
    - Index on `result`

    **Add `verification_frequency` column to `equipments` table** (D-06, D-07):
    After the three create calls, add a Schema::table('equipments') call adding:
    - `verification_frequency` string(10) nullable (values: daily, weekly, shift)

    **Migration date:** Use the current date `2026_07_25` as prefix, with sequence `000001` (same prefix as calibrations migration). Full filename: `2026_07_25_000001_create_verifications_tables.php`.
  </action>
  <verify>
    <automated>php artisan migrate --pretend --path=database/migrations/2026_07_25_000001_create_verifications_tables.php 2>&1 | grep -c "create table\|alter table"</automated>
  </verify>
  <done>Migration creates 3 tables (verification_templates, verifications, verification_params) and adds verification_frequency column to equipments</done>
</task>

<task type="auto">
  <name>Task 2: Create enums, exception, models, and modify Equipment/Category (D-01 through D-07)</name>
  <files>
    backend/app/Enums/VerificationResult.php
    backend/app/Exceptions/VerificationException.php
    backend/app/Models/Verification.php
    backend/app/Models/VerificationTemplate.php
    backend/app/Models/VerificationParam.php
    backend/app/Models/Equipment.php
    backend/app/Models/Category.php
  </files>
  <action>
    Create four new files and modify two existing models. Follow established patterns from Calibration module exactly.

    **1. `backend/app/Enums/VerificationResult.php`** — string enum (per D-04):
    ```
    case WithinRange = 'within_range';
    case OutsideRange = 'outside_range';
    case NotMeasured = 'not_measured';
    ```
    - Method `label()` returning Portuguese labels: 'Dentro do Intervalo', 'Fora do Intervalo', 'Não Medido'
    - Method `color()` returning semantic colors: 'success' (WithinRange), 'danger' (OutsideRange), 'warn' (NotMeasured)
    - Method `isWithinRange(): bool` returning true for WithinRange

    **2. `backend/app/Exceptions/VerificationException.php`** — identical pattern to CalibrationException:
    - Default message: 'Operação de verificação inválida.'
    - Default code: 422
    - `render()` method returning JsonResponse with `message` and `error: 'verification_error'`

    **3. `backend/app/Models/VerificationTemplate.php`** (D-01, D-02):
    - Traits: HasFactory, HasUuids, SoftDeletes, LogsActivity
    - Table: `verification_templates`
    - $fillable: equipment_category_id, parameter_name, parameter_unit, tolerance_min, tolerance_max, sort_order, created_by, updated_by, deleted_by
    - $casts: tolerance_min => 'decimal:6', tolerance_max => 'decimal:6', sort_order => 'integer'
    - Relationship: `category()` belongsTo(Category::class, 'equipment_category_id')
    - Scope: `scopeByCategory(Builder $query, string $categoryId)`

    **4. `backend/app/Models/Verification.php`** (D-03):
    - Traits: HasFactory, HasUuids, SoftDeletes, LogsActivity
    - Table: `verifications`
    - $fillable: equipment_id, verified_at, operator_id, notes, created_by, updated_by, deleted_by
    - $casts: verified_at => 'datetime'
    - $auditExclude: ['updated_by', 'deleted_by']
    - Relationships:
      - `equipment()` belongsTo(Equipment::class)
      - `operator()` belongsTo(User::class, 'operator_id')
      - `params()` hasMany(VerificationParam::class) with orderBy('created_at')
      - `createdBy()` belongsTo(User::class, 'created_by')->withDefault()
    - Scopes: `scopeByEquipment(Builder $query, string $equipmentId)`, `scopeByDateRange(Builder $query, string $from, string $to)`

    **5. `backend/app/Models/VerificationParam.php`** (D-04):
    - Traits: HasFactory, HasUuids, SoftDeletes, LogsActivity
    - Table: `verification_params`
    - $fillable: verification_id, template_id, value, result, notes, created_by, updated_by, deleted_by
    - $casts: value => 'decimal:6', result => VerificationResult::class
    - Relationships:
      - `verification()` belongsTo(Verification::class)
      - `template()` belongsTo(VerificationTemplate::class)
      - `createdBy()` belongsTo(User::class, 'created_by')->withDefault()
    - Accessor: `getResultLabelAttribute()` returning `$this->result?->label()`

    **6. MODIFY `backend/app/Models/Equipment.php`** — Add (per D-06, D-07):
    - Add 'verification_frequency' to $fillable
    - Add 'verification_frequency' => 'string' to $casts
    - Add relationship:
      ```
      public function verifications()
      {
          return $this->hasMany(Verification::class);
      }
      ```
    - Add relationship (optional helper):
      ```
      public function lastVerification()
      {
          return $this->hasOne(Verification::class)->latestOfMany('verified_at');
      }
      ```

    **7. MODIFY `backend/app/Models/Category.php`** — Add (per D-01):
    - Add relationship:
      ```
      public function verificationTemplates()
      {
          return $this->hasMany(VerificationTemplate::class, 'equipment_category_id');
      }
      ```
  </action>
  <verify>
    <automated>php artisan model:show Verification 2>&1 | grep -c "Verification"; php artisan model:show VerificationTemplate 2>&1 | grep -c "VerificationTemplate"; php artisan model:show VerificationParam 2>&1 | grep -c "VerificationParam"</automated>
  </verify>
  <done>All 3 new models created with correct fillable/casts/relationships. Equipment has verification_frequency field and verifications() relationship. Category has verificationTemplates() relationship. VerificationResult enum has 3 cases.</done>
</task>

<task type="auto">
  <name>Task 3: Create VerificationService, factories, and update seeder/permissions (D-05, D-10, D-11, D-12, D-17)</name>
  <files>
    backend/app/Services/VerificationService.php
    backend/database/factories/VerificationTemplateFactory.php
    backend/database/factories/VerificationFactory.php
    backend/database/factories/VerificationParamFactory.php
    backend/database/seeders/VerificationSeeder.php
    backend/database/seeders/DatabaseSeeder.php
  </files>
  <action>
    **1. `backend/app/Services/VerificationService.php`** — Business logic following CalibrationService pattern exactly.

    Method `create(array $data): Verification` — transactional (DB::transaction), steps per D-05, D-10:
    a. Create Verification record:
       - equipment_id from $data['equipment_id']
       - verified_at from $data['verified_at'] ?? now()
       - operator_id from auth()->id()
       - notes from $data['notes'] ?? null
    b. Load templates for the equipment's category:
       ```
       $templates = VerificationTemplate::where('equipment_category_id', $equipment->category_id)->get()->keyBy('id');
       ```
    c. Iterate $data['params'] (array of template_id => value). For each param:
       - Look up template from $templates
       - Cast value to float if not null
       - Calculate result:
         ```
         private function calculateResult(?float $value, VerificationTemplate $template): VerificationResult
         ```
         - If value is null → NotMeasured
         - If tolerance_min !== null && value < tolerance_min → OutsideRange
         - If tolerance_max !== null && value > tolerance_max → OutsideRange
         - Otherwise → WithinRange
       - Create VerificationParam with calculated result
     d. Track if any param is OutsideRange (store as `$hasOutsideRange` flag on the Verification model or return from create())
     e. Notification is NOT fired in the service layer — it is dispatched in the controller (Plan 09-02) to avoid cross-plan dependency on ToleranceExceeded class. (D-11, D-12, D-13 — no batch, no scheduled command):
       ```
        // Notification dispatch moved to controller layer (Plan 09-02) to avoid
        // cross-plan dependency on ToleranceExceeded notification class.
        // Service returns $hasOutsideRange on the verification for the controller to check.
    f. Return $verification->load(['equipment:id,name,patrimony_id', 'params.template'])

    Method `getPendingVerifications(): Collection` — Query equipment per D-08:
    - Equipment where verification_frequency is not null
    - AND (no verifications exist for this equipment OR last verified_at + frequency < now())
    - Use CASE/WHEN for frequency to interval conversion:
      - daily → 1 day
      - weekly → 7 days
      - shift → 12 hours
    - Load with category, lastVerification
    - Return collection

    Method `getHistoryByEquipment(string $equipmentId, ?int $perPage = 15): LengthAwarePaginator`:
    - Verification::byEquipment($equipmentId)->with(['operator:id,name', 'params.template'])->orderBy('verified_at', 'desc')->paginate($perPage)

    **2. `backend/database/factories/VerificationTemplateFactory.php`**:
    - Uses HasFactory on VerificationTemplate model
    - definition(): parameter_name from fake words (random measurement name like "Temperatura", "Pressão", "Umidade", "Vibração"), parameter_unit ('°C', 'bar', '%RH', 'mm/s'), tolerance_min random negative (nullable, 30% chance null), tolerance_max random positive (nullable, 30% chance null), sort_order sequential.
    - State: `withTolerance(float $min, float $max)` — sets both tolerances.
    - State: `noTolerance()` — sets both to null.

    **3. `backend/database/factories/VerificationFactory.php`**:
    - definition(): equipment_id from existing Equipment, verified_at random past month, operator_id from existing User, notes optional.
    - After creating, attach a random number (2-5) of verification_params using `VerificationParamFactory` with this verification.

    **4. `backend/database/factories/VerificationParamFactory.php`**:
    - definition(): verification_id (must be passed), template_id from existing VerificationTemplate linked to the equipment's category, value random number, result = Null but post-create calculated.

    **5. `backend/database/seeders/VerificationSeeder.php`**:
    - Create 3 VerificationTemplates for each existing EquipmentCategory (or create 2 categories first if none exist)
    - Create 5-10 equipment with verification_frequency set
    - For each, create 3-5 Verification records (with params)
    - Ensure at least one verification has an outside_range param for testing
    - Run: `php artisan db:seed --class=VerificationSeeder`

    **6. MODIFY `backend/database/seeders/DatabaseSeeder.php`**:
    - Add `$this->call(VerificationSeeder::class);` call in the `run()` method

    **Permissions update (D-17):** Permissions `afericoes.view`, `afericoes.create`, `afericoes.edit` already exist in `RolePermissionSeeder.php` (lines 32-34). Confirm they are present. No changes needed to seeder.
  </action>
  <verify>
    <automated>php artisan db:seed --class=VerificationSeeder 2>&1; php artisan tinker --execute="echo app(App\Services\VerificationService::class)::class;" 2>&1</automated>
  </verify>
  <done>VerificationService.create() handles transaction + auto-calc + notifications. Factories produce test data. Seeder populates templates, verifications, params.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Unauthenticated requests crossing into Sanctum-guarded endpoints |
| operator→verification data | Operator-submitted param values that must be recalculated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | VerificationParam result value | high | mitigate | Result is calculated server-side in VerificationService (D-05), never accepted from client |
| T-09-02 | Tampering | Tolerance values | high | mitigate | tolerance_min/max stored in verification_templates, never editable via verification submission |
| T-09-03 | Elevation of Privilege | Verification creation | high | mitigate | Permission middleware `afericoes.create` on store endpoint (Plan 09-02) |
| T-09-04 | Spoofing | operator_id | medium | mitigate | operator_id always set from auth()->id() in VerificationService, never from request |
| T-09-05 | Denial of Service | Pending query | low | mitigate | Add composite index on (verification_frequency, verified_at); limit results to 100 |
| T-09-SC | Tampering | Package installs | high | mitigate | No new package installs required — slopcheck waived |
</threat_model>

<verification>
1. Migration runs: `php artisan migrate --pretend` includes all 3 tables + alter table
2. Models pass casts and relationship checks: `php artisan model:show Verification`
3. Seeder populates without errors: `php artisan db:seed --class=VerificationSeeder`
4. Factory generates valid data: `php artisan tinker --execute="App\Models\Verification::factory()->count(3)->create()"`
5. VerificationService create works: unit test with `VerificationServiceTest`
</verification>

<success_criteria>
- All 3 migration files created and runnable
- 3 models with correct relationships and casts
- VerificationResult enum with 3 cases + label/color methods
- VerificationService with transactional create() with auto-calc
- Factories and seeder producing test data
- Equipment has verification_frequency column and verifications() relationship
- Category has verificationTemplates() relationship
</success_criteria>

<output>
Create `.planning/phases/09-afericoes/09-01-SUMMARY.md` when done
</output>
