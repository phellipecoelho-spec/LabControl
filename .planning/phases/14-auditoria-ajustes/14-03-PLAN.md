---
phase: 14-auditoria-ajustes
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - backend/app/Http/Controllers/Api/V1/AuthController.php
  - backend/tests/Feature/Auth/RateLimitTest.php
  - backend/resources/views/auth/forgot-password.blade.php
  - backend/tests/Feature/Auth/ForgotPasswordSentViewTest.php
  - backend/app/Http/Controllers/Api/V1/VerificationController.php
  - backend/app/Services/VerificationService.php
  - backend/tests/Feature/VerificationUatFixTest.php
  - backend/app/Http/Controllers/Api/V1/MaintenanceOrderController.php
  - backend/app/Services/MaintenanceService.php
  - backend/tests/Feature/MaintenanceVerificationTest.php
  - backend/.env.example
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - VERF-01
  - VERF-02
  - MAINT-01
  - MAINT-02
must_haves:
  truths:
    - Rate limit funcional no login (X tentativas por minuto retornam 429)
    - E-mail de forgot password é enviado com view HTML bem formatada
    - Aferições (Phase 09) operam sem bugs — UAT criteria atendidos
    - Manutenções (Phase 10) operam sem bugs — verification criteria atendidos
    - Cada bug fix tem test associado que passa
  artifacts:
    - backend/tests/Feature/Auth/RateLimitTest.php
    - backend/tests/Feature/Auth/ForgotPasswordSentViewTest.php
    - backend/resources/views/auth/forgot-password.blade.php
    - backend/tests/Feature/VerificationUatFixTest.php
    - backend/tests/Feature/MaintenanceVerificationTest.php
  key_links:
    - AuthController → RateLimiter (login attempts)
    - ForgotPasswordController → Inertia/Blade view (forgot-password)
    - VerificationController → VerificationService (tolerance check, UAT-01)
    - MaintenanceOrderController → MaintenanceService (status transition, VERF-01)
---

<objective>
Corrigir bugs e gaps de verificação pendentes de fases anteriores (Phase 02 Autenticação, Phase 09 Aferições, Phase 10 Manutenções), com cada correção acompanhada de test automatizado.

Purpose: Fechar todos os gaps de verificação e UAT antes do v1.0.
Output: Tests + fixes para rate limit, forgot password view, aferições UAT, manutenções verification.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@backend/app/Http/Controllers/Api/V1/AuthController.php
@backend/tests/Feature/Auth/LoginTest.php
@backend/tests/Feature/Auth/PasswordResetTest.php
@backend/app/Http/Controllers/Api/V1/VerificationController.php
@backend/app/Services/VerificationService.php
@backend/app/Http/Controllers/Api/V1/MaintenanceOrderController.php
@backend/app/Services/MaintenanceService.php
@backend/tests/Feature/Http/Controllers/Api/V1/MaintenanceOrderControllerTest.php
@backend/app/Providers/RouteServiceProvider.php
@backend/routes/api.php
@backend/resources/views/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir gaps de Autenticação (Phase 02) — RateLimit + ForgotPasswordView</name>
  <files>
    backend/app/Http/Controllers/Api/V1/AuthController.php,
    backend/tests/Feature/Auth/RateLimitTest.php,
    backend/resources/views/auth/forgot-password.blade.php,
    backend/tests/Feature/Auth/ForgotPasswordSentViewTest.php
  </files>
  <behavior>
    - RateLimitTest: 6 tentativas de login com senha errada em 1 minuto → 429 Too Many Requests
    - ForgotPasswordSentViewTest: POST /forgot-password com email válido → retorna 200 + mensagem "Email enviado"
    - ForgotPasswordSentViewTest: POST /forgot-password com email inválido → retorna 422
  </behavior>
  <action>
    PER D-10 e D-11: Corrigir 3 gaps da Phase 02 (Autenticação).

    1. Rate Limit (AUTH-01):
       - Se AuthController não tem rate limiting, aplicar `RateLimiter` do Laravel no login:
         - Adicionar `use Illuminate\Cache\RateLimiter;` no controller
         - Método `login()`: antes de validar credenciais, verificar `RateLimiter::tooManyAttempts($key, $maxAttempts=5)`
         - Se excedido, retornar `response()->json(['message' => 'Muitas tentativas. Aguarde 1 minuto.'], 429)`
         - Após falha de login, incrementar `RateLimiter::hit($key, $decaySeconds=60)`
         - Após login bem-sucedido, limpar `RateLimiter::clear($key)`
         - Chave: `'login:' . $request->ip()`
       - Criar `RateLimitTest.php`: enviar 6 requisições POST /api/v1/auth/login com senha errada → 6ª retorna 429
       - Usar `$this->withHeader('Accept', 'application/json')` para evitar redirect

    2. ForgotPasswordSentView (AUTH-03):
       - Verificar se existe blade view para forgot-password. Se não existir:
         - Criar `resources/views/auth/forgot-password.blade.php`:
           - Template HTML mínimo com layout de email (não página web)
           - Conteúdo: "Clique no link abaixo para redefinir sua senha:" + link
           - Usar variável `$url` ou `$actionUrl` conforme Laravel Notifications
         - Verificar se `ForgotPasswordController` ou `PasswordResetLinkController` existe e envia email formatado
         - Se não existir notificação de reset de senha, verificar que `User` model implementa `MustVerifyEmail` + `CanResetPassword`
       - Criar `ForgotPasswordSentViewTest.php`:
         - Test: POST /api/v1/auth/forgot-password com email existente → 200 + JSON com mensagem de sucesso
         - Test: POST /api/v1/auth/forgot-password com email inexistente → 422
       - NÃO modificar fluxo de reset de senha existente — apenas garantir que view existe e endpoint responde corretamente

    3. E2E Playwright tests (gap mencionado):
       - Sinalizar como "deferred for v2" conforme CONTEXT.md (não implementar agora)
  </action>
  <verify>
    <automated>
      docker exec labcontrol-php php artisan test --filter=RateLimitTest --filter=ForgotPasswordSentViewTest 2>&1
    </automated>
  </verify>
  <done>
    RateLimitTest e ForgotPasswordSentViewTest criados e passando. Rate limiter ativo no login (5 tentativas/min → 429).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir UAT gaps de Aferições (Phase 09)</name>
  <files>
    backend/app/Http/Controllers/Api/V1/VerificationController.php,
    backend/app/Services/VerificationService.php,
    backend/tests/Feature/VerificationUatFixTest.php
  </files>
  <behavior>
    - VerificationUatFixTest: criar aferição com parâmetros dentro da tolerância → status "passed"
    - VerificationUatFixTest: criar aferição com parâmetros fora da tolerância → status "failed" + ToleranceExceeded notification
    - VerificationUatFixTest: listar aferições por equipamento → retorna histórico
  </behavior>
  <action>
    PER D-09 e D-11: Corrigir 6 UAT items da Phase 09 (Aferições).

    1. Carregar os UAT items do Phase 09 — verificar que a UAT foi definida em SPEC.md ou similar.
       (Como não há arquivo 09-UAT.md no disco, inferir do contexto: Phase 09 implementou VerificationController, VerificationService, e Notification ToleranceExceeded.)

    2. Ler VerificationController e VerificationService para identificar possíveis bugs:
       - Verificar fluxo de criação: `store()` → `VerificationService::create()`
       - Verificar fluxo de conclusão: `complete()` → `VerificationService::complete()`
       - Verificar que tolerância é calculada corretamente nos parâmetros da aferição
       - Verificar que notificação `ToleranceExceeded` é disparada quando resultado excede limites

    3. Corrigir bugs encontrados (hipóteses baseadas em padrão do código):
       - Se `VerificationService::create()` não associa equipamento corretamente → corrigir
       - Se cálculo de tolerância usa operadores incorretos → corrigir
       - Se `complete()` não atualiza status para "passed"/"failed" → corrigir
       - Se notificação não é enviada → garantir `Notification::send()` no controller

    4. Criar `VerificationUatFixTest.php` com testes:
       - test_can_create_verification: POST /api/v1/verifications → 201 + dados corretos
       - test_verification_within_tolerance_passes: criar verification com params dentro da tolerância → complete → status 'passed'
       - test_verification_exceeds_tolerance_fails: criar verification com params fora da tolerância → complete → status 'failed'
       - test_tolerance_exceeded_notification_is_sent: verificar que Notification::fake() captura ToleranceExceeded
       - test_verification_history_by_equipment: GET /api/v1/verifications?equipment_id=X → retorna lista

    5. Usar RefreshDatabase + RolePermissionSeeder + admin user pattern (como em MaintenanceOrderControllerTest).
  </action>
  <verify>
    <automated>
      docker exec labcontrol-php php artisan test --filter=VerificationUatFixTest 2>&1
    </automated>
  </verify>
  <done>
    VerificationUatFixTest criado com 5+ testes passando. Fluxo de criação, tolerância, notificação, e histórico funcionando.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Corrigir verification gaps de Manutenções (Phase 10)</name>
  <files>
    backend/app/Http/Controllers/Api/V1/MaintenanceOrderController.php,
    backend/app/Services/MaintenanceService.php,
    backend/tests/Feature/MaintenanceVerificationTest.php
  </files>
  <behavior>
    - MaintenanceVerificationTest: criar ordem com status 'open' → complete → status 'completed'
    - MaintenanceVerificationTest: tentar completar ordem já completada → 422
    - MaintenanceVerificationTest: tentar editar ordem em andamento → permissão/validação correta
  </behavior>
  <action>
    PER D-09 e D-11: Corrigir verification gaps da Phase 10 (Manutenções).

    1. Carregar verification gaps — inferir do contexto: MaintenanceOrderController implementa CRUD com service.
       Verificar que MaintenanceService lida corretamente com transições de status.

    2. Ler MaintenanceService e MaintenanceOrderController para identificar possíveis bugs:
       - Verificar fluxo `complete()`: deve mudar status para 'completed' e registrar data de conclusão
       - Verificar que ordens completadas não podem ser re-completadas
       - Verificar que apenas status válidos são aceitos nas transições

    3. Corrigir bugs encontrados:
       - Se `complete()` permite re-completar → adicionar validação de status atual
       - Se transição de status não valida origem → adicionar guard clause
       - Se MaintenanceOrderController::update() permite editar ordens concluídas → adicionar validação

    4. Criar `MaintenanceVerificationTest.php` com testes:
       - test_create_maintenance: POST /api/v1/maintenance-orders → 201 + status correto
       - test_complete_maintenance: POST /api/v1/maintenance-orders/{id}/complete → status 'completed'
       - test_cannot_complete_already_completed: tentar completar ordem já concluída → 422
       - test_cannot_edit_completed_order: PUT /api/v1/maintenance-orders/{id} em ordem concluída → 422
       - test_maintenance_cancellation: POST /api/v1/maintenance-orders/{id}/cancel → status 'cancelled'
       - test_maintenance_history_by_equipment: GET /api/v1/maintenance-orders?equipment_id=X → retorna lista

    5. Usar RefreshDatabase + RolePermissionSeeder + admin user + Equipment::factory (pattern existente em MaintenanceOrderControllerTest).
  </action>
  <verify>
    <automated>
      docker exec labcontrol-php php artisan test --filter=MaintenanceVerificationTest 2>&1
    </automated>
  </verify>
  <done>
    MaintenanceVerificationTest criado com 6+ testes passando. Transições de status validadas, proteção contra dupla conclusão implementada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Login requests (rate limiting) |
| API → Mail | Forgot password email (email delivery) |
| Client → API | Verification/Maintenance CRUD (status transitions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-B01 | Denial of Service | AuthController::login | medium | mitigate | RateLimiter implementado (5 tentativas/min por IP) — Task 1 |
| T-14-B02 | Elevation of Privilege | MaintenanceOrderController::update | medium | mitigate | Validação de transição de status impede edição de ordens concluídas — Task 3 |
| T-14-B03 | Tampering | VerificationService::complete | low | mitigate | Tolerância calculada server-side — não aceita resultado manipulado do client |
</threat_model>

<verification>
1. `php artisan test --filter=RateLimitTest` — 1 test, pass
2. `php artisan test --filter=ForgotPasswordSentViewTest` — 2 tests, pass
3. `php artisan test --filter=VerificationUatFixTest` — 5 tests, pass
4. `php artisan test --filter=MaintenanceVerificationTest` — 6 tests, pass
</verification>

<success_criteria>
- Rate limit funcional: 6ª tentativa de login em 1 minuto → 429
- Forgot password envia email com view HTML
- Aferições: criação, tolerância, notificação, histórico funcionando
- Manutenções: transições de status validadas, proteção contra dupla conclusão
- Total: 14+ novos testes passando
</success_criteria>

<output>
Criar `.planning/phases/14-auditoria-ajustes/14-03-SUMMARY.md` após execução.
</output>