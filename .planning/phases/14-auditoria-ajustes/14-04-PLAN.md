---
phase: 14-auditoria-ajustes
plan: 04
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - docker/docker-compose.yml
  - docker/nginx/default.conf
  - scripts/backup.sh
  - scripts/setup.sh
  - scripts/setup.ps1
  - .env.example
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - Docker Compose inicia todos os serviços com health checks
    - Script de backup do PostgreSQL funciona e gera arquivo .sql
    - Script de setup automatizado cria o ambiente do zero
    - .env.example documenta todas as variáveis necessárias
  artifacts:
    - docker/docker-compose.yml — health checks aprimorados para nginx e php
    - scripts/backup.sh — backup do banco PostgreSQL + limpeza de backups antigos
    - scripts/setup.sh — setup automatizado para Linux/Mac
    - scripts/setup.ps1 — setup automatizado para Windows
    - .env.example — documentação completa de variáveis de ambiente
  key_links:
    - docker-compose.yml → healthchecks (nginx, php, postgres, redis)
    - scripts/setup.sh → docker compose up → artisan migrate → npm build
    - .env.example → APP_KEY, DB_*, REDIS_*, MAIL_*
---

<objective>
Preparar o ambiente para deploy: finalizar Docker Compose com health checks, criar scripts de backup e setup, documentar variáveis de ambiente.

Purpose: Garantir que o sistema pode ser implantado em qualquer ambiente com um comando.
Output: Docker configurado + scripts de backup/setup + .env documentado.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@docker/docker-compose.yml
@docker/nginx/default.conf
@docker/php/Dockerfile
@scripts/setup.ps1
@backend/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalizar Docker Compose com health checks completos e scripts de backup</name>
  <files>
    docker/docker-compose.yml,
    scripts/backup.sh,
    docker/nginx/default.conf
  </files>
  <action>
    PER D-12, D-13:

    1. Docker Compose (`docker/docker-compose.yml`):
       - Manter estrutura existente (serviços nginx, php, postgres, redis)
       - **Nginx**: Adicionar `healthcheck`:
         ```yaml
         healthcheck:
           test: ["CMD", "curl", "-f", "http://localhost/health"]
           interval: 30s
           timeout: 10s
           retries: 3
         ```
       - **PHP**: Adicionar `healthcheck`:
         ```yaml
         healthcheck:
           test: ["CMD", "php", "-r", "echo 'ok';"]
           interval: 30s
           timeout: 10s
           retries: 3
         ```
       - **Redis**: Adicionar `healthcheck` (já existe no postgres):
         ```yaml
         healthcheck:
           test: ["CMD", "redis-cli", "ping"]
           interval: 10s
           timeout: 5s
           retries: 3
         ```
       - Adicionar `restart: unless-stopped` em todos os serviços
       - Adicionar `depends_on` com `condition: service_healthy` para:
         - nginx depende de php (health)
         - php depende de postgres (health) + redis (health)
       - NÃO modificar credenciais (ainda hardcoded — production deploy usará .env)
       - NÃO remover volumes existentes (postgres_data, redis_data)

    2. Script de backup (`scripts/backup.sh`):
       - PER D-13: Script bash para Linux/Mac
       ```bash
       #!/bin/bash
       # LabControl PostgreSQL Backup Script
       TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
       BACKUP_DIR="/backups"
       DB_NAME="${DB_NAME:-labcontrol}"
       DB_USER="${DB_USER:-labcontrol}"
       DB_HOST="${DB_HOST:-localhost}"
       RETENTION_DAYS="${RETENTION_DAYS:-30}"

       mkdir -p "$BACKUP_DIR"
       pg_dump -h "$DB_HOST" -U "$DB_USER" -d "$DB_NAME" -F c -f "$BACKUP_DIR/${DB_NAME}_${TIMESTAMP}.dump"
       gzip "$BACKUP_DIR/${DB_NAME}_${TIMESTAMP}.dump"
       find "$BACKUP_DIR" -name "${DB_NAME}_*.dump.gz" -mtime +$RETENTION_DAYS -delete
       echo "Backup: $BACKUP_DIR/${DB_NAME}_${TIMESTAMP}.dump.gz"
       ```
       - Tornar executável: `chmod +x scripts/backup.sh`

    3. Nginx (`docker/nginx/default.conf`):
       - Verificar se tem rota /health para o healthcheck
       - Se não existir, adicionar no server block:
         ```
         location /health {
             access_log off;
             return 200 "healthy\n";
             add_header Content-Type text/plain;
         }
         ```
       - Garantir que proxy passa headers corretos para API:
         ```
         proxy_set_header X-Real-IP $remote_addr;
         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
         proxy_set_header X-Forwarded-Proto $scheme;
         ```
  </action>
  <verify>
    <automated>
      docker compose -f docker/docker-compose.yml config 2>&1
    </automated>
  </verify>
  <done>
    docker-compose.yml com health checks em todos os 4 serviços, restart policy, dependências saudáveis. backup.sh criado com pg_dump + compressão + rotação (30 dias).
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts de setup automatizado e documentar .env</name>
  <files>
    scripts/setup.sh,
    scripts/setup.ps1,
    .env.example
  </files>
  <action>
    PER D-14, D-15:

    1. Script de setup Linux/Mac (`scripts/setup.sh`):
       ```bash
       #!/bin/bash
       set -euo pipefail

       echo "=== LabControl Setup ==="

       # Verificar pré-requisitos
       command -v docker >/dev/null 2>&1 || { echo "Docker não encontrado"; exit 1; }
       command -v docker compose >/dev/null 2>&1 || { echo "Docker Compose não encontrado"; exit 1; }

       # Copiar .env se não existir
       if [ ! -f backend/.env ]; then
           cp .env.example backend/.env
           echo ".env criado a partir de .env.example — ajuste as variáveis se necessário"
       fi

       # Build e iniciar containers
       docker compose -f docker/docker-compose.yml build
       docker compose -f docker/docker-compose.yml up -d

       # Aguardar banco
       echo "Aguardando PostgreSQL..."
       until docker compose -f docker/docker-compose.yml exec -T postgres pg_isready -U labcontrol; do
           sleep 2
       done

       # Instalar dependências e rodar migrations
       docker compose -f docker/docker-compose.yml exec -T php composer install --no-interaction
       docker compose -f docker/docker-compose.yml exec -T php php artisan key:generate --force
       docker compose -f docker/docker-compose.yml exec -T php php artisan migrate --force
       docker compose -f docker/docker-compose.yml exec -T php php artisan db:seed --force

       # Frontend
       docker compose -f docker/docker-compose.yml exec -T php npm install
       docker compose -f docker/docker-compose.yml exec -T php npm run build

       echo "=== Setup completo! ==="
       echo "Frontend: http://localhost"
       echo "Backend API: http://localhost/api"
       ```

    2. Script de setup Windows (`scripts/setup.ps1`):
       - Já existe parcialmente — ler e atualizar com melhorias:
       ```powershell
       # LabControl Setup Script (Windows)
       Write-Host "=== LabControl Setup ===" -ForegroundColor Cyan

       # Verificar Docker
       $dockerExists = Get-Command docker -ErrorAction SilentlyContinue
       if (-not $dockerExists) {
           Write-Error "Docker não encontrado. Instale Docker Desktop primeiro."
           exit 1
       }

       # Copiar .env se não existir
       if (-not (Test-Path "backend\.env")) {
           Copy-Item ".env.example" "backend\.env"
           Write-Host ".env criado a partir de .env.example" -ForegroundColor Yellow
       }

       # Build e iniciar
       docker compose -f docker/docker-compose.yml build
       docker compose -f docker/docker-compose.yml up -d

       # Aguardar banco
       Write-Host "Aguardando PostgreSQL..." -ForegroundColor Yellow
       do {
           Start-Sleep -Seconds 2
           $ready = docker compose -f docker/docker-compose.yml exec -T postgres pg_isready -U labcontrol 2>$null
       } while ($LASTEXITCODE -ne 0)

       # Instalar dependências
       docker compose -f docker/docker-compose.yml exec -T php composer install --no-interaction
       docker compose -f docker/docker-compose.yml exec -T php php artisan key:generate --force
       docker compose -f docker/docker-compose.yml exec -T php php artisan migrate --force
       docker compose -f docker/docker-compose.yml exec -T php php artisan db:seed --force

       # Frontend
       docker compose -f docker/docker-compose.yml exec -T php npm install
       docker compose -f docker/docker-compose.yml exec -T php npm run build

       Write-Host "=== Setup completo! ===" -ForegroundColor Green
       Write-Host "Frontend: http://localhost"
       Write-Host "Backend API: http://localhost/api"
       ```

    3. .env.example (`backend/.env.example` — ou raiz):
       - PER D-15: Documentar TODAS as variáveis necessárias com comentários descritivos:
       ```ini
       # === LabControl Environment Configuration ===
       # Copy this file to .env and adjust values for your environment

       # Application
       APP_NAME=LabControl
       APP_ENV=local
       APP_KEY=
       APP_DEBUG=true
       APP_URL=http://localhost

       # Database (PostgreSQL)
       DB_CONNECTION=pgsql
       DB_HOST=postgres
       DB_PORT=5432
       DB_DATABASE=labcontrol
       DB_USERNAME=labcontrol
       DB_PASSWORD=labcontrol

       # Redis (Cache & Queue)
       REDIS_HOST=redis
       REDIS_PORT=6379
       REDIS_PASSWORD=null
       CACHE_DRIVER=redis
       QUEUE_CONNECTION=redis

       # Session
       SESSION_DRIVER=redis
       SESSION_LIFETIME=120
       SESSION_DOMAIN=localhost
       SANCTUM_STATEFUL_DOMAINS=localhost

       # Mail (SMTP)
       MAIL_MAILER=smtp
       MAIL_HOST=mailpit
       MAIL_PORT=1025
       MAIL_USERNAME=null
       MAIL_PASSWORD=null
       MAIL_ENCRYPTION=null
       MAIL_FROM_ADDRESS="labcontrol@example.com"
       MAIL_FROM_NAME="${APP_NAME}"

       # Filesystem
       FILESYSTEM_DISK=local
       # Para produção, usar S3:
       # FILESYSTEM_DISK=s3
       # AWS_ACCESS_KEY_ID=
       # AWS_SECRET_ACCESS_KEY=
       # AWS_DEFAULT_REGION=us-east-1
       # AWS_BUCKET=

       # Logging
       LOG_CHANNEL=stack
       LOG_LEVEL=debug

       # PWA
       VITE_APP_URL=http://localhost
       ```
       - Salvar como `.env.example` na raiz (não dentro de backend/)
       - NÃO incluir valores sensíveis reais — usar valores dummy com instruções
  </action>
  <verify>
    <automated>
      # Verificar que scripts existem e têm permissão de execução
      Test-Path "scripts/setup.sh"
      Test-Path "scripts/setup.ps1"
      Test-Path ".env.example"
    </automated>
  </verify>
  <done>
    setup.sh (Linux/Mac), setup.ps1 (Windows), e .env.example criados. Backup script pronto. Docker Compose com health checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker | Rede Docker expõe portas 80, 5432, 6379 |
| Docker → PostgreSQL | Conexão de banco com credenciais |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-D01 | Information Disclosure | docker-compose.yml (credenciais hardcoded) | high | accept | Válido para dev. Produção deve usar .env com docker-compose env_file |
| T-14-D02 | Spoofing | backup.sh (acesso a pg_dump) | low | mitigate | Script roda no host com permissões de usuário — sem sudo |
| T-14-D03 | Tampering | setup.sh (execução como root) | medium | mitigate | Script usa `set -euo pipefail` e verifica docker antes de executar |
</threat_model>

<verification>
1. `docker compose -f docker/docker-compose.yml config` — YAML válido
2. Testar manualmente: `docker compose -f docker/docker-compose.yml up -d` → todos health checks passam
3. `chmod +x scripts/backup.sh && scripts/backup.sh` (com banco rodando)
4. Verificar .env.example contém variáveis APP_KEY, DB_*, REDIS_*, MAIL_*
</verification>

<success_criteria>
- Docker Compose funcional com health checks em 4 serviços
- backup.sh: pg_dump + compressão gzip + rotação (30 dias)
- setup.sh / setup.ps1: setup completo em 1 comando
- .env.example com todas as variáveis documentadas
</success_criteria>

<output>
Criar `.planning/phases/14-auditoria-ajustes/14-04-SUMMARY.md` após execução.
</output>