---
phase: 14-auditoria-ajustes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/Models/Equipment.php
  - backend/app/Models/InventoryItem.php
  - backend/app/Models/Loan.php
  - backend/app/Models/Calibration.php
  - backend/app/Models/Verification.php
  - backend/app/Models/MaintenanceOrder.php
  - backend/app/Models/User.php
  - backend/app/Http/Controllers/Api/V1/EquipmentController.php
  - backend/app/Http/Controllers/Api/V1/InventoryItemController.php
  - backend/app/Http/Controllers/Api/V1/LoanController.php
  - backend/app/Http/Controllers/Api/V1/CalibrationController.php
  - backend/app/Http/Controllers/Api/V1/VerificationController.php
  - backend/app/Http/Controllers/Api/V1/MaintenanceOrderController.php
  - backend/tests/Feature/AuditCoverageEquipmentTest.php
  - backend/tests/Feature/AuditCoverageInventoryTest.php
  - backend/tests/Feature/AuditCoverageLoanTest.php
  - backend/tests/Feature/AuditCoverageCalibrationTest.php
  - backend/tests/Feature/AuditCoverageVerificationTest.php
  - backend/tests/Feature/AuditCoverageMaintenanceTest.php
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - LOGS-01
  - LOGS-02
must_haves:
  truths:
    - Auditoria cobre todos os módulos CRUD (create, update, delete geram ActivityLog)
    - Testes automatizados provam a cobertura de auditoria para cada módulo
    - Tabela de rastreabilidade reflete o status real de cada requisito v1
  artifacts:
    - AuditCoverageEquipmentTest.php — feature tests para Equipment CRUD → ActivityLog
    - AuditCoverageInventoryTest.php — feature tests para InventoryItem CRUD → ActivityLog
    - AuditCoverageLoanTest.php — feature tests para Loan CRUD → ActivityLog
    - AuditCoverageCalibrationTest.php — feature tests para Calibration CRUD → ActivityLog
    - AuditCoverageVerificationTest.php — feature tests para Verification CRUD → ActivityLog
    - AuditCoverageMaintenanceTest.php — feature tests para MaintenanceOrder CRUD → ActivityLog
    - REQUIREMENTS.md atualizado com status real, coluna Module e Verified by
  key_links:
    - Controller → Model → LogsActivity trait → ActivityLog
    - Teste → assertion → activity_logs table
    - REQUIREMENTS.md traceability table → Phase 14 status audit
---

<objective>
Verificar e garantir que todos os controllers CRUD dos módulos do sistema geram registros de auditoria via LogsActivity trait, criar testes de feature que comprovam a cobertura, e atualizar a tabela de rastreabilidade de requisitos.

Purpose: Garantir conformidade com LOGS-01 (auditar todas as operações) e LOGS-02 (visualizar logs por módulo).
Output: Testes de auditoria para 6 módulos + REQUIREMENTS.md atualizado.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@backend/app/Traits/LogsActivity.php
@backend/app/Models/ActivityLog.php
@backend/app/Http/Controllers/Api/V1/ActivityLogController.php
@backend/tests/Feature/ActivityLogTest.php
@backend/tests/Feature/EquipmentApiTest.php
@backend/tests/Feature/Http/Controllers/Api/V1/MaintenanceOrderControllerTest.php
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar e garantir cobertura de LogsActivity em todos os módulos</name>
  <files>
    backend/app/Models/Equipment.php,
    backend/app/Models/InventoryItem.php,
    backend/app/Models/Loan.php,
    backend/app/Models/Calibration.php,
    backend/app/Models/Verification.php,
    backend/app/Models/MaintenanceOrder.php,
    backend/app/Models/User.php
  </files>
  <action>
    PER D-03: Verificar que TODOS os modelos dos módulos (Equipment, InventoryItem, Loan, Calibration, Verification, MaintenanceOrder, User) já usam `use App\Traits\LogsActivity;` e incluem `LogsActivity` no `use HasFactory, ...` da classe.

    1. Verificar cada model listado — confirmar que LogsActivity está presente (via grep).
       Equipamentos: Equipment já usa LogsActivity (linha 5 e 14).
       Estoque: InventoryItem usa LogsActivity.
       Empréstimos: Loan usa LogsActivity.
       Calibrações: Calibration usa LogsActivity.
       Aferições: Verification usa LogsActivity.
       Manutenções: MaintenanceOrder usa LogsActivity.
       Usuários: User usa LogsActivity.

    2. Para cada model que JÁ usa LogsActivity, NÃO modificar. A trait já foi aplicada via bootLogsActivity nos eventos created/updated/deleted.

    3. Para cadastro/logout/login manual (não via model events como AuthController):
       - Em AuthController, verificar que `login`, `login_failed`, e `logout` geram ActivityLog direto no controller (padrão existente em ActivityLogTest).
       - Se algum controller/service faz operações CRUD sem model events (ex: operações em lote, transações complexas), adicionar chamada explícita a `ActivityLog::create(...)`.

    4. NÃO modificar models que já usam LogsActivity — apenas verificar e documentar cobertura.
    5. NÃO adicionar LogsActivity em CategoryController, ManufacturerController, SupplierController, RoleController, ProfileController (são CRUD de suporte/mestre — escopo LOGS-01 cobre módulos principais).
  </action>
  <verify>
    <automated>
      Get-Content backend/app/Models/Equipment.php | Select-String "LogsActivity" | Measure-Object | % { $_.Count -ge 1 }
      Get-Content backend/app/Models/InventoryItem.php | Select-String "LogsActivity" | Measure-Object | % { $_.Count -ge 1 }
      Get-Content backend/app/Models/Loan.php | Select-String "LogsActivity" | Measure-Object | % { $_.Count -ge 1 }
      Get-Content backend/app/Models/Calibration.php | Select-String "LogsActivity" | Measure-Object | % { $_.Count -ge 1 }
      Get-Content backend/app/Models/Verification.php | Select-String "LogsActivity" | Measure-Object | % { $_.Count -ge 1 }
      Get-Content backend/app/Models/MaintenanceOrder.php | Select-String "LogsActivity" | Measure-Object | % { $_.Count -ge 1 }
    </automated>
  </verify>
  <done>
    Todos os 6+ modelos principais confirmados com LogsActivity. Documentação do escopo de cobertura de auditoria registrada.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar feature tests de auditoria para cada módulo</name>
  <files>
    backend/tests/Feature/AuditCoverageEquipmentTest.php,
    backend/tests/Feature/AuditCoverageInventoryTest.php,
    backend/tests/Feature/AuditCoverageLoanTest.php,
    backend/tests/Feature/AuditCoverageCalibrationTest.php,
    backend/tests/Feature/AuditCoverageVerificationTest.php,
    backend/tests/Feature/AuditCoverageMaintenanceTest.php
  </files>
  <behavior>
    - Cada test: autentica admin → executa CREATE → assertDatabaseHas('activity_logs', ['action'=>'created', 'module'=>'{ModelName}'])
    - Cada test: autentica admin → executa UPDATE → assertDatabaseHas('activity_logs', ['action'=>'updated', 'module'=>'{ModelName}'])
    - Cada test: autentica admin → executa DELETE → assertDatabaseHas('activity_logs', ['action'=>'deleted', 'module'=>'{ModelName}'])
    - Tratar modelos que usam service layer (Calibration, Loan, Verification) — testar via controller endpoint, não service direto
  </behavior>
  <action>
    PER D-04: Criar 6 arquivos de teste seguindo o padrão de ActivityLogTest.php, cada um focado em um módulo.

    Usar o padrão estabelecido:
    - `use RefreshDatabase`
    - `setUp()` com seed `RolePermissionSeeder` + criar usuário admin
    - Sanctum::actingAs para autenticação
    - Factory para criar registros iniciais
    - assertDatabaseHas para verificar activity_logs após cada operação

    Criar tests para Equipment (já tem API tests — adicionar assertions de activity_logs no create/update/delete):
    - EquipmentAuditTest.php: test_equipment_creation_logs_activity, test_equipment_update_logs_activity, test_equipment_deletion_logs_activity

    Para InventoryItem (já existe InventoryItem com factory):
    - AuditCoverageInventoryTest.php: test_inventory_creation_logs_activity, test_inventory_update_logs_activity, test_inventory_deletion_logs_activity

    Para Loan (usa LoanService): 
    - AuditCoverageLoanTest.php: test_loan_creation_logs_activity (via POST /api/v1/loans), test_loan_update_logs_activity, test_loan_deletion_logs_activity, test_loan_activate_logs_activity, test_loan_return_logs_activity

    Para Calibration (usa CalibrationService):
    - AuditCoverageCalibrationTest.php: test_calibration_creation_logs_activity, test_calibration_update_logs_activity, test_calibration_deletion_logs_activity, test_calibration_complete_logs_activity

    Para Verification (usa VerificationService):
    - AuditCoverageVerificationTest.php: test_verification_creation_logs_activity, test_verification_update_logs_activity, test_verification_deletion_logs_activity

    Para MaintenanceOrder (usa MaintenanceService):
    - AuditCoverageMaintenanceTest.php: test_maintenance_creation_logs_activity, test_maintenance_update_logs_activity, test_maintenance_deletion_logs_activity, test_maintenance_complete_logs_activity

    Cada test deve cobrir no mínimo as 3 operações CRUD base (create, update, delete).
    Usar factories existentes nos namespaces Database\Factories.
    NÃO recriar factories — usar Equipment::factory(), Calibration::factory(), etc.
  </action>
  <verify>
    <automated>
      docker exec labcontrol-php php artisan test --filter=AuditCoverage 2>&1
    </automated>
  </verify>
  <done>
    6 arquivos de teste criados, mínimo 3 assertions por arquivo. Todos passam: `php artisan test --filter=AuditCoverage` retorna 0 failures.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar tabela de rastreabilidade em REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    PER D-20 e D-21: Atualizar a tabela `## Traceability` em REQUIREMENTS.md:

    1. Adicionar colunas "Module" e "Verified by" ao cabeçalho da tabela
    2. Mapear cada requisito ao módulo correspondente:
       - INFRA-01/02/03 → Infraestrutura
       - AUTH-01/02/03/04 → Autenticação
       - USERS-01/02/03 → Usuários
       - LAYOUT-01/02/03 → Layout
       - EQUIP-01/02/03/04 → Equipamentos
       - INVT-01/02/03 → Estoque
       - LOAN-01/02/03 → Empréstimos
       - CAL-01/02/03/04 → Calibrações
       - VERF-01/02 → Aferições
       - MAINT-01/02 → Manutenções
       - DASH-01/02 → Dashboard
       - REPT-01/02 → Relatórios
       - PWA-01/02 → PWA
       - LOGS-01/02 → Auditoria
    3. Atualizar status de cada requisito com informação real (não chutar):
       - Manter Completed para INVT-01, INVT-02, CAL-01 a CAL-04
       - Manter Complete para CAL-01 a CAL-04 (padronizar para "Complete" ou "Completed")
       - Manter Pending para requisitos que realmente não foram implementados
       - LOGS-01 e LOGS-02 mudar para "Complete" (já implementado via LogsActivity trait + AuditLogsPage.vue)
    4. Adicionar coluna "Verified by" com:
       - "Phase 14 — Plan 01 AuditTests" para requisitos com cobertura de auditoria testada
       - "Manual" para requisitos sem teste automatizado dedicado
       - deixar vazio para Pending
    5. Atualizar contagem de coverage no rodapé
    6. NÃO modificar v2 requirements (MOBL-01 etc.) ou Out of Scope
  </action>
  <verify>
    <automated>
      # Verificar que colunas Module e Verified by existem na tabela de traceability
      php -r "
        \$content = file_get_contents('.planning/REQUIREMENTS.md');
        \$hasModule = strpos(\$content, 'Module') !== false;
        \$hasVerifiedBy = strpos(\$content, 'Verified by') !== false;
        echo (\$hasModule && \$hasVerifiedBy) ? 'PASS: Columns found' : 'FAIL: Columns missing';
      "
    </automated>
  </verify>
  <done>
    REQUIREMENTS.md atualizado com 38 requisitos mapeados, colunas Module e Verified by adicionadas, status reais refletidos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → ActivityLog | Admin autenticado gera registros de auditoria; logs não podem ser alterados após criação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | activity_logs table | medium | mitigate | LogsActivity trait usa somente INSERT — models não expõem update/delete na tabela de logs; verificar que nenhum controller permite alterar logs |
| T-14-02 | Information Disclosure | AuditLogsPage.vue | low | mitigate | Já protegido por middleware `permission:auditoria.view` no ActivityLogController |
| T-14-03 | Repudiation | activity_logs.user_id | medium | mitigate | `auth()->id()` garante que usuário autenticado é registrado; se auth falhar, user_id fica null (login/logout exceptions tratadas separadamente) |
</threat_model>

<verification>
1. `php artisan test --filter=AuditCoverage` — todos os 6 test files passam
2. `grep -c "Module" .planning/REQUIREMENTS.md` — coluna Module existe
3. `grep -c "Verified by" .planning/REQUIREMENTS.md` — coluna Verified by existe
</verification>

<success_criteria>
- 6 feature test files criados e passando
- Cada módulo testado: Equipment, Inventory, Loan, Calibration, Verification, Maintenance
- Cada test cobre create, update, delete → ActivityLog assertions
- REQUIREMENTS.md com traceability table atualizada (38 requitos, colunas Module + Verified by)
- Coverage contagem reflete status real
</success_criteria>

<output>
Criar `.planning/phases/14-auditoria-ajustes/14-01-SUMMARY.md` após execução.
</output>