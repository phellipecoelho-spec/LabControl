---
phase: 14-auditoria-ajustes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/EmptyState.vue
  - frontend/src/components/LoadingSkeleton.vue
  - frontend/src/modules/equipment/pages/EquipmentListPage.vue
  - frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
  - frontend/src/modules/inventory/pages/InventoryItemListPage.vue
  - frontend/src/modules/inventory/pages/InventoryItemDetailPage.vue
  - frontend/src/modules/loans/pages/LoanListPage.vue
  - frontend/src/modules/loans/pages/LoanDetailPage.vue
  - frontend/src/modules/calibrations/pages/CalibrationListPage.vue
  - frontend/src/modules/calibrations/pages/CalibrationDetailPage.vue
  - frontend/src/modules/verifications/pages/VerificationPendingPage.vue
  - frontend/src/modules/maintenance/pages/MaintenanceListPage.vue
  - frontend/src/modules/maintenance/pages/MaintenanceDetailPage.vue
  - frontend/src/modules/reports/pages/ReportPage.vue
  - frontend/src/modules/dashboard/pages/DashboardPage.vue
  - frontend/src/styles/empty-state.css
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
must_haves:
  truths:
    - Todas as páginas de lista exibem EmptyState quando não há dados
    - Todas as páginas exibem loading skeleton (PrimeVue Skeleton) durante carregamento
    - Todos os formulários têm validação visual consistente (mensagens de erro PrimeVue)
    - Dark mode consistente em todas as páginas
    - Interface é responsiva em mobile (menus, tabelas, formulários)
  artifacts:
    - frontend/src/components/EmptyState.vue — componente reutilizável com ilustração + texto + CTA
    - frontend/src/components/LoadingSkeleton.vue — componente de loading skeleton reutilizável
    - frontend/src/styles/empty-state.css — estilos globais para empty states
  key_links:
    - EmptyState → ListPage (exibido condicionalmente quando lista vazia)
    - LoadingSkeleton → Page (exibido durante loading state)
    - Form validation → PrimeVue Message/InlineMessage (padrão visual único)
---

<objective>
Realizar polimento de UI em todos os módulos do sistema: criar componentes reutilizáveis de EmptyState e LoadingSkeleton, padronizar estados vazios, verificar responsividade mobile, consistência dark mode, e validação visual de formulários.

Purpose: Garantir experiência de usuário profissional e consistente em todo o sistema antes do v1.0.
Output: Componentes reutilizáveis + integração em todas as páginas.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@frontend/src/modules/dashboard/components/EmptyState.vue
@frontend/src/modules/equipment/pages/EquipmentListPage.vue
@frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
@frontend/src/modules/inventory/pages/InventoryItemListPage.vue
@frontend/src/modules/loans/pages/LoanListPage.vue
@frontend/src/modules/calibrations/pages/CalibrationListPage.vue
@frontend/src/modules/maintenance/pages/MaintenanceListPage.vue
@frontend/src/modules/reports/pages/ReportPage.vue
@frontend/src/modules/dashboard/pages/DashboardPage.vue
@frontend/src/App.vue
@frontend/src/styles/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componentes reutilizáveis EmptyState e LoadingSkeleton</name>
  <files>
    frontend/src/components/EmptyState.vue,
    frontend/src/components/LoadingSkeleton.vue,
    frontend/src/styles/empty-state.css
  </files>
  <action>
    PER D-06, D-07, D-08:

    1. Criar `frontend/src/components/EmptyState.vue`:
       - Props: `icon` (string, default: 'pi pi-inbox'), `title` (string), `description` (string), `actionLabel` (string, optional), `actionRoute` (string, optional), `actionIcon` (string, default: 'pi pi-plus')
       - Slots: `actions` (para custom CTAs além do botão padrão)
       - Template: ilustração (ícone PrimeVue grande opaco) + título em negrito + descrição + botão CTA condicional
       - Estilos scoped: centralizado, max-width 400px, padding 4rem 1.5rem
       - Usar componentes PrimeVue (Button) — não criar novos componentes de propósito geral (D-07)

    2. Criar `frontend/src/components/LoadingSkeleton.vue`:
       - Props: `variant` ('table' | 'card' | 'form' | 'detail'), `rows` (number, default: 5)
       - Template condicional:
         - `table`: DataTable simulada com Skeleton rows + columns
         - `card`: Card simulado com Skeleton + texto
         - `form`: Formulário simulado com Skeleton para inputs
         - `detail`: Detalhe simulado com Skeleton para labels + valores
       - Usar componentes `<Skeleton>` do PrimeVue

    3. Criar `frontend/src/styles/empty-state.css` (global, importado em main.ts se não existir):
       - Classe `.empty-state-illustration` para ícone grande e opaco (opacidade 0.3-0.4)
       - Classe `.empty-state-container` para centralização
       - NÃO duplicar estilos scoped — manter no componente.

    4. NÃO modificar o EmptyState existente em dashboard/components — deixá-lo como está.
       O novo EmptyState em components/ é o reutilizável para todas as páginas.
  </action>
  <verify>
    <automated>
      # Verificar que os arquivos foram criados
      Test-Path "frontend/src/components/EmptyState.vue"
      Test-Path "frontend/src/components/LoadingSkeleton.vue"
    </automated>
  </verify>
  <done>
    EmptyState.vue (componente reutilizável com props icon/title/description/action) e LoadingSkeleton.vue (variant table/card/form/detail) criados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar EmptyState e LoadingSkeleton em todas as páginas de lista e detalhe</name>
  <files>
    frontend/src/modules/equipment/pages/EquipmentListPage.vue,
    frontend/src/modules/equipment/pages/EquipmentDetailPage.vue,
    frontend/src/modules/inventory/pages/InventoryItemListPage.vue,
    frontend/src/modules/inventory/pages/InventoryItemDetailPage.vue,
    frontend/src/modules/loans/pages/LoanListPage.vue,
    frontend/src/modules/loans/pages/LoanDetailPage.vue,
    frontend/src/modules/calibrations/pages/CalibrationListPage.vue,
    frontend/src/modules/calibrations/pages/CalibrationDetailPage.vue,
    frontend/src/modules/verifications/pages/VerificationPendingPage.vue,
    frontend/src/modules/maintenance/pages/MaintenanceListPage.vue,
    frontend/src/modules/maintenance/pages/MaintenanceDetailPage.vue,
    frontend/src/modules/reports/pages/ReportPage.vue,
    frontend/src/modules/dashboard/pages/DashboardPage.vue
  </files>
  <action>
    PER D-06: Verificar e adicionar estados de empty + loading em cada página.

    Para cada página de lista (EquipmentListPage, InventoryItemListPage, LoanListPage, CalibrationListPage, VerificationPendingPage, MaintenanceListPage, ReportPage):
    1. Ler o componente existente para entender estrutura de template e script.
    2. Adicionar loading state ref (boolean, default true).
    3. No template, usar `v-if="loading"` → `<LoadingSkeleton variant="table" />`.
    4. Usar `v-else-if="items.length === 0"` → `<EmptyState>` com título e descrição contextual + CTA de criação.
    5. Usar `v-else` → manter template existente.
    6. No script setup, importar EmptyState e LoadingSkeleton dos componentes globais.

    Para cada página de detalhe (EquipmentDetailPage, InventoryItemDetailPage, LoanDetailPage, CalibrationDetailPage, MaintenanceDetailPage):
    1. Adicionar loading state.
    2. Template: `v-if="loading"` → `<LoadingSkeleton variant="detail" />`.
    3. `v-else-if="!item"` → `<EmptyState>` com "Registro não encontrado".
    4. `v-else` → manter template existente.

    Para DashboardPage:
    1. Já possui EmptyState do dashboard. Verificar se usa loading skeleton.
    2. Adicionar LoadingSkeleton variant="card" se ausente.

    Verificar também:
    - Dark mode consistency: todas as páginas devem usar classes `p-*` do PrimeVue que respeitam tema escuro
    - Se alguma página usa cor fixa (ex: `color: #000`), substituir por variável CSS ou classe PrimeVue
    - NÃO alterar lógica de negócio — apenas adicionar estados de UI
    - NÃO remover funcionalidades existentes
  </action>
  <verify>
    <automated>MISSING — Wave 0 deve verificar via inspeção visual em ambiente dev (frontend rodando).</automated>
  </verify>
  <done>
    Todas as 13+ páginas modificadas têm estados de loading (Skeleton) e empty (EmptyState) implementados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Auditar e padronizar responsividade mobile, dark mode, e validação visual de formulários</name>
  <files>
    frontend/src/styles/empty-state.css,
    frontend/src/modules/equipment/pages/EquipmentFormPage.vue,
    frontend/src/modules/inventory/pages/InventoryItemFormPage.vue
  </files>
  <action>
    PER D-06: Auditar responsividade, dark mode e validação visual.

    1. Responsividade mobile:
       - Verificar que tabelas usam `scrollable` (DataTable prop) para overflow horizontal
       - Verificar que formulários usam `fluid` prop em inputs PrimeVue
       - Verificar sidebars/menus: drawer mode para mobile (já implementado em Phase 04)
       - Em páginas de formulário (EquipmentFormPage, InventoryItemFormPage): adicionar wrapper responsivo com grid CSS (1 coluna mobile, 2 colunas desktop)
       - NÃO quebrar layout desktop existente

    2. Validação visual de formulários:
       - Verificar que todos os formulários usam `<Message>` ou `<InlineMessage>` do PrimeVue para erros
       - Verificar que inputs usam `:invalid` ou `v-invalid` do PrimeVue (borda vermelha)
       - Verificar que mensagens de erro são exibidas via `v-if="errors.field"` próximo ao input
       - Padronizar: usar pattern `v-model="form.field" :class="{ 'p-invalid': errors.field }"` + `<small class="p-error" v-if="errors.field">{{ errors.field }}</small>`
       - NÃO criar sistema de validação próprio — usar validação PrimeVue existente

    3. Dark mode consistency:
       - Verificar que cores fixas (ex: `#fff`, `#000`, `#333`) são substituídas por variáveis CSS:
         - `var(--p-surface-{50..950})` para backgrounds
         - `var(--p-text-color)` para texto
         - `var(--p-primary-color)` para cor primária
       - Verificar que todos os cards usam `p-card` e não divs com background fixo
       - Verificar que modals/dialogs usam `p-dialog` e não divs custom
       - Em `App.vue`, garantir que o atributo `data-p-theme="dark"` está presente consistentemente

    4. NÃO modificar lógica de negócios, API calls, ou stores.
    NÃO adicionar dependências npm.
  </action>
  <verify>
    <automated>MISSING — Wave 0 deve verificar em ambiente dev com resize de janela + toggle dark mode no navegador.</automated>
  </verify>
  <done>
    Responsividade mobile verificada e ajustada, dark mode consistente (sem cores fixas), formulários com validação visual PrimeVue padronizada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Browser | Dados exibidos na UI (sem risco de segurança, apenas UX) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-UI1 | Information Disclosure | EmptyState icon/description | low | accept | EmptyState exibe texto informativo sobre módulo vazio — sem dados sensíveis |
| T-14-UI2 | Denial of Service | LoadingSkeleton com loops infinitos | low | mitigate | Skeleton usa props fixas `rows` com default 5 — sem computed que possa causar render infinito |
</threat_model>

<verification>
1. `npm run dev` compila sem erros (verificar lint)
2. Navegar em cada página em mobile viewport (375px) — layout não quebra
3. Toggle dark mode — contraste adequado em todas as páginas
</verification>

<success_criteria>
- EmptyState.vue reutilizável (icon, title, description, action props)
- LoadingSkeleton.vue com 4 variants (table, card, form, detail)
- 13+ páginas com loading + empty states implementados
- Formulários com validação visual PrimeVue consistente
- Dark mode sem cores fixas — uso de variáveis CSS PrimeVue
- Responsivo em mobile 375px+
</success_criteria>

<output>
Criar `.planning/phases/14-auditoria-ajustes/14-02-SUMMARY.md` após execução.
</output>