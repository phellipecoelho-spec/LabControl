---
wave: 4
depends_on:
  - 05-03
files_modified:
  - backend/app/Services/EquipmentPhotoService.php
  - backend/app/Http/Controllers/Api/V1/EquipmentPhotoController.php
  - backend/routes/api.php
  - frontend/src/modules/equipment/components/EquipmentPhotoUploader.vue
  - frontend/src/modules/equipment/components/EquipmentLogsSection.vue
  - frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
  - backend/tests/Feature/EquipmentPhotoTest.php
autonomous: true
requirements:
  - EQUIP-03
  - EQUIP-04
---

# 05-04: Fotos e Histórico de Alterações

<objective>
Implementar upload de fotos dos equipamentos (EQUIP-03) e exibição do histórico de alterações (EQUIP-04) com timeline visual na aba de logs.
</objective>

## Tasks

### T1: Service — EquipmentPhotoService

<read_first>
- backend/app/Services/AvatarService.php
- backend/app/Models/EquipmentPhoto.php
</read_first>

<action>
Criar `backend/app/Services/EquipmentPhotoService.php`:

Método upload(UploadedFile $file, string $equipmentId): EquipmentPhoto
- Validar mime: jpg, jpeg, png, webp
- Validar max size: 5MB (5 * 1024 * 1024)
- Gerar filename único: uuid + extensão original
- Storage path: `equipment/{$equipmentId}/photos/{$filename}`
- Store via Storage::disk('public')->put()
- Criar EquipmentPhoto com path, original_name, size, mime_type, sort_order (último + 1)

Método delete(string $photoId): void
- Buscar EquipmentPhoto
- Storage::disk('public')->delete($photo->path)
- EquipmentPhoto::destroy()

Método reorder(array $photoIds): void
- Para cada id no array, atualizar sort_order com índice do array

Registrar no Service Container: `app()->singleton(EquipmentPhotoService::class)`
</action>

<acceptance_criteria>
- EquipmentPhotoService com métodos upload, delete, reorder
- Validação: mime jpg/jpeg/png/webp, max 5MB
- Storage em equipment/{id}/photos/ no disco public
</acceptance_criteria>

### T2: Controller — EquipmentPhotoController

<read_first>
- backend/app/Http/Controllers/Api/V1/UserController.php
- backend/app/Services/EquipmentPhotoService.php
</read_first>

<action>
Criar `backend/app/Http/Controllers/Api/V1/EquipmentPhotoController.php`:

Middleware: auth:sanctum + permission:equipamentos.edit

Actions:
- index(Equipment $equipment): retornar fotos do equipamento ordenadas por sort_order
- store(Request $request, Equipment $equipment): upload de foto
  - Validar: photo required|image|mimes:jpg,jpeg,png,webp|max:5120
  - Chamar EquipmentPhotoService::upload
  - Retornar 201 com dados da foto
- destroy(EquipmentPhoto $photo): deletar foto
  - Chamar EquipmentPhotoService::delete
  - Retornar 204
- reorder(Request $request, Equipment $equipment): reordenar fotos
  - Validar: photo_ids required|array
  - Chamar EquipmentPhotoService::reorder
  - Retornar fotos reordenadas
</action>

<acceptance_criteria>
- EquipmentPhotoController com index, store, destroy, reorder
- Validação de arquivo: image, mimes, max:5120
- Rotas em equipment group: equipments/{equipment}/photos
</acceptance_criteria>

### T3: Routes — Photo endpoints

<read_first>
- backend/routes/api.php
</read_first>

<action>
No arquivo `backend/routes/api.php`, adicionar dentro do grupo v1/equipments:

```php
Route::prefix('equipments/{equipment}')->group(function () {
    Route::get('photos', [EquipmentPhotoController::class, 'index']);
    Route::post('photos', [EquipmentPhotoController::class, 'store']);
    Route::delete('photos/{photo}', [EquipmentPhotoController::class, 'destroy']);
    Route::post('photos/reorder', [EquipmentPhotoController::class, 'reorder']);
});
```
</action>

<acceptance_criteria>
- 4 rotas de foto criadas como sub-recurso de equipments/{equipment}
</acceptance_criteria>

### T4: Component — EquipmentPhotoUploader

<read_first>
- frontend/src/modules/profile/components/AvatarUploader.vue
- frontend/src/modules/equipment/types/equipment.ts
</read_first>

<action>
Criar `frontend/src/modules/equipment/components/EquipmentPhotoUploader.vue`:

Props: equipmentId (string), photos (EquipmentPhoto[]).
Emits: photos-updated (EquipmentPhoto[]).

Template:
- Dropzone área com "Arraste fotos aqui ou clique para selecionar" (FileUpload PrimeVue com mode="advanced", accept="image/jpeg,image/png,image/webp", maxFileSize=5242880)
- Preview das fotos existentes em grid (flex-wrap, gap): cada foto com imagem (src via api /storage/ path), botão X para deletar (com ConfirmDialog)
- Drag-and-drop para reordenar (ou botões up/down)

Ao fazer upload: POST /equipments/{equipmentId}/photos com FormData, após sucesso atualizar lista e emitir photos-updated.
Ao deletar: DELETE /equipments/{equipmentId}/photos/{photoId}.
Limite visual: máximo 10 fotos (mostrar mensagem "Limite de 10 fotos atingido").

Script: usar axios direto para upload com FormData + Content-Type multipart/form-data.
</action>

<acceptance_criteria>
- EquipmentPhotoUploader com dropzone, preview em grid, delete individual
- Upload via POST com FormData, multipart
- Limite visível de 10 fotos
- Feedback toast após upload/delete
</acceptance_criteria>

### T5: Component — EquipmentLogsSection

<read_first>
- frontend/src/modules/admin/pages/AuditLogsPage.vue (Timeline pattern)
- backend/app/Models/ActivityLog.php
</read_first>

<action>
Criar `frontend/src/modules/equipment/components/EquipmentLogsSection.vue`:

Props: equipmentId (string).

Template:
- Carregar activity logs via api.get('/activity-logs', { params: { subject_type: 'equipment', subject_id: equipmentId } })
- Exibir em Timeline PrimeVue (mesmo padrão de AuditLogsPage.vue)
- Cada entry: ícone (pi-pencil para update, pi-plus para create, pi-trash para delete), descrição formatada (action + changes snapshot), timestamp, usuário

Descrição formatada:
- created: "Equipamento cadastrado por {user}" com detalhes iniciais
- updated: "Campo X alterado de 'Y' para 'Z' por {user}" (se changes JSON disponível)
- deleted: "Equipamento removido por {user}"

Loading state: Skeleton enquanto carrega. Empty state: "Nenhuma alteração registrada."
</action>

<acceptance_criteria>
- EquipmentLogsSection carrega logs filtrados por subject_type e subject_id
- Timeline com ícones por tipo de ação
- Estado de loading e empty tratados
</acceptance_criteria>

### T6: Integrar aba Arquivos e Logs na EquipmentDetailPage

<read_first>
- frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
- frontend/src/modules/equipment/components/EquipmentPhotoUploader.vue
- frontend/src/modules/equipment/components/EquipmentLogsSection.vue
</read_first>

<action>
No `EquipmentDetailPage.vue`, substituir os placeholders:
- Aba 4 (Arquivos): `<EquipmentPhotoUploader :equipmentId="equipment.id" :photos="equipment.photos" @photos-updated="onPhotosUpdated" />`
- Aba 5 (Logs): `<EquipmentLogsSection :equipmentId="equipment.id" />`

Adicionar função onPhotosUpdated que atualiza equipment.photos localmente.
</action>

<acceptance_criteria>
- EquipmentDetailPage integra EquipmentPhotoUploader na aba 4
- EquipmentDetailPage integra EquipmentLogsSection na aba 5
</acceptance_criteria>

### T7: Testes — EquipmentPhotoTest

<read_first>
- backend/tests/Feature/EquipmentApiTest.php
- backend/app/Services/EquipmentPhotoService.php
</read_first>

<action>
Criar `backend/tests/Feature/EquipmentPhotoTest.php`:

Testes:
1. test_can_upload_equipment_photo: POST /equipments/{id}/photos com imagem fake (UploadedFile::fake()->image('photo.jpg')) retorna 201
2. test_upload_rejects_invalid_file: POST com arquivo texto retorna 422
3. test_upload_rejects_large_file: POST com imagem >5MB retorna 422
4. test_can_list_equipment_photos: GET /equipments/{id}/photos retorna 200 com array de fotos
5. test_can_delete_equipment_photo: DELETE /equipments/{id}/photos/{photoId} retorna 204
6. test_can_reorder_photos: POST /equipments/{id}/photos/reorder com array de ids retorna 200

Usar RefreshDatabase + Storage::fake('public').
</action>

<acceptance_criteria>
- 6 testes passando com php artisan test --filter EquipmentPhotoTest
- Testes usam Storage::fake('public')
</acceptance_criteria>

## Verification

1. `cd backend && php artisan migrate:fresh --seed` sem erros
2. `cd backend && php artisan test --filter EquipmentPhotoTest` passa 6/6
3. `cd frontend && npx vite build` sem erros
4. Upload de foto no navegador: selecionar jpg <5MB, ver preview, deletar, reordenar
5. Aba Logs mostra Timeline com ações do equipamento

## must_haves

truths:
  - EquipmentPhotoService uploada fotos em storage/app/public/equipment/{id}/photos/
  - Foto validada por mime (jpg/jpeg/png/webp) e size (max 5MB)
  - EquipmentPhotoUploader com dropzone, preview grid, delete e upload
  - EquipmentLogsSection exibe Timeline com logs de alterações do equipamento
  - LogsActivity trait captura todas as mutações no Equipment Model
  - Abas Arquivos e Logs integradas na EquipmentDetailPage

## Artifacts This Phase Produces

- `backend/app/Services/EquipmentPhotoService.php` — Serviço de upload/reorder/delete de fotos
- `backend/app/Http/Controllers/Api/V1/EquipmentPhotoController.php` — API de fotos
- `frontend/src/modules/equipment/components/EquipmentPhotoUploader.vue` — Componente de upload
- `frontend/src/modules/equipment/components/EquipmentLogsSection.vue` — Componente de histórico
- `backend/tests/Feature/EquipmentPhotoTest.php` — 6 testes de upload