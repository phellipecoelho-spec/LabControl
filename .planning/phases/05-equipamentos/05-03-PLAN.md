---
wave: 3
depends_on:
  - 05-02b
files_modified:
  - frontend/src/modules/equipment/services/EquipmentService.ts
  - frontend/src/modules/equipment/store/EquipmentStore.ts
  - frontend/src/modules/equipment/types/equipment.ts
  - frontend/src/modules/equipment/pages/EquipmentListPage.vue
  - frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
  - frontend/src/modules/equipment/pages/EquipmentFormPage.vue
  - frontend/src/modules/equipment/components/EquipmentInfoTab.vue
  - frontend/src/modules/equipment/components/EquipmentLocationTab.vue
  - frontend/src/modules/equipment/components/EquipmentTechnicalTab.vue
  - frontend/src/types/navigation.ts
  - frontend/src/router/routes.ts
  - frontend/src/stores/auth.ts
autonomous: true
requirements:
  - EQUIP-01
  - EQUIP-02
---

# 05-03: Frontend CRUD — Listagem, Cadastro e Detalhes do Equipamento

<objective>
Criar interface completa de gerenciamento de equipamentos: listagem com DataTable, página de detalhes com abas, formulário de criação/edição e integração com navegação do sistema.
</objective>

## Tasks

### T1: Types — equipment.ts

<read_first>
- frontend/src/stores/auth.ts (User interface pattern)
- frontend/src/types/navigation.ts (type pattern)
</read_first>

<action>
Criar `frontend/src/modules/equipment/types/equipment.ts`:

```typescript
export interface Category {
  id: string
  name: string
  slug: string
  equipments_count?: number
}

export interface Manufacturer {
  id: string
  name: string
  country?: string
  website?: string
}

export interface Supplier {
  id: string
  name: string
  cnpj?: string
  contact_name?: string
  contact_email?: string
  contact_phone?: string
}

export interface EquipmentPhoto {
  id: string
  path: string
  sort_order: number
}

export interface Equipment {
  id: string
  name: string
  patrimony_id?: string
  serial_number?: string
  category?: Category
  manufacturer?: Manufacturer
  supplier?: Supplier
  location?: string
  acquisition_date?: string
  warranty_end?: string
  status: 'active' | 'inactive' | 'maintenance' | 'retired'
  description?: string
  technical_specs?: string
  notes?: string
  photos?: EquipmentPhoto[]
  created_at: string
  updated_at: string
}

export interface EquipmentFormData {
  name: string
  patrimony_id?: string
  serial_number?: string
  category_id?: string
  manufacturer_id?: string
  supplier_id?: string
  location?: string
  acquisition_date?: string
  warranty_end?: string
  status: string
  description?: string
  technical_specs?: string
  notes?: string
}
```
</action>

<acceptance_criteria>
- equipment.ts exporta Category, Manufacturer, Supplier, EquipmentPhoto, Equipment, EquipmentFormData
- Interfaces compatíveis com EquipmentResource do backend
</acceptance_criteria>

### T2: Service — EquipmentService

<read_first>
- frontend/src/services/api.ts
- frontend/src/stores/users.ts (fetchAll pattern)
</read_first>

<action>
Criar `frontend/src/modules/equipment/services/EquipmentService.ts`:

```typescript
import { api } from '@/services/api'
import type { Equipment } from '../types/equipment'

export const equipmentService = {
  async list(params?: Record<string, any>) {
    const response = await api.get('/equipments', { params })
    return response.data
  },

  async getById(id: string) {
    const response = await api.get(`/equipments/${id}`)
    return response.data
  },

  async create(data: Record<string, any>) {
    const response = await api.post('/equipments', data)
    return response.data
  },

  async update(id: string, data: Record<string, any>) {
    const response = await api.put(`/equipments/${id}`, data)
    return response.data
  },

  async delete(id: string) {
    await api.delete(`/equipments/${id}`)
  },

  async listCategories(params?: Record<string, any>) {
    const response = await api.get('/categories', { params })
    return response.data
  },

  async listManufacturers(params?: Record<string, any>) {
    const response = await api.get('/manufacturers', { params })
    return response.data
  },

  async listSuppliers(params?: Record<string, any>) {
    const response = await api.get('/suppliers', { params })
    return response.data
  },
}
```
</action>

<acceptance_criteria>
- EquipmentService com métodos list, getById, create, update, delete
- Métodos auxiliares: listCategories, listManufacturers, listSuppliers
</acceptance_criteria>

### T3: Store — EquipmentStore

<read_first>
- frontend/src/stores/users.ts
- frontend/src/modules/equipment/types/equipment.ts
</read_first>

<action>
Criar `frontend/src/modules/equipment/store/EquipmentStore.ts`:

Pinia store com:
- State: equipments (Equipment[]), loading (boolean), pagination (current_page, last_page, total, per_page), categories (Category[]), manufacturers (Manufacturer[]), suppliers (Supplier[])
- Actions: fetchAll(params), fetchById(id), create(data), update(id, data), destroy(id), fetchCategories(), fetchManufacturers(), fetchSuppliers()
- Padrão idêntico ao users store: api.get('/equipments', { params }) com tratamento de resposta paginada

Store registrada via defineStore('equipment', ...).
</action>

<acceptance_criteria>
- EquipmentStore com state, actions e pagination idêntico ao pattern do users store
- Actions de categoria, fabricante e fornecedor para popular dropdowns
</acceptance_criteria>

### T4: Page — EquipmentListPage

<read_first>
- frontend/src/modules/admin/pages/UsersPage.vue (DataTable CRUD pattern)
- frontend/src/modules/equipment/store/EquipmentStore.ts
</read_first>

<action>
Criar `frontend/src/modules/equipment/pages/EquipmentListPage.vue`:

Template:
- Toast e ConfirmDialog (topo)
- Header: título "Equipamentos" + Breadcrumb + botão "Novo Equipamento" (com permissão equipamentos.create)
- Toolbar com: InputText (search), Select (categoria), Select (status)
- DataTable com: nome, patrimônio, categoria (tag ou text), fabricante, série, localização, status (Tag com severity: active=success, inactive=danger, maintenance=warning, retired=info), ações (ver, editar, excluir)
- Paginator lazy
- EquipmentFormDialog (componente T5)

Script:
- Store equipmentStore, onMounted fetchAll + fetchCategories + fetchManufacturers + fetchSuppliers
- Filters reativos: search, category_id, status
- Debounce na search (400ms)
- onPage, openCreateDialog, openEditDialog, confirmDelete, handleSave

Regras de permissão:
- Botão "Novo": v-if="authStore.hasPermission('equipamentos.create')"
- Botão editar: v-if="authStore.hasPermission('equipamentos.edit')"
- Botão excluir: v-if="authStore.hasPermission('equipamentos.delete')"
</action>

<acceptance_criteria>
- EquipmentListPage com DataTable, filtros, paginação lazy
- EquipmentFormDialog para criação/edição
- Controle de permissões em ações
</acceptance_criteria>

### T5: Page — EquipmentFormPage

<read_first>
- frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
- frontend/src/modules/equipment/store/EquipmentStore.ts
- frontend/src/modules/equipment/components/EquipmentInfoTab.vue
- frontend/src/modules/equipment/components/EquipmentLocationTab.vue
- frontend/src/modules/equipment/components/EquipmentTechnicalTab.vue
</read_first>

<action>
Criar `frontend/src/modules/equipment/pages/EquipmentFormPage.vue`:

PÁGINA DEDICADA (não modal) para criação/edição de equipamentos, seguindo D-07.

Template:
- Page header: "Novo Equipamento" (criação) ou "Editar: {equipment.name}" (edição)
- Tabs com abas de formulário (reutiliza estrutura de DetailPage):
  - Aba 1 - Principal: name (InputText), patrimony_id (InputText), serial_number (InputText), category_id (Select com options da store), manufacturer_id (Select), supplier_id (Select), status (SelectButton: Ativo/Inativo/Manutenção/Baixado)
  - Aba 2 - Localização: location (InputText), acquisition_date (DatePicker), warranty_end (DatePicker)
  - Aba 3 - Técnica: description (Textarea), technical_specs (Textarea), notes (Textarea)
- Footer com botões "Cancelar" (volta para listagem) e "Salvar"

Validação (campos obrigatórios conforme D-09):
- name: required
- category_id: required
- manufacturer_id: required
- serial_number: required
- location: required
- Campos obrigatórios com asterisco vermelho
- Botão Salvar disabled enquanto campos obrigatórios inválidos

Route: criada via código vs parágrafo de rota (produzível natural):
- Criação: route /equipments/new, recebe props: { isEditing: false }
- Edição: route /equipments/:id/edit, tem rota nova; equipamento carregado via fetchById(id)
- onMounted: se route.params.id existir, fetch equipment e preencher formulário

Após salvar com sucesso: Toast de sucesso + router.push('/equipments').

Integrar na listagem (T4): botão "Novo" → router.push({ name: 'equipment-create' }), botão "Editar" → router.push({ name: 'equipment-edit', params: { id } }).
</action>

<acceptance_criteria>
- EquipmentFormPage é página dedicada (não modal), seguindo D-07
- 5 campos obrigatórios (name, category_id, manufacturer_id, serial_number, location) validados conforme D-09
- Formulário com 3 abas
- Navegação para listagem via router.push após salvar
</acceptance_criteria>

### T6: Page — EquipmentDetailPage

<read_first>
- frontend/src/modules/profile/pages/ProfilePage.vue (Tabs pattern)
- frontend/src/modules/equipment/types/equipment.ts
</read_first>

<action>
Criar `frontend/src/modules/equipment/pages/EquipmentDetailPage.vue`:

Template:
- Page header com nome do equipamento + status Tag + botão "Voltar"
- Tabs PrimeVue com 5 abas:

**Aba 1 - Principal**: EquipmentInfoTab (dados principais: nome, patrimônio, série, categoria badge, fabricante, fornecedor, status)
**Aba 2 - Localização**: EquipmentLocationTab (localização, acquisition_date, warranty_end)
**Aba 3 - Técnica**: EquipmentTechnicalTab (description, technical_specs)
**Aba 4 - Arquivos**: placeholder para EquipmentPhotoUploader (05-04)
**Aba 5 - Logs**: placeholder para EquipmentLogsSection (05-04)

Route param: id. fetchById(id) no onMounted. Loading state com Skeleton.
</action>

<acceptance_criteria>
- EquipmentDetailPage com 5 abas (2 preenchidas, 2 placeholders para 05-04)
- Carrega equipamento por ID no onMounted
- Status Tag com severity correta
</acceptance_criteria>

### T7: Components — EquipmentInfoTab, EquipmentLocationTab, EquipmentTechnicalTab

<read_first>
- frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
</read_first>

<action>
Criar 3 componentes em `frontend/src/modules/equipment/components/`:

**EquipmentInfoTab.vue**: props: equipment (Equipment). Exibe em grid 2 colunas: Nome, Patrimônio, Nº Série, Categoria (Tag), Fabricante, Fornecedor, Status (Tag). Cada campo com label + valor.

**EquipmentLocationTab.vue**: props: equipment (Equipment). Exibe: Localização, Data Aquisição (formatada pt-BR), Fim Garantia (formatada pt-BR). Se warranty_end < today, exibir badge "Vencida" vermelha.

**EquipmentTechnicalTab.vue**: props: equipment (Equipment). Exibe description, technical_specs em cards separados. Se vazio, mostrar "Nenhuma especificação técnica cadastrada."
</action>

<acceptance_criteria>
- 3 componentes com props tipadas
- EquipmentInfoTab com grid 2 colunas
- EquipmentLocationTab com data formatada pt-BR e badge de garantia vencida
- EquipmentTechnicalTab com estado vazio
</acceptance_criteria>

### T7: Navigation — equipment routes

<read_first>
- frontend/src/router/routes.ts
- frontend/src/types/navigation.ts
</read_first>

<action>
Em `frontend/src/router/routes.ts`, adicionar 4 rotas:

```typescript
{
  path: '/equipments',
  name: 'equipments',
  meta: { requiresAuth: true, module: 'gestao' },
  component: () => import('@/modules/equipment/pages/EquipmentListPage.vue'),
},
{
  path: '/equipments/new',
  name: 'equipment-create',
  meta: { requiresAuth: true, module: 'gestao' },
  component: () => import('@/modules/equipment/pages/EquipmentFormPage.vue'),
},
{
  path: '/equipments/:id/edit',
  name: 'equipment-edit',
  meta: { requiresAuth: true, module: 'gestao' },
  component: () => import('@/modules/equipment/pages/EquipmentFormPage.vue'),
},
{
  path: '/equipments/:id',
  name: 'equipment-detail',
  meta: { requiresAuth: true, module: 'gestao' },
  component: () => import('@/modules/equipment/pages/EquipmentDetailPage.vue'),
},
```

Em `frontend/src/types/navigation.ts`, na categoria 'Gestão', adicionar módulo:
```typescript
{
  label: 'Equipamentos',
  icon: 'pi pi-microchip',
  route: 'equipments',
  permission: 'equipamentos.view',
}
```
</action>

<acceptance_criteria>
- 4 rotas adicionadas: /equipments (lista), /equipments/new (criar), /equipments/:id/edit (editar), /equipments/:id (detalhes)
- NavCategory 'Gestão' em navigation.ts com módulo Equipamentos
- Lazy loading com () => import()
</acceptance_criteria>

## Verification

1. `cd frontend && npx vue-tsc --noEmit` sem erros de tipo
2. `cd frontend && npx vite build` sem erros
3. Navegar para /equipments mostra lista vazia com DataTable
4. Sidebar mostra "Equipamentos" na categoria Gestão com ícone microchip

## must_haves

truths:
  - EquipmentListPage com DataTable, filtros e paginação lazy
  - EquipmentDetailPage com 5 abas (2 implementadas, 2 placeholders)
  - EquipmentFormDialog com criação/edição e validação
  - Rotas /equipments e /equipments/:id funcionando
  - Sidebar com entrada "Equipamentos" na categoria Gestão
  - Controle de permissões em todas as ações da listagem

## Artifacts This Phase Produces

- `frontend/src/modules/equipment/types/equipment.ts` — Interfaces TypeScript
- `frontend/src/modules/equipment/services/EquipmentService.ts` — API service
- `frontend/src/modules/equipment/store/EquipmentStore.ts` — Pinia store
- `frontend/src/modules/equipment/pages/EquipmentListPage.vue` — Lista com DataTable
- `frontend/src/modules/equipment/pages/EquipmentDetailPage.vue` — Detalhes com abas
- `frontend/src/modules/equipment/components/EquipmentFormDialog.vue` — Formulário
- `frontend/src/modules/equipment/components/EquipmentInfoTab.vue` — Aba Principal
- `frontend/src/modules/equipment/components/EquipmentLocationTab.vue` — Aba Localização
- `frontend/src/modules/equipment/components/EquipmentTechnicalTab.vue` — Aba Técnica