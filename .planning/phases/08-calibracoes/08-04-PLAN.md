---
phase: 08-calibracoes
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - frontend/src/modules/calibrations/pages/CalibrationListPage.vue
  - frontend/src/modules/calibrations/pages/CalibrationDetailPage.vue
  - frontend/src/modules/calibrations/components/CalibrationInfoTab.vue
  - frontend/src/modules/calibrations/components/CalibrationCertificateTab.vue
  - frontend/src/modules/calibrations/components/CalibrationTimelineTab.vue
  - frontend/src/modules/calibrations/components/CalibrationCreateDialog.vue
  - frontend/src/modules/calibrations/components/CalibrationConcludeDialog.vue
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04

must_haves:
  truths:
    - User can list calibrations with filters: equipment, status, date range, laboratory (D-15)
    - User can create calibrations via modal dialog with all required fields (D-17, D-19)
    - User can view calibration detail with 3 tabs: Info, Certificates, Timeline (D-18)
    - User can conclude a calibration via dialog (D-20)
    - User can cancel a calibration with confirmation
    - User can view, upload, and download certificates per calibration (D-10)
    - Overdue and due-soon calibrations have visual indicators
    - Actions gated by permissions (D-21)
    - Create button gated by calibracoes.create
    - Concluir button gated by calibracoes.concluir (only when status=scheduled)
    - Cancelar button gated by calibracoes.cancel (only when status=scheduled)
  artifacts:
    - frontend/src/modules/calibrations/pages/CalibrationListPage.vue
    - frontend/src/modules/calibrations/pages/CalibrationDetailPage.vue
    - frontend/src/modules/calibrations/components/CalibrationInfoTab.vue
    - frontend/src/modules/calibrations/components/CalibrationCertificateTab.vue
    - frontend/src/modules/calibrations/components/CalibrationTimelineTab.vue
    - frontend/src/modules/calibrations/components/CalibrationCreateDialog.vue
    - frontend/src/modules/calibrations/components/CalibrationConcludeDialog.vue
  key_links:
    - CalibrationListPage uses CalibrationStore.fetchAll + CalibrationStore.destroy + CalibrationStore.cancel
    - CalibrationDetailPage uses CalibrationStore.fetchById + CalibrationStore.complete
    - CalibrationCreateDialog emits 'saved' → list page refetch
    - CalibrationConcludeDialog calls CalibrationService.complete or store method
    - All components check authStore.hasPermission('calibracoes.*') for action visibility (D-21)
---

<objective>
Criar os componentes de UI do módulo de Calibrações — páginas, dialogs e abas de detalhe.

Purpose: Entregar a interface completa para gerenciamento de calibrações: listagem com filtros, criação/edição/conclusão/cancelamento via dialogs, detail page com 3 abas, e upload de certificados.
Output: 7 componentes Vue — 2 pages, 5 components (3 tabs + 2 dialogs).
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md

Required reading:
- @.planning/phases/07-emprestimos/07-04-PLAN.md — UI component pattern
- @.planning/codebase/CONVENTIONS.md — Vue component conventions (SFC, composition API, PrimeVue usage)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-calibracoes/08-CONTEXT.md
@.planning/phases/08-calibracoes/08-RESEARCH.md
@.planning/phases/08-calibracoes/08-PATTERNS.md
@.planning/phases/08-calibracoes/08-03-PLAN.md

Prior phase analogs:
@frontend/src/modules/loans/pages/LoanListPage.vue
@frontend/src/modules/loans/pages/LoanDetailPage.vue
@frontend/src/modules/loans/components/LoanCreateDialog.vue
@frontend/src/modules/loans/components/LoanReturnDialog.vue
@frontend/src/modules/loans/components/LoanInfoTab.vue
@frontend/src/modules/loans/components/LoanItemsTab.vue
@frontend/src/modules/loans/components/LoanTimelineTab.vue
@frontend/src/modules/equipment/components/EquipmentPhotoUploader.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CalibrationCreateDialog and CalibrationConcludeDialog</name>
  <files>
    frontend/src/modules/calibrations/components/CalibrationCreateDialog.vue
    frontend/src/modules/calibrations/components/CalibrationConcludeDialog.vue
  </files>
  <read_first>
    @frontend/src/modules/loans/components/LoanCreateDialog.vue
    @frontend/src/modules/loans/components/LoanReturnDialog.vue
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 1130-1145, dialog patterns)
  </read_first>
  <action>
    Create 2 dialog components. Ensure directory exists: `frontend/src/modules/calibrations/components/`

    **1. `CalibrationCreateDialog.vue`** (analog: LoanCreateDialog.vue) per D-17, D-19:
    - Props: `visible: boolean`
    - Emits: `update:visible`, `saved`
    - Imports: `useCalibrationStore` from store, `authStore` from @/stores/auth, `onMounted`, `ref` from vue
    - Form fields (D-19):
      - **Equipamento** (Select, required): fetched from API via `useCalibrationStore().fetchEquipment()`, options = equipment.map(e => ({ label: e.name, value: e.id })), with filter option
      - **Parte do Equipamento** (InputText, optional) (D-05)
      - **Data Agendada** (DatePicker, required) — validate after_or_equal:today per D-19
      - **Intervalo** — two inline fields:
        - Interval Value (InputNumber, required, min:1) (D-02)
        - Interval Unit (Select, required) — options from INTERVAL_UNIT_OPTIONS ('months'/'days'/'hours') (D-02)
      - **Responsável** (InputText, optional)
      - **Laboratório** (InputText, optional)
      - **Observações** (Textarea, optional)
    - Form validation: `submitted` flag, red highlights on invalid fields, error messages
    - `resetForm()`: clear all fields
    - `handleSave()`: set submitted=true, validate, call store.create(formData), emit 'saved', reset, close
    - `saving: ref(false)` — disable button during save
    - AuthStore check: button only visible when `authStore.hasPermission('calibracoes.create')` (D-21)

    **2. `CalibrationConcludeDialog.vue`** (role-match: LoanReturnDialog.vue) per D-20:
    - Props: `visible: boolean`, `calibration: Calibration | null`
    - Emits: `update:visible`, `saved`
    - Form fields (D-20):
      - **Data de Conclusão** (DatePicker, optional) — default: today
      - **Nº Certificado** (InputText, optional)
      - **Responsável** (InputText, optional) — pre-filled com valor da calibração
      - **Laboratório** (InputText, optional) — pre-filled
      - **Observações** (Textarea, optional)
    - `handleSave()`: call store.complete(calibration.id, formData) → POST /calibrations/{id}/complete
    - `resetForm()`: populate from calibration prop if available
    - `saving` guard
    - `watch(calibration)` to populate fields when dialog opens with a calibration
  </action>
  <acceptance_criteria>
    - CalibrationCreateDialog has all fields per D-19 with proper validation
    - CalibrationConcludeDialog has all fields per D-20 with default today for completed_at
    - Both dialogs emit 'saved' on successful API call
    - Both use useCalibrationStore for API calls
    - CreateDialog validates field before submit (required: equipment_id, scheduled_date, interval_value, interval_unit)
    - CreateDialog fetched equipment list on mount
  </acceptance_criteria>
  <verify>
    <automated>cd frontend; npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "calibration"</automated>
  </verify>
  <done>CalibrationCreateDialog and CalibrationConcludeDialog created and type-checked</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CalibrationInfoTab, CalibrationCertificateTab, and CalibrationTimelineTab</name>
  <files>
    frontend/src/modules/calibrations/components/CalibrationInfoTab.vue
    frontend/src/modules/calibrations/components/CalibrationCertificateTab.vue
    frontend/src/modules/calibrations/components/CalibrationTimelineTab.vue
  </files>
  <read_first>
    @frontend/src/modules/loans/components/LoanInfoTab.vue
    @frontend/src/modules/loans/components/LoanItemsTab.vue
    @frontend/src/modules/loans/components/LoanTimelineTab.vue
    @frontend/src/modules/equipment/components/EquipmentPhotoUploader.vue
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 1085-1128, tab patterns)
  </read_first>
  <action>
    Create 3 tab components:

    **1. `CalibrationInfoTab.vue`** (analog: LoanInfoTab.vue)
    - Props: `calibration: Calibration`
    - Display fields using PrimeVue Card + Divider groups:
      - **Equipamento**: name + patrimony_id
      - **Parte**: part_name or "—"
      - **Período**: scheduled_date, completed_at (ou "Pendente"), next_due_at (ou "—"), with due/due_soon badges (D-03)
      - **Periodicidade**: formatado como "{interval_value} {interval_unit}" (ex: "6 meses") — exibir em português (meses/dias/horas)
      - **Status**: Tag com severity: info=scheduled, success=completed, secondary=cancelled (D-03)
      - **Responsável**: or "—"
      - **Laboratório**: or "—"
      - **Nº Certificado**: certificate_number or "—"
      - **Observações**: notes or "—" (truncado com tooltip)
    - Layout: 2-column grid (col-6) for most fields, full width for notes

    **2. `CalibrationCertificateTab.vue`** (partial analog: LoanItemsTab.vue + EquipmentPhotoUploader pattern)
    - Props: `calibrationId: string`, `certificates: CalibrationCertificate[]`
    - Emits: `refresh`
    - Upload file button: use PrimeVue FileUpload or a simple Button + hidden file input
      - Validação: PDF, imagens (jpg/png/webp), max 10MB
      - Upload via `calibrationService.uploadCertificate(this.calibrationId, file)`
      - Progress indicator durante upload
    - Lista de certificados em DataTable:
      - Colunas: Nome do Arquivo, Tipo (mime_type), Tamanho (formatado), Nº Certificado, Emissor, Data Emissão, Ações
      - Ações: Download (pi pi-download) → URL construída de `import.meta.env.VITE_API_URL + '/storage/' + cert.filepath`, Delete (pi pi-trash) com confirmação, gated por `calibracoes.edit`
    - Refetch após upload/delete: emit 'refresh'
    - Empty state: "Nenhum certificado anexado"
    - Limitação visual: mostrar "Adicionar Certificado" apenas se `authStore.hasPermission('calibracoes.edit')` (D-21)

    **3. `CalibrationTimelineTab.vue`** (analog: LoanTimelineTab.vue)
    - Props: `calibration: Calibration`
    - Computed `timelineEvents`:
      - **Criação**: icon=pi pi-plus, color=blue, date=calibration.created_at, title="Calibração agendada", description com equipment name + part_name
      - **Conclusão** (if completed_at): icon=pi pi-check, color=green, date=completed_at, title="Calibração concluída", description com next_due_at formatado
      - **Cancelamento** (if cancelled): icon=pi pi-times, color=red, date=updated_at (same as cancelled transition), title="Calibração cancelada"
    - Template: PrimeVue Timeline component (align="left") com marker + Card content pattern
    - If no events: empty state message
  </action>
  <acceptance_criteria>
    - CalibrationInfoTab displays all fields in 2-column grid with formatted dates and status Tags
    - CalibrationCertificateTab shows certificate list with download/delete actions and upload button
    - Certificate file upload validates PDF, JPG, PNG, WebP, max 10MB
    - CalibrationTimelineTab shows 2-3 events depending on calibration status
    - All three tabs use PrimeVue components (Card, DataTable, Timeline etc.)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend; npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "calibration"</automated>
  </verify>
  <done>3 tab components (Infotab, CertificateTab, TimelineTab) created and type-checked</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create CalibrationListPage and CalibrationDetailPage</name>
  <files>
    frontend/src/modules/calibrations/pages/CalibrationListPage.vue
    frontend/src/modules/calibrations/pages/CalibrationDetailPage.vue
  </files>
  <read_first>
    @frontend/src/modules/loans/pages/LoanListPage.vue
    @frontend/src/modules/loans/pages/LoanDetailPage.vue
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 1048-1083, page patterns)
  </read_first>
  <action>
    Create 2 page components. Ensure directory exists: `frontend/src/modules/calibrations/pages/`

    **1. `CalibrationListPage.vue`** (analog: LoanListPage.vue) per D-17:
    - Template structure:
      - Toast + ConfirmDialog at top
      - Title "Calibrações" with description + "Nova Calibração" button (gated por `calibracoes.create` via authStore.hasPermission)
      - Toolbar with filters (D-15):
        - Equipment (Select): fetches equipment list on mount, options label=name/value=id, style min-width 220px, filterable
        - Status (Select): options from CALIBRATION_STATUS_OPTIONS, clearable
        - Data (DatePicker range): two DatePickers (from/to)
        - Laboratory (InputText): placeholder "Buscar por laboratório..."
      - DataTable with lazy pagination (D-16 columns):
        - **Equipamento**: equipment.name
        - **Parte**: part_name ou "—"
        - **Data Agendada**: formatted via formatDate(scheduled_date)
        - **Data Conclusão**: completed_at formatted ou "—"
        - **Próxima Data**: next_due_at formatted ou "—", com Tag "Vence em X dias" se is_due_soon (severity=warn)
        - **Laboratório**: laboratory ou "—"
        - **Status**: Tag com severity: scheduled=info, completed=success, cancelled=secondary
        - **Ações**: View (eye, always), Edit (pencil, only scheduled + calibracoes.edit), Delete (trash, only scheduled/cancelled + calibracoes.edit)
      - DataTable features: stripedRows, lazy, @page event, server-side pagination (15 per page), sortField
      - `rowClass` binding: row-due class if is_due (calibration overdue) — background highlight
    - Script setup:
      - Use `useCalibrationStore`, `authStore`, `onMounted`
      - `filters` reactive object with equipment_id, status, from, to, laboratory
      - `showCreateDialog`, `showConcludeDialog`, `selectedCalibration` refs
      - `onMounted`: fetch calibrations + equipment list
      - `handleFilterChange()` → fetchAll with current page 1
      - `onSearch` debounce (300ms) for laboratory filter
      - `handlePage(event)` → fetchAll(event.page + 1)
      - `handleDelete(id)` → confirm dialog → store.destroy(id) → refetch
      - `handleCancel(id)` → confirm dialog → store.cancel(id) → refetch
      - `handleComplete(calibration)` → open CalibrationConcludeDialog
      - Utility functions: getStatusLabel, getStatusSeverity, formatDate

    **2. `CalibrationDetailPage.vue`** (analog: LoanDetailPage.vue) per D-18:
    - Template structure:
      - Back button (router.push to /calibrations) + title "Detalhes da Calibração" with status Tag
      - Due badge if is_due_soon or is_due
      - Action buttons (permission-gated D-21):
        - "Concluir" (pi pi-check, calibracoes.concluir, visible only when status=scheduled) → open ConcludeDialog
        - "Cancelar" (pi pi-times, calibracoes.cancel, visible only when status=scheduled) → confirm → store.cancel(id)
      - Tabs component with 3 TabPanels:
        - Tab 0 "Dados da Calibração": CalibrationInfoTab (prop: calibration)
        - Tab 1 "Certificados": CalibrationCertificateTab (props: calibrationId, certificates; listen to @refresh → refetch)
        - Tab 2 "Timeline": CalibrationTimelineTab (prop: calibration)
      - Skeleton loading state while store.loading is true
    - Script setup:
      - `useRoute`, `useRouter`, `useCalibrationStore`, `authStore`
      - `onMounted`: store.fetchById(route.params.id)
      - `activeTab: ref(0)`
      - `confirmComplete(calibration)`: open ConcludeDialog
      - `confirmCancel(calibration)`: confirm dialog → store.cancel(id) → store.fetchById(id)
      - `handleRefresh()`: store.fetchById(route.params.id) — called after certificate upload/delete
      - Computed `calibration` from store.currentCalibration
      - Toast notification on success/error
    - Integration with CalibrationConcludeDialog:
      - `showConcludeDialog: ref(false)`
      - After successful conclusion → refetch detail → show success toast
  </action>
  <acceptance_criteria>
    - CalibrationListPage has 4 filters (equipment, status, date range, laboratory) + DataTable with 8 columns (D-15, D-16)
    - Create dialog opens from "Nova Calibração" button gated by calibracoes.create (D-21)
    - DataTable has server-side pagination, striped rows, row-level due highlighting
    - Action buttons per row: view all, edit/delete conditional on status + permission
    - CalibrationDetailPage has back button, status tag, action buttons (Concluir/Cancelar)
    - DetailPage has 3 tabs: Info, Certificates, Timeline (D-18)
    - Conclude dialog pre-fills completed_at with today (D-20)
    - All permission checks use authStore.hasPermission('calibracoes.*') (D-21)
    - Overdue calibrations have visual indicators (row highlight, due tags)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend; npx vue-tsc --noEmit 2>&1 | Select-String -Pattern "error" -NotMatch; if ($?) { npx vite build --logLevel error 2>&1 | Select-String -Pattern "error" -NotMatch }</automated>
  </verify>
  <done>CalibrationListPage and CalibrationDetailPage created, type-checked, and build verified</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Role |
|----------|------|------|
| `frontend/src/modules/calibrations/pages/CalibrationListPage.vue` | Page | DataTable list with 4 filters, pagination, due indicators, action buttons |
| `frontend/src/modules/calibrations/pages/CalibrationDetailPage.vue` | Page | 3-tab detail view with action buttons, ConcludeDialog integration |
| `frontend/src/modules/calibrations/components/CalibrationInfoTab.vue` | Component | Fields display with 2-column grid, status tag, periodicidade formatting |
| `frontend/src/modules/calibrations/components/CalibrationCertificateTab.vue` | Component | Certificate DataTable list + FileUpload + download/delete actions |
| `frontend/src/modules/calibrations/components/CalibrationTimelineTab.vue` | Component | PrimeVue Timeline with 2-3 events (created/completed/cancelled) |
| `frontend/src/modules/calibrations/components/CalibrationCreateDialog.vue` | Component | Modal form with 8 fields, validation, equipment select |
| `frontend/src/modules/calibrations/components/CalibrationConcludeDialog.vue` | Component | Modal form for completing a calibration (completed_at, cert number, etc.) |

<verification>
1. `npx vue-tsc --noEmit` — type-check passes
2. `npx vite build` — build completes without errors
3. Navigation: /calibrations renders list page, /calibrations/:id renders detail page
4. All permission gates functional: create button, complete button, cancel button, edit/delete buttons
</verification>

<success_criteria>
- 7 Vue components created and type-checked
- List page with 4 filters, lazy pagination, due indicators, and action buttons
- Detail page with 3 tabs, status-aware action buttons, ConcludeDialog
- Create dialog with all fields per D-19, validation, and equipment selector
- Certificate tab with upload/download/delete per D-10
- Timeline tab showing calibration lifecycle events
- All actions permission-gated per D-21
- Vite build passes
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→API | All data operations go through CalibrationService → axios (interceptor attaches Sanctum cookie) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Elevation of Privilege | Action buttons | medium | mitigate | v-if="authStore.hasPermission('calibracoes.X')" on all mutation actions; server-side also enforces middleware |
| T-08-13 | Repudiation | Deletion/cancel actions | low | mitigate | ConfirmDialog on all destructive actions before calling API |
| T-08-14 | Information Disclosure | Certificate download | low | accept | File served through authenticated API route; direct storage URL not exposed in frontend |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this phase |
</threat_model>

<output>
Create `.planning/phases/08-calibracoes/08-04-SUMMARY.md` when done
</output>
