---
phase: 08-calibracoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database/migrations/2026_07_25_000001_create_calibrations_tables.php
  - backend/app/Enums/CalibrationStatus.php
  - backend/app/Models/Calibration.php
  - backend/app/Models/CalibrationCertificate.php
  - backend/app/Exceptions/CalibrationException.php
  - backend/app/Services/CalibrationService.php
  - backend/app/Services/CalibrationCertificateService.php
  - backend/database/factories/CalibrationFactory.php
  - backend/database/seeders/CalibrationSeeder.php
  - backend/database/seeders/RolePermissionSeeder.php
  - backend/database/seeders/DatabaseSeeder.php
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-04

must_haves:
  truths:
    - Calibrations belong to one equipment (FK equipment_id)
    - Calibration statuses are: scheduled → completed/cancelled (terminal)
    - Periodicidade modeled as interval_value + interval_unit (months/days/hours)
    - next_due_at computed as completed_at + interval
    - Certificates stored as 1:N via calibration_certificates table
    - Files stored at storage/app/public/calibrations/certificates/
  artifacts:
    - backend/database/migrations/2026_07_25_000001_create_calibrations_tables.php
    - backend/app/Enums/CalibrationStatus.php
    - backend/app/Models/Calibration.php
    - backend/app/Models/CalibrationCertificate.php
    - backend/app/Exceptions/CalibrationException.php
    - backend/app/Services/CalibrationService.php
    - backend/app/Services/CalibrationCertificateService.php
    - backend/database/factories/CalibrationFactory.php
    - backend/database/seeders/CalibrationSeeder.php
  key_links:
    - Calibration belongsTo Equipment (equipment_id)
    - Calibration hasMany CalibrationCertificate
    - CalibrationService::calculateNextDue() uses Carbon arithmetic (addMonths/addDays/addHours)
    - CalibrationCertificateService uploads to disk `public` at calibrations/certificates/
    - Composite index (status, next_due_at) used by scopeDueSoon
    - Composite index (equipment_id, status) used by scopeByEquipment
---

<objective>
Criar a camada de dados e modelos do módulo de Calibrações — migration composta, enums, models, exceptions, services, factory e seeder.

Purpose: Estabelecer a base de dados e lógica de negócio para o módulo de calibrações, permitindo que os plans subsequentes (API, frontend) construam sobre estruturas já definidas.
Output: Migration, CalibrationStatus enum, Calibration model, CalibrationCertificate model, CalibrationException, CalibrationService, CalibrationCertificateService, CalibrationFactory, CalibrationSeeder, permissões seedadas em RolePermissionSeeder.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md

Required reading:
- .planning/phases/05-equipamentos/05-01b-PLAN.md — Model patterns (HasUuids, SoftDeletes, LogsActivity)
- .planning/phases/07-emprestimos/07-01-PLAN.md — LoanService pattern (transactional business logic)
- .planning/codebase/CONVENTIONS.md — Naming conventions, import styles
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-calibracoes/08-CONTEXT.md
@.planning/phases/08-calibracoes/08-RESEARCH.md
@.planning/phases/08-calibracoes/08-PATTERNS.md

Prior phase patterns:
@backend/app/Enums/LoanStatus.php
@backend/app/Models/Loan.php
@backend/app/Models/EquipmentPhoto.php
@backend/app/Exceptions/LoanException.php
@backend/app/Services/LoanService.php
@backend/app/Services/EquipmentPhotoService.php
@backend/database/migrations/2026_07_21_000001_create_loans_tables.php
@backend/database/factories/LoanFactory.php
@backend/database/seeders/RolePermissionSeeder.php
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create compound migration for calibrations + calibration_certificates tables</name>
  <files>
    backend/database/migrations/2026_07_25_000001_create_calibrations_tables.php
  </files>
  <read_first>
    @backend/database/migrations/2026_07_21_000001_create_loans_tables.php
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 209-281, schema definition)
  </read_first>
  <action>
    Create compound migration file `2026_07_25_000001_create_calibrations_tables.php` following the exact pattern of `2026_07_21_000001_create_loans_tables.php`. Two tables in one migration:

    **Table `calibrations`** (per D-01, D-02, D-03, D-04, D-05):
    - `uuid('id')->primary()->default(DB::raw('gen_random_uuid()'))`
    - `foreignUuid('equipment_id')->constrained('equipments')` (D-01: 1:N equipment→calibrations)
    - `string('part_name', 255)->nullable()` (D-05: optional component/part)
    - `string('status', 20)->default('scheduled')` (D-03: scheduled|completed|cancelled)
    - `date('scheduled_date')` — data planejada
    - `timestamp('completed_at')->nullable()` — preenchido na conclusão (D-02)
    - `timestamp('next_due_at')->nullable()` — calculado: completed_at + interval (D-02)
    - `integer('interval_value')` — ex: 6, 30, 1000 (D-02)
    - `string('interval_unit', 10)` — months, days, hours (D-02)
    - `string('responsible', 255)->nullable()`
    - `string('laboratory', 255)->nullable()`
    - `string('certificate_number', 100)->nullable()`
    - `text('notes')->nullable()`
    - `foreignUuid('created_by')->nullable()->constrained('users')`
    - `uuid('updated_by')->nullable()`
    - `uuid('deleted_by')->nullable()`
    - `timestamps()`
    - `softDeletes()`
    - Índices: `equipment_id`, `status`, `scheduled_date`, `next_due_at`, `laboratory`, composite `(status, next_due_at)` (D-11: due-check query), composite `(equipment_id, status)` (CAL-04: equipment history filtered)

    **Table `calibration_certificates`** (per D-07, D-08):
    - `uuid('id')->primary()->default(DB::raw('gen_random_uuid()'))`
    - `foreignUuid('calibration_id')->constrained('calibrations')->onDelete('cascade')`
    - `string('filename', 255)` — nome original do arquivo
    - `string('filepath', 255)` — caminho no storage
    - `string('mime_type', 50)`
    - `integer('size_bytes')`
    - `string('certificate_number', 100)->nullable()`
    - `string('issuer', 255)->nullable()`
    - `date('issued_at')->nullable()`
    - `date('validity_start')->nullable()`
    - `date('validity_end')->nullable()`
    - `text('notes')->nullable()`
    - `timestamps()`
    - Índice: `calibration_id`
    - Sem SoftDeletes (mesmo padrão de equipment_photos, cascade delete)

    `down()`: drop `calibration_certificates` then `calibrations` (reverse order).
  </action>
  <acceptance_criteria>
    - Migration file exists at the expected path
    - `php artisan migrate` runs without error
    - `php artisan migrate:rollback` drops tables in correct order
    - `calibrations` table has all 15+ columns including indexes
    - `calibration_certificates` table has all 12+ columns
    - Composite indexes `(status, next_due_at)` and `(equipment_id, status)` exist
  </acceptance_criteria>
  <verify>
    <automated>cd backend; php artisan migrate --force; php artisan migrate:rollback --step 1 --force; php artisan migrate --force</automated>
  </verify>
  <done>Migration `2026_07_25_000001_create_calibrations_tables.php` created and verified running up + down + up</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CalibrationStatus enum, Calibration model, CalibrationCertificate model, CalibrationException, Factories and Seeders</name>
  <files>
    backend/app/Enums/CalibrationStatus.php
    backend/app/Models/Calibration.php
    backend/app/Models/CalibrationCertificate.php
    backend/app/Exceptions/CalibrationException.php
    backend/database/factories/CalibrationFactory.php
    backend/database/seeders/CalibrationSeeder.php
    backend/database/seeders/RolePermissionSeeder.php
    backend/database/seeders/DatabaseSeeder.php
  </files>
  <read_first>
    @backend/app/Enums/LoanStatus.php
    @backend/app/Models/Loan.php
    @backend/app/Models/EquipmentPhoto.php
    @backend/app/Exceptions/LoanException.php
    @backend/database/factories/LoanFactory.php
    @backend/database/seeders/DatabaseSeeder.php
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 292-477, enum/models/exceptions)
  </read_first>
  <action>
    Create the following files following exact pattern analogs:

    **1. `CalibrationStatus.php`** (analog: LoanStatus.php)
    - Namespace: `App\Enums`
    - Backed enum (`string`)
    - Cases: `Scheduled = 'scheduled'`, `Completed = 'completed'`, `Cancelled = 'cancelled'`
    - Method `label(): string` returning 'Agendada', 'Concluída', 'Cancelada' respectively
    - Method `canTransitionTo(CalibrationStatus $target): bool`:
      - `Scheduled` → Completed, Cancelled allowed
      - `Completed` → false (terminal per D-03)
      - `Cancelled` → false (terminal per D-03)

    **2. `Calibration.php`** (analog: Loan.php)
    - Traits: `HasFactory, HasUuids, SoftDeletes, LogsActivity`
    - Table: `calibrations`
    - $fillable: all schema columns per D-04
    - $casts: `scheduled_date => date`, `completed_at => datetime`, `next_due_at => datetime`, `interval_value => integer`, `status => CalibrationStatus::class`
    - $auditExclude: `['updated_by', 'deleted_by']`
    - Relationships:
      - `equipment()` — `belongsTo(Equipment::class)`
      - `createdBy()` — `belongsTo(User::class, 'created_by')->withDefault()`
      - `deletedBy()` — `belongsTo(User::class, 'deleted_by')->withDefault()`
      - `certificates()` — `hasMany(CalibrationCertificate::class)`
    - Accessors:
      - `getIsDueAttribute(): bool` — status === Completed && next_due_at !== null && next_due_at->isPast()
      - `getIsDueSoonAttribute(): bool` — status === Completed && next_due_at !== null && next_due_at->isFuture() && next_due_at->diffInDays(now()) <= 30
    - Scopes:
      - `scopeDue(Builder $query)` — where status=Completed && next_due_at < now()
      - `scopeDueSoon(Builder $query, int $days = 30)` — where status=Completed && next_due_at between now() and now()+days
      - `scopeByEquipment(Builder $query, string $equipmentId)` — where equipment_id
      - `scopeByStatus(Builder $query, string $status)` — where status
      - `scopeByDateRange(Builder $query, string $from, string $to)` — whereBetween scheduled_date
      - `scopeByLaboratory(Builder $query, string $laboratory)` — where laboratory ilike

    **3. `CalibrationCertificate.php`** (analog: EquipmentPhoto.php)
    - Traits: `HasFactory, HasUuids`
    - No SoftDeletes (matches equipment_photos pattern)
    - $fillable: all schema columns per D-08
    - $casts: `size_bytes => integer`, `issued_at => date`, `validity_start => date`, `validity_end => date`
    - Relationship:
      - `calibration()` — `belongsTo(Calibration::class)`

    **4. `CalibrationException.php`** (analog: LoanException.php)
    - Namespace: `App\Exceptions`, extends `Exception`
    - Construct: default message "Operação de calibração inválida.", default code 422
    - render(): JSON response with `message` and `error: calibration_error`

    **5. `CalibrationFactory.php`** (analog: LoanFactory.php)
    - Model: `App\Models\Calibration`
    - Definition with realistic defaults: random equipment from existing, `part_name` optional (50%), status `scheduled`, `scheduled_date` between now and +30 days, `interval_value` = 6 or 12, `interval_unit` = 'months', `responsible` fake name, `laboratory` fake company, `notes` optional sentence

    **6. Modify `RolePermissionSeeder.php`**:
    - Add 5 new permissions to the `$permissions` array (D-21): `calibracoes.view`, `calibracoes.create`, `calibracoes.edit`, `calibracoes.concluir`, `calibracoes.cancel`
    - Add `calibracoes.*` to admin role (all), supervisor role (view, create, concluir), laboratorista role (view), tecnico role (view)

    **7. Modify `DatabaseSeeder.php`**:
    - Add `$this->call(CalibrationSeeder::class)` call in the `run()` method, placed after LoanSeeder

    **8. Create `CalibrationSeeder.php`**:
    - Run `CalibrationFactory::new()->count(15)->create()` to seed sample calibrations
    - Create at least 3 with `completed` status and past `next_due_at` dates (due), and 3 with `completed` status and `next_due_at` within 30 days (due_soon)
  </action>
  <acceptance_criteria>
    - CalibrationStatus enum compiles with 3 cases + label() + canTransitionTo()
    - Calibration model has all relationships, accessors, and scopes
    - CalibrationCertificate model has fillable fields and calibration() relationship
    - CalibrationException renders JSON with `error: calibration_error`
    - `php artisan db:seed --class=RolePermissionSeeder` adds 5 calibracoes.* permissions
    - `php artisan db:seed --class=CalibrationSeeder` creates 15 calibration records
    - Factory generates valid calibration records
  </acceptance_criteria>
  <verify>
    <automated>cd backend; php artisan test --filter="Calibration" 2>/dev/null; if ($?) { php artisan tinker --execute="echo (new ReflectionClass('App\Enums\CalibrationStatus'))->isEnum() ? 'enum OK' : 'FAIL';" }</automated>
  </verify>
  <done>CalibrationStatus enum, Calibration model, CalibrationCertificate model, CalibrationException, factory, seeder, and permission seeds all created and verified</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create CalibrationService and CalibrationCertificateService</name>
  <files>
    backend/app/Services/CalibrationService.php
    backend/app/Services/CalibrationCertificateService.php
  </files>
  <read_first>
    @backend/app/Services/LoanService.php
    @backend/app/Services/EquipmentPhotoService.php
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 479-579, CalibrationService)
    @.planning/phases/08-calibracoes/08-RESEARCH.md (lines 839-890, CalibrationCertificateService)
  </read_first>
  <action>
    Create two service files following exact pattern analogs:

    **1. `CalibrationService.php`** (analog: LoanService.php)
    - Namespace: `App\Services`
    - Imports: `App\Enums\CalibrationStatus`, `App\Exceptions\CalibrationException`, `App\Models\Calibration`, `Carbon`, `DB`
    - Methods:

      **`create(array $data): Calibration`** — transactional per D-02:
      - DB::transaction → create Calibration with status=Scheduled, equipment_id, part_name (nullable), scheduled_date, interval_value, interval_unit, responsible (nullable), laboratory (nullable), notes (nullable), created_by=auth()->id()
      - Return `$calibration->load(['equipment:id,name,patrimony_id'])`

      **`complete(Calibration $calibration, array $data): Calibration`** — transactional per D-02, D-20:
      - Guard: if status !== Scheduled → throw CalibrationException
      - Parse completed_at from $data or default now()
      - Call `$this->calculateNextDue($completedAt, $interval_value, $interval_unit)` to compute next_due_at
      - Update: status=Completed, completed_at, next_due_at, certificate_number, responsible, laboratory, notes
      - Return fresh()->load(...)

      **`cancel(Calibration $calibration): Calibration`** — transactional:
      - Guard: if status !== Scheduled → throw CalibrationException
      - Update: status=Cancelled
      - Return fresh()->load(...)

      **`calculateNextDue(Carbon $completedAt, int $value, string $unit): Carbon`** — private:
      - match($unit) → addMonths($value) | addDays($value) | addHours($value) | default addMonths

      **`checkDueSoon(int $days = 30): Collection`** — per D-11:
      - Return Calibration::dueSoon($days)->with(['equipment:id,name,patrimony_id'])->get()

    **2. `CalibrationCertificateService.php`** (analog: EquipmentPhotoService.php, adapted per D-09, D-10)
    - Namespace: `App\Services`
    - Private constants: MAX_SIZE = 10*1024*1024, ALLOWED_MIMES = ['application/pdf','image/jpeg','image/png','image/webp'], DISK = 'public'
    - Methods:

      **`upload(UploadedFile $file, string $calibrationId): CalibrationCertificate`** — per D-09, D-10:
      - UUID-based filename with original extension: `Str::uuid() . '.' . $file->getClientOriginalExtension()`
      - Path: `calibrations/certificates/{$filename}` (D-09: storage/app/public/calibrations/certificates/)
      - `Storage::disk('public')->put($path, $file->get())` — throw RuntimeException if !$stored
      - Create CalibrationCertificate record with calibration_id, file metadata
      - NOTA: Não incluir metadados extras (certificate_number, issuer, etc.) no upload inicial — esses serão preenchidos via update do certificado caso necessário

      **`delete(string $certificateId): void`**:
      - FindOrFail → exists check → Storage delete → DB delete
      - RuntimeException if not found (findOrFail already throws ModelNotFoundException)
  </action>
  <acceptance_criteria>
    - CalibrationService::create() creates calibration with status=scheduled and loads equipment relationship
    - CalibrationService::complete() transitions scheduled→completed, computes next_due_at via Carbon
    - CalibrationService::complete() throws CalibrationException if status is not scheduled
    - CalibrationService::cancel() transitions scheduled→cancelled
    - CalibrationService::cancel() throws CalibrationException if not scheduled
    - CalibrationService::checkDueSoon() returns only calibrations with next_due_at within 30 days
    - CalibrationCertificateService::upload() stores file and creates DB record
    - CalibrationCertificateService::delete() removes file and DB record
    - Services use DB::transaction for mutation methods
  </acceptance_criteria>
  <verify>
    <automated>cd backend; php artisan tinker --execute="try { app(App\Services\CalibrationService::class); echo 'CalibrationService loaded OK'; } catch (\Throwable \$e) { echo 'FAIL: '.\$e->getMessage(); }"</automated>
  </verify>
  <done>CalibrationService (create/complete/cancel/checkDueSoon) and CalibrationCertificateService (upload/delete) created and verified</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Role |
|----------|------|------|
| `backend/database/migrations/2026_07_25_000001_create_calibrations_tables.php` | Migration | 2 tables: calibrations + calibration_certificates |
| `backend/app/Enums/CalibrationStatus.php` | Enum | 3 statuses with transition guard and labels |
| `backend/app/Models/Calibration.php` | Model | Core domain model with scopes, accessors, relationships |
| `backend/app/Models/CalibrationCertificate.php` | Model | Certificate file metadata model |
| `backend/app/Exceptions/CalibrationException.php` | Exception | Business rule violation exception |
| `backend/app/Services/CalibrationService.php` | Service | Business logic: create, complete, cancel, checkDueSoon |
| `backend/app/Services/CalibrationCertificateService.php` | Service | File upload/delete with storage management |
| `backend/database/factories/CalibrationFactory.php` | Factory | Test/seed data factory |
| `backend/database/seeders/CalibrationSeeder.php` | Seeder | 15 sample calibrations with due/due_soon states |
| `backend/database/seeders/RolePermissionSeeder.php` | Seeder (mod) | +5 calibracoes.* permissions |

<verification>
1. `php artisan migrate --force` runs clean
2. `php artisan db:seed --class=RolePermissionSeeder` adds calibracoes.* permissions
3. `php artisan db:seed --class=CalibrationSeeder` creates 15 records
4. CalibrationService methods instantiate and run without errors
5. CalibrationCertificateService upload/delete cycle works
</verification>

<success_criteria>
- Migration with compound tables created and verified
- All models, enums, exceptions, services created
- Permissions seed data includes 5 calibracoes permissions assigned to appropriate roles
- 15 sample calibrations seeded with realistic due/due_soon/cancelled states
- All services instantiable and basic operations tested
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→DB | CalibrationService mutation methods write to calibrations table |
| Storage→FS | CertificateService writes files to public storage disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | CalibrationService::complete | medium | mitigate | Status transition guard prevents completion of non-scheduled calibrations |
| T-08-02 | Tampering | CalibrationService::cancel | medium | mitigate | Status transition guard prevents cancellation of non-scheduled calibrations |
| T-08-03 | Information Disclosure | CalibrationCertificateService::upload | low | mitigate | UUID-based filenames prevent enumeration; MIME type validation |
| T-08-04 | Denial of Service | Certificate upload | low | accept | 10MB size limit; validation on mime type; storage exception handled |
</threat_model>

<output>
Create `.planning/phases/08-calibracoes/08-01-SUMMARY.md` when done
</output>
