---
phase: 07-emprestimos
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - frontend/src/modules/loans/types/loan.ts
  - frontend/src/modules/loans/services/LoanService.ts
  - frontend/src/modules/loans/store/LoanStore.ts
  - frontend/src/router/routes.ts
  - frontend/src/types/navigation.ts
autonomous: true
requirements:
  - LOAN-01
  - LOAN-02
  - LOAN-03

must_haves:
  truths:
    - "Tipos TypeScript (Loan, LoanStatus, EquipmentLoanPivot, LoanedEquipment, LoanFormData, ReturnItemFormData) definidos com campos corretos"
    - "LoanService expõe métodos: list, getById, create, update, delete, activate, returnItem, cancel, listUsers, listEquipment"
    - "LoanStore gerencia estado de loans, pagination, loading, users, equipment com actions fetchAll, fetchById, create, update, destroy, activate, returnItem, cancel, fetchUsers, fetchEquipment"
    - "Rota /loans (loans.index) registrada com lazy loading de LoanListPage"
    - "Rota /loans/:id (loans.show) registrada com lazy loading de LoanDetailPage"
    - "routeModuleMap inclui mapeamento 'loans.show' → 'operacoes'"
    - "Sidebar 'Operações > Empréstimos' já configurada (não modificada)"
  artifacts:
    - frontend/src/modules/loans/types/loan.ts
    - frontend/src/modules/loans/services/LoanService.ts
    - frontend/src/modules/loans/store/LoanStore.ts
    - frontend/src/router/routes.ts
  key_links:
    "LoanStore → LoanService → GET /api/v1/loans (backend 07-02)"
---

<objective>
Criar a camada de dados do módulo frontend de Empréstimos — TypeScript types, API service com todos os endpoints, Pinia store com estado e actions, e integração de rotas/navegação. Este plano prepara a fundação de dados que os componentes de UI (07-04) consumirão.

Purpose: Camada de dados e navegação do módulo de empréstimos. Sem types/service/store, nenhum componente de UI pode operar.
Output: 1 types, 1 service, 1 store, rotas atualizadas
</objective>

<execution_context>
@.planning/phases/07-emprestimos/07-CONTEXT.md
@frontend/src/modules/equipment/types/equipment.ts
@frontend/src/modules/equipment/services/EquipmentService.ts
@frontend/src/modules/equipment/store/EquipmentStore.ts
@frontend/src/modules/inventory/store/InventoryItemStore.ts
@frontend/src/router/routes.ts
@frontend/src/types/navigation.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types + LoanService + LoanStore</name>
  <files>
    frontend/src/modules/loans/types/loan.ts
    frontend/src/modules/loans/services/LoanService.ts
    frontend/src/modules/loans/store/LoanStore.ts
  </files>
  <action>
    **1. TypeScript types** (`frontend/src/modules/loans/types/loan.ts`):
    Seguir exatamente o padrão de `frontend/src/modules/equipment/types/equipment.ts`.

    ```typescript
    // Loan status enum
    export type LoanStatus = 'reserved' | 'active' | 'returned' | 'cancelled'

    // EquipmentLoan pivot
    export interface EquipmentLoanPivot {
      id: string
      loan_id: string
      equipment_id: string
      returned_at: string | null
      notes: string | null
      is_returned: boolean
    }

    // Equipment with pivot data (para exibição na lista e detalhe)
    export interface LoanedEquipment {
      id: string
      name: string
      patrimony_id?: string
      serial_number?: string
      pivot: EquipmentLoanPivot
    }

    // User summary (borrower/approver)
    export interface UserSummary {
      id: string
      name: string
      email?: string
    }

    // Loan interface
    export interface Loan {
      id: string
      status: LoanStatus
      borrower: UserSummary
      borrowed_at: string | null
      expected_return_at: string | null
      returned_at: string | null
      reason: string | null
      destination: string | null
      contact: string | null
      notes: string | null
      approved_by: UserSummary | null
      created_by: UserSummary | null
      equipment: LoanedEquipment[]
      is_overdue: boolean
      items_count: number
      returned_items_count: number
      progress: number
      created_at: string
      updated_at: string
    }

    // Form data for creating a loan
    export interface LoanFormData {
      borrower_id: string
      equipment_ids: string[]
      borrowed_at: string
      expected_return_at: string
      reason?: string
      destination?: string
      contact?: string
      notes?: string
      approved_by?: string
    }

    // Form data for returning an item
    export interface ReturnItemFormData {
      equipment_id: string
      returned_at?: string
      notes?: string
    }
    ```

    **2. LoanService** (`frontend/src/modules/loans/services/LoanService.ts`):
    - Seguir exatamente o padrão de EquipmentService
    ```typescript
    import { api } from '@/services/api'
    import type { Loan, LoanFormData, ReturnItemFormData } from '../types/loan'

    export const loanService = {
      async list(params?: Record<string, any>) {
        const response = await api.get('/loans', { params })
        return response.data
      },

      async getById(id: string) {
        const response = await api.get(`/loans/${id}`)
        return response.data
      },

      async create(data: LoanFormData) {
        const response = await api.post('/loans', data)
        return response.data
      },

      async update(id: string, data: Partial<LoanFormData>) {
        const response = await api.put(`/loans/${id}`, data)
        return response.data
      },

      async delete(id: string) {
        await api.delete(`/loans/${id}`)
      },

      async activate(id: string) {
        const response = await api.post(`/loans/${id}/activate`)
        return response.data
      },

      async returnItem(id: string, data: ReturnItemFormData) {
        const response = await api.post(`/loans/${id}/return`, data)
        return response.data
      },

      async cancel(id: string) {
        const response = await api.post(`/loans/${id}/cancel`)
        return response.data
      },

      async listUsers(params?: Record<string, any>) {
        const response = await api.get('/users', { params })
        return response.data
      },

      async listEquipment(params?: Record<string, any>) {
        const response = await api.get('/equipments', { params })
        return response.data
      },
    }
    ```

    **3. LoanStore** (`frontend/src/modules/loans/store/LoanStore.ts`):
    - Seguir exatamente o padrão de InventoryItemStore (que é o mais recente)
    - State: loans array, loading boolean, pagination object, users array (for borrower select), equipment array (for multi-select e filtro de equipamento)
    - Actions:
      - `fetchAll(params?)` — paginated list with filters
      - `fetchById(id)` — single loan detail
      - `create(data)` — POST new loan
      - `update(id, data)` — PUT loan
      - `destroy(id)` — DELETE loan
      - `activate(id)` — POST activate
      - `returnItem(id, data)` — POST return item
      - `cancel(id)` — POST cancel
      - `fetchUsers()` — GET /users for borrower select
      - `fetchEquipment(params?)` — GET /equipments for multi-select e filtro de equipamento
    - Pagination interface: current_page, last_page, total, per_page
    - Same error handling pattern as EquipmentStore/InventoryItemStore
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --strict src/modules/loans/types/loan.ts src/modules/loans/services/LoanService.ts src/modules/loans/store/LoanStore.ts 2>&1</automated>
  </verify>
  <done>TypeScript compila sem erros de tipo. Store e Service seguem o padrão dos módulos existentes. LoanStore.equipment populado para uso no filtro de equipamento da ListPage (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: Route integration + sidebar + navigation wiring</name>
  <files>
    frontend/src/router/routes.ts
    frontend/src/types/navigation.ts
  </files>
  <action>
    **1. Atualizar rotas** em `frontend/src/router/routes.ts`:

    Substituir o placeholder loans.index (linhas que importam PlaceholderPage para a rota loans):
    ```typescript
    {
      path: '/loans',
      name: 'loans.index',
      component: () => import('@/modules/loans/pages/LoanListPage.vue'),
      meta: { requiresAuth: true, module: 'loans.index', title: 'Empréstimos' },
    },
    ```

    Adicionar rota de detalhe APÓS a rota index:
    ```typescript
    {
      path: '/loans/:id',
      name: 'loans.show',
      component: () => import('@/modules/loans/pages/LoanDetailPage.vue'),
      meta: { requiresAuth: true, module: 'loans.index', title: 'Detalhes do Empréstimo' },
    },
    ```

    **2. Atualizar navigation.ts** em `frontend/src/types/navigation.ts`:

    Adicionar ao `routeModuleMap`:
    ```typescript
    'loans.show': 'operacoes',
    ```

    A sidebar já está configurada para Empréstimos em Operações com:
    - label: 'Empréstimos'
    - icon: 'pi pi-share-alt'
    - route: 'loans.index'
    - permission: 'emprestimos.view'
    Isso está correto e não precisa ser alterado.

    **Verificação:** Confirmar que a sidebar existente está correta (não modificar se já estiver configurada com `emprestimos.view`). Apenas adicionar `loans.show` ao routeModuleMap.
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit src/router/routes.ts src/types/navigation.ts 2>&1</automated>
  </verify>
  <done>Rotas /loans e /loans/:id registradas com lazy loading (placeholder loans.index substituído). routeModuleMap inclui loans.show. Sidebar já está configurada e funcional.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit --strict src/modules/loans/types/loan.ts` — sem erros de tipo
2. `cd frontend && npx vue-tsc --noEmit src/router/routes.ts src/types/navigation.ts` — sem erros
3. `cd frontend && npx vite build` — build produz sem erros
</verification>

<success_criteria>
- Tipos TypeScript completos (Loan, LoanStatus, pivot, form data)
- LoanService com todos os 10 métodos de API REST
- LoanStore com state (loans, pagination, users, equipment) e todas as actions
- Rotas /loans e /loans/:id registradas com lazy loading
- routeModuleMap atualizado para incluir loans.show
- Build frontend sem erros (tsc + vue-tsc + vite build)
</success_criteria>

<output>
`.planning/phases/07-emprestimos/07-03-SUMMARY.md`
</output>
