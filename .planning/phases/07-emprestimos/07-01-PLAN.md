---
phase: 07-emprestimos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database/migrations/2026_07_21_000001_create_loans_tables.php
  - backend/database/migrations/2026_07_21_000002_create_notifications_table.php
  - backend/app/Models/Loan.php
  - backend/app/Models/EquipmentLoan.php
  - backend/app/Enums/LoanStatus.php
  - backend/app/Services/LoanService.php
  - backend/app/Exceptions/LoanException.php
  - backend/database/factories/LoanFactory.php
  - backend/database/seeders/LoanSeeder.php
  - backend/database/seeders/DatabaseSeeder.php
autonomous: true
requirements:
  - LOAN-01
  - LOAN-02

must_haves:
  truths:
    - "A tabela `loans` existe com todos os campos especificados (D-05): id, borrower_id, status, borrowed_at, expected_return_at, returned_at, reason, destination, contact, notes, approved_by, created_by"
    - "A tabela pivot `equipment_loan` existe com os campos: loan_id, equipment_id, returned_at (nullable), notes (D-01, D-18)"
    - "A tabela `notifications` existe com schema padrão Laravel (id UUID, type, notifiable_type, notifiable_id, data JSON, read_at) — necessária para notificações de atraso do CheckOverdueLoans command (D-08)"
    - "O model Loan usa HasUuids, SoftDeletes, LogsActivity e possui cast de status para LoanStatus enum (D-03)"
    - "O model EquipmentLoan é o modelo da pivot com returned_at tracking individual (D-04, D-19)"
    - "O LoanStatus enum define os 4 estados: reserved, active, returned, cancelled"
    - "O LoanService implementa create() com transação DB, attach de equipamentos, e valida se equipamento não está emprestado"
    - "O LoanService implementa returnItem() para devolução individual de item na pivot"
    - "O LoanService implementa cancel() com validação de status permitido (só reserved pode ser cancelado)"
    - "A LoanFactory gera dados realistas de empréstimos"
    - "O LoanSeeder é registrado no DatabaseSeeder"
  artifacts:
    - backend/database/migrations/2026_07_21_000001_create_loans_tables.php
    - backend/database/migrations/2026_07_21_000002_create_notifications_table.php
    - backend/app/Models/Loan.php
    - backend/app/Models/EquipmentLoan.php
    - backend/app/Enums/LoanStatus.php
    - backend/app/Services/LoanService.php
    - backend/app/Exceptions/LoanException.php
    - backend/database/factories/LoanFactory.php
    - backend/database/seeders/LoanSeeder.php
  key_links:
    - "Migration → Equipment model (equipment_id FK) e User model (borrower_id FK)"
    - "Migration → notifications table (consumida pelo CheckOverdueLoans command em 07-02)"
    - "LoanService → valida conflito de datas (equipamento já emprestado no período)"
    - "LoanStatus enum → status transition validation no LoanService"
    - "LoanFactory → Loan model + Equipment model (attach)"
---

<objective>
Criar a base de dados e models do módulo de Empréstimos — migration compound com as tabelas `loans`, `equipment_loan` e `notifications`, models Loan e EquipmentLoan com relacionamentos, enum de status, LoanService com lógica transacional de criação/devolução/cancelamento, exceção customizada, factory e seeder.

Purpose: Fundação do módulo de empréstimos. Sem essa camada, nenhuma funcionalidade de API ou frontend pode operar. A tabela `notifications` é necessária para o comando de notificação de atrasos (07-02).
Output: 2 migrations executáveis, 2 models, 1 enum, 1 service, 1 exception, 1 factory, 1 seeder
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/database/migrations/2026_07_20_000001_create_inventory_tables.php
@backend/app/Models/Equipment.php
@app/Models/InventoryMovement.php
@backend/app/Services/InventoryMovementService.php
@backend/app/Exceptions/InsufficientStockException.php
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Compound migration — loans + equipment_loan + notifications tables</name>
  <files>
    backend/database/migrations/2026_07_21_000001_create_loans_tables.php
    backend/database/migrations/2026_07_21_000002_create_notifications_table.php
  </files>
  <action>
    Criar DUAS migrations:

    **Migration A — `2026_07_21_000001_create_loans_tables.php`** (compound, mesmo formato de `2026_07_20_000001_create_inventory_tables.php`) contendo duas tabelas:

    **1. Tabela `loans`** com:
    - `id` UUID primary key default `gen_random_uuid()`
    - `borrower_id` — FK → users (D-02), NOT NULL
    - `status` — varchar(20) NOT NULL, default 'reserved' (D-03)
    - `borrowed_at` — timestamp NOT NULL (D-05)
    - `expected_return_at` — timestamp NOT NULL
    - `returned_at` — timestamp nullable (preenchido quando TODOS itens devolvidos — D-04)
    - `reason` — text nullable (D-05)
    - `destination` — varchar(255) nullable (setor/laboratório destino — D-05)
    - `contact` — varchar(255) nullable
    - `notes` — text nullable
    - `approved_by` — FK → users, nullable (D-05)
    - `created_by` — FK → users, nullable
    - `user_id` — FK → users (proprietário do registro)
    - `updated_by` — uuid nullable
    - `deleted_by` — uuid nullable
    - timestamps + softDeletes
    - Índices: `borrower_id`, `status`, `expected_return_at`, composite `(status, expected_return_at)`, `(borrower_id, status)`

    **2. Tabela pivot `equipment_loan`** com (D-01, D-18):
    - `id` UUID primary key default `gen_random_uuid()`
    - `loan_id` — FK → loans, ON DELETE CASCADE
    - `equipment_id` — FK → equipments (tabela `equipments`)
    - `returned_at` — timestamp nullable (D-19: inicia null, preenche na devolução)
    - `notes` — text nullable (observações por item na devolução — D-18)
    - timestamps (created_at útil para ordem de inserção)
    - Índice UNIQUE `(loan_id, equipment_id)` para evitar duplicatas
    - Índices: `equipment_id`, `returned_at`
    - Sem softDeletes (pivot imutável)

    **Migration B — `2026_07_21_000002_create_notifications_table.php`** (standalone):
    Seguir schema padrão do Laravel para tabela `notifications`, adaptado para UUIDs:

    - `id` — UUID primary key default `gen_random_uuid()`
    - `type` — varchar(255) NOT NULL (ex: 'App\Notifications\LoanOverdue')
    - `notifiable_type` — varchar(255) NOT NULL (ex: 'App\Models\User')
    - `notifiable_id` — uuid NOT NULL (FK lógica para users — sem FK constraint porque notifiable pode ser qualquer model)
    - `data` — text NOT NULL (JSON armazenado como string — compatível com json_encode usado no CheckOverdueLoans command)
    - `read_at` — timestamp nullable
    - `created_at`, `updated_at` — timestamps
    - Índices: `(notifiable_type, notifiable_id)` composto (busca eficiente por destinatário), `type`

    Criar migration B separada (não compound) porque `notifications` é uma tabela de sistema reutilizável. Aplicar o mesmo padrão de snake_case, comentários D-NN, e estrutura `return new class extends Migration`.

    Para ambas, usar Schema Builder do Laravel. NÃO usar `DB::raw('gen_random_uuid()')` — usar `uuid()` helper do Schema Builder.
  </action>
  <verify>
    <automated>cd backend && php artisan migrate --force 2>&1; php artisan migrate:rollback --step=2 2>&1; php artisan migrate --force 2>&1</automated>
  </verify>
  <done>Migration roda `php artisan migrate` sem erros, cria as 3 tabelas (loans, equipment_loan, notifications), e `php artisan migrate:rollback --step=2` as remove corretamente. Tabelas inspecionáveis via Schema Builder ou `\dt` no PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 2: Models — Loan, EquipmentLoan, LoanStatus enum + LoanFactory + LoanSeeder</name>
  <files>
    backend/app/Enums/LoanStatus.php
    backend/app/Models/Loan.php
    backend/app/Models/EquipmentLoan.php
    backend/database/factories/LoanFactory.php
    backend/database/seeders/LoanSeeder.php
    backend/database/seeders/DatabaseSeeder.php
  </files>
  <action>
    **1. LoanStatus enum** (`backend/app/Enums/LoanStatus.php`):
    - PHP 8 enum backing string
    - Cases: `Reserved = 'reserved'`, `Active = 'active'`, `Returned = 'returned'`, `Cancelled = 'cancelled'`
    - Método `label()` retornando rótulo pt-BR: 'Reservado', 'Ativo', 'Devolvido', 'Cancelado'
    - Método `canTransitionTo(LoanStatus $target): bool` com as regras de transição (D-03):
      - reserved → active, cancelled
      - active → returned
      - returned → (nenhuma — terminal)
      - cancelled → (nenhuma — terminal)

    **2. Model `Loan`** (`backend/app/Models/Loan.php`):
    - Namespace App\Models
    - Traits: `HasFactory, HasUuids, SoftDeletes, LogsActivity`
    - Tabela: `loans`
    - `$fillable`: todos os campos exceto id/timestamps: 'borrower_id', 'status', 'borrowed_at', 'expected_return_at', 'returned_at', 'reason', 'destination', 'contact', 'notes', 'approved_by', 'created_by', 'user_id', 'updated_by', 'deleted_by'
    - `$casts`: 'borrowed_at' => 'datetime', 'expected_return_at' => 'datetime', 'returned_at' => 'datetime', 'status' => LoanStatus::class
    - `$auditExclude`: ['updated_by', 'deleted_by'] (seguir padrão de auditoria)
    - Relacionamentos:
      - `borrower()` → belongsTo(User::class, 'borrower_id')
      - `approvedBy()` → belongsTo(User::class, 'approved_by')->withDefault()
      - `createdBy()` → belongsTo(User::class, 'created_by')->withDefault()
      - `user()` → belongsTo(User::class)
      - `deletedBy()` → belongsTo(User::class, 'deleted_by')->withDefault()
      - `equipment()` → belongsToMany(Equipment::class, 'equipment_loan')->withPivot(['id', 'returned_at', 'notes'])->withTimestamps()->using(EquipmentLoan::class)
      - `items()` → hasMany(EquipmentLoan::class) — atalho para itens da pivot
    - Escopo: `scopeOverdue(Builder $query)` — where('status', LoanStatus::Active) e where('expected_return_at', '<', now())
    - Escopo: `scopeByStatus(Builder $query, string $status)`
    - Escopo: `scopeByBorrower(Builder $query, string $borrowerId)`
    - Escopo: `scopeByDateRange(Builder $query, string $from, string $to)` — filtra por borrowed_at
    - Escopo: `scopeByEquipment(Builder $query, string $equipmentId)` — filtra por equipment_id na pivot
    - Acessor: `isOverdue` boolean — true se status=active e expected_return_at passou
    - Acessor: `returnedItemsCount` e `totalItemsCount` para progresso de devolução

    **3. Model `EquipmentLoan`** (`backend/app/Models/EquipmentLoan.php`):
    - Namespace App\Models
    - Traits: `HasUuids` (sem SoftDeletes — pivot imutável)
    - Tabela: `equipment_loan`
    - `$fillable`: 'loan_id', 'equipment_id', 'returned_at', 'notes'
    - `$casts`: 'returned_at' => 'datetime'
    - Relacionamentos:
      - `loan()` → belongsTo(Loan::class)
      - `equipment()` → belongsTo(Equipment::class)
    - Acessor: `isReturned` boolean — true se returned_at não for null

    Usar o mesmo padrão de imports, traits e docblocks dos models existentes (Equipment, InventoryItem).

    **4. LoanFactory** (`backend/database/factories/LoanFactory.php`):
    - Namespace Database\Factories
    - Model: Loan::class
    - `definition()` retornando:
      - `borrower_id` → User::factory()
      - `status` → random entre LoanStatus::Reserved e LoanStatus::Active
      - `borrowed_at` → now() sub 1-30 dias
      - `expected_return_at` → borrowed_at + 1-14 dias
      - `returned_at` → null (só preenchido se todos itens devolvidos)
      - `reason` → fake()->sentence(3)
      - `destination` → fake()->word() . ' Lab'
      - `contact` → fake()->phoneNumber()
      - `notes` → fake()->optional()->sentence()
      - `user_id` → User::factory()
    - Método `reserved()` — state com status=reserved
    - Método `active()` — state com status=active
    - Método `returned()` — state com status=returned, returned_at preenchido
    - Método `withItems(int $count = 2)` — afterCreating que attach equipamentos aleatórios via `$loan->equipment()->attach($equipmentIds)`
    - Se status=returned, preencher returned_at nos itens da pivot

    **5. LoanSeeder** (`backend/database/seeders/LoanSeeder.php`):
    - Namespace Database\Seeders
    - Cria 5 equipamentos (se não existirem) para referência
    - Cria 10 empréstimos com factory: 3 reserved, 3 active, 2 returned, 2 cancelled (usando states)
    - Cada empréstimo com 1-3 equipamentos
    - Log no console: "Seed: {count} empréstimos criados"

    **6. Atualizar DatabaseSeeder**:
    Adicionar `$this->call([LoanSeeder::class]);` após InventorySeeder.

    Seguir exatamente o padrão de EquipmentSeeder e InventorySeeder para factories e seeders.
  </action>
  <verify>
    <automated>cd backend && php artisan tinker --execute="(new App\Enums\LoanStatus('reserved'))->label();" 2>&1; php artisan tinker --execute="\$l = new App\Models\Loan(); echo get_class(\$l);" 2>&1; php artisan db:seed --class=LoanSeeder --force 2>&1</automated>
  </verify>
  <done>LoanStatus enum carrega sem erros e retorna label pt-BR. Model Loan e EquipmentLoan instanciam sem erros. Relacionamentos são reconhecidos pelo Laravel. Seeder cria 10 empréstimos com estados variados. DatabaseSeeder inclui LoanSeeder.</done>
</task>

<task type="auto">
  <name>Task 3: LoanService + LoanException</name>
  <files>
    backend/app/Exceptions/LoanException.php
    backend/app/Services/LoanService.php
  </files>
  <action>
    **1. LoanException** (`backend/app/Exceptions/LoanException.php`):
    - Mesmo padrão de `InsufficientStockException`
    - Namespace App\Exceptions
    - extends Exception
    - Construtor com message('Operação de empréstimo inválida.') e code(422) default
    - Método `render()` retornando JsonResponse com message e error 'loan_error'

    **2. LoanService** (`backend/app/Services/LoanService.php`):
    - Namespace App\Services
    - Métodos:

    `create(array $data): Loan` — transacional (DB::transaction):
      - Recebe: borrower_id, borrowed_at, expected_return_at, equipment_ids (array), reason, destination, contact, notes, approved_by (opcional)
      - Valida: (D-02) borrower_id existe em users
      - Valida: (D-01) equipment_ids não vazio
      - Valida: NENHUM equipment_id pode estar em outro loan com status reserved ou active no período — usar subquery na equipment_loan + loans para evitar conflito de datas (Agent's Discretion: overlap validation)
      - Cria Loan com status=LoanStatus::Reserved, user_id=auth()->id()
      - Attach equipamentos via `$loan->equipment()->attach($equipmentIds)` — todos com returned_at=null (D-19)
      - Retorna $loan com relacionamentos carregados
      - Dispara LoanException com mensagem específica para cada violação

    `activate(Loan $loan): Loan` — transacional:
      - Valida: status atual é Reserved (D-03)
      - Altera status para Active
      - Se borrowed_at não foi preenchido, usa now()
      - Salva e retorna

    `returnItem(Loan $loan, string $equipmentId, ?string $returnedAt, ?string $notes): EquipmentLoan` — transacional (D-04, D-14):
      - Valida: status do loan é Active
      - Valida: equipment pertence ao loan
      - Atualiza returned_at na pivot (default now() se não informado)
      - Atualiza notes na pivot
      - Verifica se TODOS os itens do loan foram devolvidos
      - Se sim: atualiza loan.status=Returned e loan.returned_at=now()
      - Retorna o EquipmentLoan atualizado

    `cancel(Loan $loan): Loan` — transacional:
      - Valida: status atual é Reserved (só reserva pode ser cancelada)
      - Altera status para Cancelled
      - Salva e retorna

    `checkOverdue(): array` — consulta loans overdue (status=active, expected_return_at < now()) e retorna collection para processamento no comando

    **Validação de equipamento já emprestado:**
    No método create, antes de attach, verificar se algum equipment_id já está em equipment_loan com loan.status IN ('reserved', 'active') e períodos conflitantes. Lançar LoanException com mensagem listando equipamentos conflitantes. (Agent's Discretion: overlap strategy — considerar qualquer loan ativo/reservado no período como conflito.)

    Usar o mesmo padrão transacional de InventoryMovementService (DB::transaction + lockForUpdate opcional).
  </action>
  <verify>
    <automated>cd backend && php artisan tinker --execute="\$service = app(App\Services\LoanService::class); echo get_class(\$service);" 2>&1</automated>
  </verify>
  <done>LoanService instancia via container. LoanException renderiza resposta JSON com status 422. Métodos create/returnItem/cancel implementam validação de transição de status e operam dentro de transação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → LoanService | Dados de criação de empréstimo cruzam validação de negócio |
| LoanService → Database | Transação DB com INSERT em loans + equipment_loan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | LoanService::create() | high | mitigate | Validação de equipment_ids: verificar se equipamento já está em loan ativo/reservado no período antes de criar. Lançar LoanException com lista de conflitos. |
| T-07-02 | Elevation of Privilege | Loan status transitions | high | mitigate | LoanService valida transições permitidas via LoanStatus::canTransitionTo(). Apenas reserved→active, reserved→cancelled, active→returned são permitidas. |
| T-07-03 | Information Disclosure | Loan listagem | medium | mitigate | Scopes de filtro no model Loan permitem consultas eficientes sem expor dados sensíveis além do necessário. Permissões (emprestimos.view) controlam acesso na camada Controller. |
</threat_model>

<verification>
1. `php artisan migrate --force` executa sem erros (3 tabelas criadas)
2. `php artisan migrate:rollback --step=2` reverte corretamente
3. `php artisan db:seed --class=LoanSeeder --force` executa sem erros
4. `php artisan tinker` consegue instanciar Loan, EquipmentLoan, LoanStatus
5. LoanStatus::Active->canTransitionTo(LoanStatus::Returned) retorna true; LoanStatus::Returned->canTransitionTo(LoanStatus::Active) retorna false
6. Tabela `notifications` existe com schema (id UUID, type, notifiable_type, notifiable_id, data, read_at)
</verification>

<success_criteria>
- Migration A com tabelas `loans` e `equipment_loan` criada e rollbackável
- Migration B com tabela `notifications` criada e rollbackável
- Model Loan com todos os relacionamentos, casts e scopes funcionando
- Model EquipmentLoan com tracking de returned_at individual
- LoanStatus enum com validação de transições
- LoanService com create (transacional + validação conflito), activate, returnItem (devolução parcial com detecção de conclusão), cancel
- LoanFactory gerando dados de teste com equipamentos
- LoanSeeder registrado no DatabaseSeeder
</success_criteria>

<output>
`.planning/phases/07-emprestimos/07-01-SUMMARY.md`
</output>
