---
phase: 07-emprestimos
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - frontend/src/modules/loans/pages/LoanListPage.vue
  - frontend/src/modules/loans/pages/LoanDetailPage.vue
  - frontend/src/modules/loans/components/LoanCreateDialog.vue
  - frontend/src/modules/loans/components/LoanReturnDialog.vue
  - frontend/src/modules/loans/components/LoanInfoTab.vue
  - frontend/src/modules/loans/components/LoanItemsTab.vue
  - frontend/src/modules/loans/components/LoanTimelineTab.vue
autonomous: true
requirements:
  - LOAN-01
  - LOAN-02
  - LOAN-03

must_haves:
  truths:
    - "Usuário com permissão emprestimos.view vê lista de empréstimos com DataTable paginada e filtros por período, status e equipamento (D-11)"
    - "Usuário com permissão emprestimos.create pode criar novo empréstimo via Dialog (D-13)"
    - "Usuário pode clicar em um empréstimo e ver DetailPage com 3 abas (D-12)"
    - "Usuário pode devolver itens parcialmente via Dialog de devolução na DetailPage (D-14)"
    - "Usuário pode ativar/cancelar empréstimo da DetailPage"
    - "Usuário vê indicador visual de atraso para empréstimos vencidos"
    - "Filtro de equipamento na LoanListPage usa MultiSelect populado por LoanStore.fetchEquipment() — compatível com D-11"
  artifacts:
    - frontend/src/modules/loans/pages/LoanListPage.vue
    - frontend/src/modules/loans/pages/LoanDetailPage.vue
    - frontend/src/modules/loans/components/LoanCreateDialog.vue
    - frontend/src/modules/loans/components/LoanReturnDialog.vue
    - frontend/src/modules/loans/components/LoanInfoTab.vue
    - frontend/src/modules/loans/components/LoanItemsTab.vue
    - frontend/src/modules/loans/components/LoanTimelineTab.vue
  key_links:
    - "LoanListPage → LoanStore.fetchAll() com params de filtro (incluindo equipment_id) → LoanService.list() → GET /api/v1/loans"
    - "LoanListPage → LoanStore.equipment (populado em onMounted via store.fetchEquipment()) → filtro MultiSelect"
    - "LoanCreateDialog → LoanStore.create() → POST /api/v1/loans → recarrega lista"
    - "LoanDetailPage → LoanStore.fetchById() → GET /api/v1/loans/{id}"
    - "LoanReturnDialog → LoanStore.returnItem() → POST /api/v1/loans/{id}/return"
---

<objective>
Criar os componentes de UI do módulo de Empréstimos — LoanListPage com DataTable paginada + filtros (incluindo filtro por equipamento), LoanDetailPage com 3 abas (Dados, Itens, Timeline), diálogo de criação, diálogo de devolução parcial, e componentes de aba. Consome a camada de dados criada em 07-03.

Purpose: Interface completa do módulo de empréstimos seguindo o padrão ListPage + DetailPage + Dialog (D-10).
Output: 2 pages, 5 components
</objective>

<execution_context>
@.planning/phases/07-emprestimos/07-CONTEXT.md
@frontend/src/modules/equipment/pages/EquipmentListPage.vue
@frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
@frontend/src/modules/equipment/store/EquipmentStore.ts
@frontend/src/modules/equipment/services/EquipmentService.ts
@frontend/src/modules/equipment/types/equipment.ts
@frontend/src/modules/inventory/pages/InventoryItemListPage.vue
@frontend/src/modules/loans/store/LoanStore.ts
@frontend/src/modules/loans/types/loan.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: LoanListPage with filters (including equipment per D-11) + LoanCreateDialog</name>
  <files>
    frontend/src/modules/loans/pages/LoanListPage.vue
    frontend/src/modules/loans/components/LoanCreateDialog.vue
  </files>
  <action>
    **1. LoanListPage** (`frontend/src/modules/loans/pages/LoanListPage.vue`):
    - Seguir exatamente o padrão de InventoryItemListPage.vue (mais recente)
    - Template:
      - Header: "Empréstimos" + subtitle "Gerencie os empréstimos de equipamentos"
      - Botão "Novo Empréstimo" (visível se authStore.hasPermission('emprestimos.create'))
      - Toolbar com filtros (D-11: filtros por período, status, equipamento):
        - InputText com placeholder "Buscar por tomador..." e debounce 400ms
        - Select para status (Todos, Reservado, Ativo, Devolvido, Cancelado) — mapear LoanStatus
        - MultiSelect para equipamento: options populadas de `store.equipment` com label `"${eq.name} - ${eq.patrimony_id}"`, value `eq.id`. Placeholder "Filtrar por equipamento..." — (D-11: filtro por equipamento)
        - DateRange (de/até) para borrowed_at — usar PrimeVue DatePicker se disponível, ou dois inputs de data
      - DataTable com lazy pagination, stripedRows, size="small":
        - Colunas: Tomador (borrower.name), Equipamentos (nomes concatenados ou tooltip), Data Retirada (borrowed_at formatada), Data Prevista Devolução (expected_return_at formatada), Status (Tag com cor), Ações
        - Coluna Status: Tag com severity mapeada: reserved=info, active=warning, returned=success, cancelled=secondary. Se is_overdue=true, exibir badge "Atrasado" adicional.
        - Coluna Ações: botões eye (detalhe), edit (se edit permission e status=reserved), delete (se delete permission e status=reserved ou cancelled)
      - Row styling opcional: destaque vermelho se is_overdue
      - Toast + ConfirmDialog para feedback

    - Script:
      - `filters` ref com search, status, equipment_ids (array de strings para MultiSelect), from, to
      - `statusOptions` com {label, value} mapeando LoanStatus para pt-BR
      - `equipmentOptions` — computed de store.equipment mapeado para {label, value} para MultiSelect
      - Métodos: onSearch (debounce), fetchLoans, onPage, onFilterEquipamento (atualiza equipment_ids e re-fetch)
      - `viewLoan(loan)` → router.push /loans/{id}
      - `confirmDelete(loan)` → confirm dialog + store.destroy
      - `showCreateDialog` ref → controla visibilidade do LoanCreateDialog
      - onMounted: fetchLoans + store.fetchUsers() + store.fetchEquipment({ all: true }) — carrega equipamentos para o MultiSelect de filtro
      - computed `firstRow` para paginação
      - `handleFilterChange()` — sempre que qualquer filtro muda, reinicia paginação e chama fetchLoans com params atualizados

    **2. LoanCreateDialog** (`frontend/src/modules/loans/components/LoanCreateDialog.vue`):
    - Dialog visível condicionalmente (controlado por v-model:visible do parent)
    - Header: "Novo Empréstimo"
    - Formulário dentro do Dialog:
      - Borrower: Select com busca (filter) — options populadas de store.users
      - Equipment: MultiSelect com busca — options populadas de store.equipment com label "name - patrimony_id"
      - borrowed_at: DatePicker (calendário), label "Data de Retirada"
      - expected_return_at: DatePicker, label "Previsão de Devolução"
      - reason: Textarea opcional, label "Motivo"
      - destination: InputText opcional, label "Destino / Setor"
      - contact: InputText opcional, label "Contato"
      - notes: Textarea opcional, label "Observações"
      - approved_by: Select opcional, label "Aprovador" — filtra usuários com role supervisor/admin

    - Validação: required highlight nos campos obrigatórios (borrower, equipment, datas)
    - Botões: "Cancelar" e "Salvar"
    - On save: chama store.create(), fecha dialog, mostra toast success, emite evento para parent recarregar
    - Loading state no botão Salvar durante a requisição
    - Error handling: toast error com mensagem do backend

    Seguir exatamente o padrão de componentes dos módulos existentes (template Vue 3 + script setup lang="ts" + scoped styles).
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit src/modules/loans/pages/LoanListPage.vue src/modules/loans/components/LoanCreateDialog.vue 2>&1</automated>
  </verify>
  <done>Componentes compilam sem erros. LoanListPage exibe DataTable com filtros funcionais, incluindo filtro MultiSelect por equipamento (D-11). LoanCreateDialog abre, valida, e submete criação.</done>
</task>

<task type="auto">
  <name>Task 2: LoanDetailPage with 3 tab components + LoanReturnDialog</name>
  <files>
    frontend/src/modules/loans/pages/LoanDetailPage.vue
    frontend/src/modules/loans/components/LoanInfoTab.vue
    frontend/src/modules/loans/components/LoanItemsTab.vue
    frontend/src/modules/loans/components/LoanTimelineTab.vue
    frontend/src/modules/loans/components/LoanReturnDialog.vue
  </files>
  <action>
    **1. LoanDetailPage** (`frontend/src/modules/loans/pages/LoanDetailPage.vue`):
    - Seguir exatamente o padrão de EquipmentDetailPage.vue
    - Template:
      - Header com back button + loan id/código + Tag de status
      - Ações no header (condicionais por permissão/status):
        - Botão "Ativar" — visible se status=reserved e hasPermission('emprestimos.edit') — chama store.activate()
        - Botão "Cancelar" — visible se status=reserved e hasPermission('emprestimos.finalizar') — chama store.cancel() com confirm
        - Botão "Devolver Itens" — visible se status=active e hasPermission('emprestimos.edit') — abre LoanReturnDialog
      - Tabs (D-12):
        - "Dados do Empréstimo" → LoanInfoTab
        - "Itens" → LoanItemsTab
        - "Timeline" → LoanTimelineTab
    - Script: fetch loan por id no onMounted, loading state, error handling
    - Estado reativo para o loan, activeTab, showReturnDialog

    **2. LoanInfoTab** (`frontend/src/modules/loans/components/LoanInfoTab.vue`):
    - Props: `loan: Loan`
    - Exibe em grid/cards:
      - Tomador: nome, contato (do loan)
      - Período: borrowed_at → expected_return_at formatados
      - Progresso de devolução: barra de progresso PrimeVue com percentual
      - Destino, Motivo
      - Aprovador (se preenchido)
      - Observações
      - Status atual com Tag
      - Se atrasado: card de alerta vermelho com dias em atraso
    - Layout: usar PrimeVue Card + Divider + campo-label padrão

    **3. LoanItemsTab** (`frontend/src/modules/loans/components/LoanItemsTab.vue`):
    - Props: `items: LoanedEquipment[]`
    - DataTable simples (sem paginação — poucos itens):
      - Colunas: Equipamento (nome), Patrimônio, Nº Série, Status (Devolvido/Pendente), Devolvido Em, Observações
      - Tag verde "Devolvido" se returned_at preenchido, Tag amarela "Pendente" se não
      - Se returned_at preenchido, exibir data formatada e observações da devolução

    **4. LoanTimelineTab** (`frontend/src/modules/loans/components/LoanTimelineTab.vue`):
    - Props: `loan: Loan`
    - Timeline visual (PrimeVue Timeline):
      - Eventos:
        1. "Empréstimo criado" — data created_at, por created_by
        2. "Status: Reservado" — data borrowed_at se preenchida
        3. "Empréstimo ativado" — se houve transição para active (monitorar via created_at / observar se há auditoria)
        4. Para cada item devolvido: "Item {nome} devolvido" — data returned_at
        5. "Empréstimo concluído" — data returned_at (quando loan.status=returned)
        6. "Empréstimo cancelado" — se status=cancelled
      - Caso não haja dados de timeline (sem logs activity), exibir mensagem "Histórico disponível em breve"
      - Layout: Timeline component do PrimeVue com ícones

    **5. LoanReturnDialog** (`frontend/src/modules/loans/components/LoanReturnDialog.vue`):
    - Dialog controlado por v-model:visible
    - Header: "Devolução de Itens"
    - Lista de equipamentos NÃO devolvidos (filter items onde !pivot.is_returned):
      - Cada item em linha/checkbox com:
        - Nome do equipamento
        - InputText para observações (notes) por item
        - DatePicker para data de devolução (default now())
    - Botão "Confirmar Devolução" apenas quando ao menos 1 item selecionado
    - On confirm:
      - Para cada item selecionado: chama store.returnItem() sequencialmente (ou em paralelo)
      - Recarrega loan detail
      - Toast success "Devolução registrada com sucesso"
      - Fecha dialog
    - Error handling por item: toast com detalhe do erro

    Seguir exatamente o padrão dos componentes Vue 3 + script setup + scoped styles dos módulos existentes (EquipmentDetailPage, EquipmentInfoTab, etc).
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit src/modules/loans/pages/LoanDetailPage.vue src/modules/loans/components/LoanInfoTab.vue src/modules/loans/components/LoanItemsTab.vue src/modules/loans/components/LoanTimelineTab.vue src/modules/loans/components/LoanReturnDialog.vue 2>&1</automated>
  </verify>
  <done>Componentes compilam sem erros. LoanDetailPage exibe tabs navegáveis. LoanReturnDialog permite devolução parcial.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` — sem erros de tipo em todos os novos arquivos
2. `cd frontend && npx vite build` — build produz sem erros
3. Navegação: sidebar → Empréstimos → /loans carrega LoanListPage
4. Filtro de equipamento na LoanListPage filtra corretamente (D-11)
5. Clique em empréstimo → /loans/:id carrega LoanDetailPage com 3 abas
6. Botão "Novo Empréstimo" abre dialog com formulário completo
7. Botão "Devolver Itens" na DetailPage abre LoanReturnDialog
</verification>

<success_criteria>
- LoanListPage exibe DataTable paginada com filtros por status, período, equipamento (MultiSelect), e busca por tomador
- LoanCreateDialog funcional com seleção de tomador, multi-equipamento, datas e campos opcionais
- LoanDetailPage com 3 abas: Dados, Itens (com status de devolução), Timeline
- LoanReturnDialog permite devolução parcial com observações por item
- Indicador visual de atraso na lista e no detalhe
- Filtro de equipamento funcional conforme D-11
- Validação de permissões em todos os botões de ação
- Build frontend sem erros (vue-tsc + vite build)
</success_criteria>

<output>
`.planning/phases/07-emprestimos/07-04-SUMMARY.md`
</output>
