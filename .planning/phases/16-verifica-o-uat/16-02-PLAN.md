---
phase: 16-verifica-o-uat
plan: 02
title: Execução UAT-01 — 6 cenários de Aferições (Fase 09) com registro em 16-UAT.md (W2)
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .planning/phases/16-verifica-o-uat/16-UAT.md   # registro dos cenários (CRIADO neste plano)
autonomous: false
requirements: [UAT-01]
created: 2026-08-09

# Goal-backward verification
must_haves:
  truths:
    - "16-UAT.md criado no formato XX-UAT.md (frontmatter status/phase/source/started/updated + Current Test + lista Tests + Summary + Gaps)"
    - "Os 6 cenários UAT-01 registrados com o texto CANÔNICO recuperado de 09-UAT.md (git ddff1a5^), com coverage_id UAT-01-S1..S6 — nenhum cenário recriado de memória"
    - "Usuários de permissão criados via tinker: tecnico@uat.test (role tecnico, SEM afericoes.view) e consulta@uat.test (role consulta, COM afericoes.view SEM afericoes.create)"
    - "Cada cenário S1-S6 com result final (pass/fail) + evidência registrada (descrição da execução e estado observado) na lista Tests"
    - "Evidência automatizada de apoio registrada: VerificationUatFixTest (5/28), RbacRegressionTest (14/23), SeederIdempotencyTest (2/14) verdes"
  artifacts:
    - .planning/phases/16-verifica-o-uat/16-UAT.md
    - "usuários de teste: tecnico@uat.test, consulta@uat.test (criados via tinker no PostgreSQL real)"
  key_links:
    - "16-UAT.md S1-S6 -> 09-UAT.md (git ddff1a5^) -> requisito UAT-01 (REQUIREMENTS.md)"
    - "coverage_id UAT-01-S{n} -> cenário {n} da lista Tests (rastreabilidade no formato XX-UAT.md)"
---

<read_first>
@C:/Users/phell/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/phell/.config/opencode/gsd-core/templates/summary.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-verifica-o-uat/16-RESEARCH.md
@.planning/phases/16-verifica-o-uat/16-VALIDATION.md
@.planning/phases/16-verifica-o-uat/16-PATTERNS.md
@.planning/phases/15-corre-es-de-funcionamento/15-UAT.md        # formato exato do registro (analog)
@frontend/src/modules/verifications/pages/VerificationPendingPage.vue
@frontend/src/modules/verifications/components/VerificationFormDialog.vue
@frontend/src/modules/verifications/components/VerificationHistoryTab.vue
@frontend/src/modules/equipment/pages/EquipmentDetailPage.vue   # abas 3/6 + gating por permissão
</read_first>

<objective>
Executar os 6 cenários UAT pendentes de Aferições (Fase 09) contra a UI real em http://localhost:5173 (dev server), após as correções da Fase 15 (RBAC enforcement, rotas canônicas, seeders idempotentes), e documentar o resultado de cada um em 16-UAT.md com evidência (aprovado/reprovado + descrição do que foi executado/observado). Cenários 1-4 executados com admin; cenários 5-6 executados com usuários de permissão criados via tinker (tecnico sem afericoes.view; consulta com afericoes.view sem afericoes.create).

Purpose: UAT-01 exige "Usuário executa os 6 cenários UAT de Aferições sem falhas ou bloqueios". Esta é uma fase human-in-the-loop: o executor prepara o registro e os usuários de teste, então o usuário humano executa cada cenário na UI e o executor registra o resultado com evidência no formato XX-UAT.md (consumido por /gsd-verify-work).

Output: 16-UAT.md criado com os 6 cenários UAT-01 registrados (result + evidência); 2 usuários de teste criados; evidência automatizada de apoio registrada.
</objective>

<acceptance_criteria>
1. 16-UAT.md existe com frontmatter (status/phase/source/started/updated), seção Current Test, lista Tests, Summary (total/passed/issues/pending/skipped/blocked) e Gaps — formato idêntico a 15-UAT.md.
2. Os 6 cenários UAT-01 usam o texto canônico do 09-UAT.md recuperado via `git show ddff1a5^:.planning/phases/09-afericoes/09-UAT.md` — verificar por comparação direta (não recriar de memória).
3. Cada cenário S1-S6 tem coverage_id UAT-01-S{n} e result final: `pass` (com evidência) ou `fail` (com motivo exato: mensagem de erro, status HTTP, screenshot).
4. tecnico@uat.test tem role tecnico (sem afericoes.view) e consulta@uat.test tem role consulta (afericoes.view sim, afericoes.create não) — comprovado via tinker.
5. Filtros de apoio verdes registrados no 16-UAT.md: VerificationUatFixTest (5/28), RbacRegressionTest (14/23), SeederIdempotencyTest (2/14).
6. Ao fim: Summary do 16-UAT.md atualizado (passed/issues/pending com os números reais dos 6 cenários).
</acceptance_criteria>

<execution_context>
- Repo: `D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl`
- UI (ÚNICO caminho): http://localhost:5173 (dev server vite, proxy /api → :80). NÃO usar nginx :80.
- Login admin: admin@labcontrol.com / @dmin123
- Rate limit login: 5 falhas → 429/60s — usar credenciais corretas.
- Usuários de teste (criados neste plano): tecnico@uat.test / senha123 (role tecnico), consulta@uat.test / senha123 (role consulta).
- Mecânica: /gsd-verify-work 16 — executor registra o que o usuário responde/executa.
- Credenciais corretas devem ser anotadas (Pitfall 3 — evita bloqueio por tentativa errada).
</execution_context>

## Context / Estado Verificado

- **Cenários canônicos (NÃO recriar):** os 6 cenários UAT-01 estão integralmente no 09-UAT.md recuperado do git (arquivo arquivado no working tree pelo commit ddff1a5). Recuperar com `git show ddff1a5^:.planning/phases/09-afericoes/09-UAT.md` e copiar o texto VERBATIM para o 16-UAT.md.
- **Dados disponíveis (W1):** PostgreSQL real populado — pendentes de aferição reais existem (equipamentos com verification_frequency sem aferição recente). Pitfall 6: o estado vazio "Todos os equipamentos estão em dia" é verificado como CONSEQUÊNCIA do fluxo (após aferir os pendentes), registrando as duas observações.
- **RBAC ativo (Fase 15):** usuário sem permissão recebe 403 real; gating visual (v-if hasPermission) é UX — a barreira de verdade é o middleware.
- **Pitfall 7 (wording do toast):** o cenário 4 espera "Tolerância excedida para o equipamento"; o código atual mostra toast warn com summary "Tolerância excedida" + detail "Um ou mais parâmetros estão fora da tolerância permitida." (VerificationFormDialog.vue:263-269). Registrar equivalência de intenção no resultado — NÃO reprovar por diferença cosmética de texto.
- **Numeração de abas no EquipmentDetailPage:** Principal(0), Localização(1), Técnica(2), Aferições(3), Arquivos(4), Logs(5), Manutenções(6) [VERIFIED].
- **Rotas canônicas (Fase 15):** GET /api/v1/verifications/pending + GET /api/v1/equipments/{id}/verifications — o frontend usa estas; rotas legadas inexistem.

### Tasks

<tasks>

<task type="auto">
<name>Task 1: Criar 16-UAT.md com os 6 cenários canônicos UAT-01 + usuários de teste + evidência automatizada de apoio</name>
<files>
- .planning/phases/16-verifica-o-uat/16-UAT.md
</files>
<read_first>
- (executar) `git show ddff1a5^:.planning/phases/09-afericoes/09-UAT.md` — TEXTO CANÔNICO dos 6 cenários (copiar VERBATIM)
- (executar) `git show ddff1a5^:.planning/phases/10-manutencaoes/10-VERIFICATION.md` — referência dos 5 itens UAT-02 (para o formato; itens serão registrados na W3 — 16-03)
- @.planning/phases/15-corre-es-de-funcionamento/15-UAT.md — formato exato (frontmatter, Current Test, lista Tests com expected/result/source/coverage_id, Summary, Gaps)
- @.planning/phases/16-verifica-o-uat/16-RESEARCH.md (Pattern 1 — formato; Pitfall 7 — wording do toast)
</read_first>
<action>
Criar o registro 16-UAT.md no formato XX-UAT.md (copiar a estrutura de 15-UAT.md), contendo os 6 cenários UAT-01 com texto canônico recuperado do git — NUNCA recriar de memória (Don't Hand-Roll #5).

1. Recuperar o texto canônico: `git show ddff1a5^:.planning/phases/09-afericoes/09-UAT.md` e copiar VERBATIM as seções "Current Test" e a lista "Tests" (cenários 1-6).
2. Frontmatter YAML (padrão 15-UAT.md):
   ```
   ---
   status: testing
   phase: 16-verifica-o-uat
   source: 16-01-SUMMARY.md
   started: <data ISO atual>
   updated: <data ISO atual>
   ---
   ```
3. Seção `Current Test`: número 1, nome do cenário 1, expected (texto canônico multilinha `|`), `awaiting: user response`.
4. Lista `Tests` — cada cenário com:
   - `expected:` texto canônico do cenário (VERBATIM do 09-UAT.md)
   - `result: pending` (inicial; será atualizado na Task 2/3)
   - `source: manual` (os 6 são manuais; a evidência automatizada de apoio fica referenciada no expected ou como nota)
   - `coverage_id: UAT-01-S{n}` (n = 1..6, conforme ordem do 09-UAT.md)
5. Criar os 2 usuários de teste via tinker no PostgreSQL real (Pitfall 4 — RBAC exige roles anexadas; nenhum seeder cria usuários não-admin):
   ```bash
   docker compose -f docker/docker-compose.yml exec -T php php artisan tinker --execute="
     \$u = App\Models\User::factory()->create(['name' => 'Tecnico UAT', 'email' => 'tecnico@uat.test', 'password' => bcrypt('senha123')]);
     \$u->roles()->attach(App\Models\Role::where('slug', 'tecnico')->value('id'));"
   docker compose -f docker/docker-compose.yml exec -T php php artisan tinker --execute="
     \$u = App\Models\User::factory()->create(['name' => 'Consulta UAT', 'email' => 'consulta@uat.test', 'password' => bcrypt('senha123')]);
     \$u->roles()->attach(App\Models\Role::where('slug', 'consulta')->value('id'));"
   ```
   Anotar as credenciais no próprio 16-UAT.md (Nota de execução) para evitar bloqueio por tentativa errada (Pitfall 3).
6. Rodar as suítes de apoio automatizado e registrar como evidência (citar o comando --filter no expected dos cenários correspondentes):
   - `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=VerificationUatFixTest` (5/28 — apoio S1-S4: criação, tolerância, histórico)
   - `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=RbacRegressionTest` (14/23 — apoio S5/S6: 403 para sem-permissão)
   - `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=SeederIdempotencyTest` (2/14 — apoio S1 base de dados/BUG-01)
7. Registrar a evidência automatizada: nos cenários com cobertura automatizada, manter `result: pending` (a execução manual ainda é o critério) mas acrescentar no `expected` a ref do teste de apoio (ex.: "Apoio automatizado: php artisan test --filter=VerificationUatFixTest — 5 passed/28 assertions, verde na W1"). NÃO marcar cenários manuais como `source: automated`.
</action>
<verify>
<automated>docker compose -f docker/docker-compose.yml exec -T php php artisan tinker --execute="echo 'tecnico='.App\Models\User::where('email','tecnico@uat.test')->first()?->roles()->pluck('slug')->join(',').' consulta='.App\Models\User::where('email','consulta@uat.test')->first()?->roles()->pluck('slug')->join(',');"; (Get-Content .planning\phases\16-verifica-o-uat\16-UAT.md | Select-String -Pattern "UAT-01-S1|UAT-01-S6" | Measure-Object).Count</automated>
</verify>
<acceptance_criteria>
- 16-UAT.md criado com frontmatter completo, Current Test e 6 entradas Tests com coverage_id UAT-01-S1..S6 e texto canônico (comparável ao git show).
- tecnico@uat.test → role `tecnico`; consulta@uat.test → role `consulta` (saída do tinker comprova).
- Os 3 filtros de apoio (VerificationUatFixTest, RbacRegressionTest, SeederIdempotencyTest) verdes, com resultado registrado no 16-UAT.md.
- Credenciais dos usuários de teste anotadas no 16-UAT.md.
</acceptance_criteria>
<done>16-UAT.md existe com frontmatter, Current Test e 6 cenários canônicos (UAT-01-S1..S6, result pending, texto verbatim do git show ddff1a5^); tecnico@uat.test e consulta@uat.test criados com as roles corretas; 3 filtros de apoio verdes registrados como evidência.</done>
<post_conditions>
- Registro 16-UAT.md pronto para a execução manual (Current Test = cenário 1).
- Usuários de permissão existem no banco real e podem logar na UI.
- Evidência automatizada de apoio registrada e referenciável por cenário.
</post_conditions>
<logging_monitoring>
N/A — criação de documento de verificação e usuários de teste via tinker (sem caminho de código user-observável alterado). Registrar no 16-UAT.md a saída do tinker (roles) e os resultados dos filtros como evidência.
</logging_monitoring>
</task>

<task type="auto" gate="blocking-human" checkpoint="checkpoint:human-verify">
<name>Task 2: Execução manual UAT-01 cenários 1-4 com admin — pendentes, formulário dinâmico, aba Aferições, toast de tolerância</name>
<files>
- .planning/phases/16-verifica-o-uat/16-UAT.md   # atualizar result/evidência dos cenários 1-4
</files>
<read_first>
- @.planning/phases/16-verifica-o-uat/16-UAT.md (Current Test — cenário atual)
- @frontend/src/modules/verifications/pages/VerificationPendingPage.vue
- @frontend/src/modules/verifications/components/VerificationFormDialog.vue
- @frontend/src/modules/verifications/components/VerificationHistoryTab.vue
- @frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
- (executar) `git show ddff1a5^:.planning/phases/09-afericoes/09-UAT.md` — texto canônico dos cenários 1-4 (expected do Current Test)
</read_first>
<action>
Execução MANUAL human-in-the-loop dos cenários 1-4 com o usuário admin na UI real (http://localhost:5173). O executor guia, o usuário executa no browser, o executor registra.

**Passo 0 — preparação da sessão (automatizado, sem parar):**
- Abrir http://localhost:5173 e logar com admin@labcontrol.com / @dmin123.
- Confirmar no browser (DevTools console + Network) que /api/v1/verifications/pending responde 200 e a página carrega.

**Cenário 1 — DataTable de Aferições Pendentes (layout, loading, estado vazio):**
- Navegar para /verifications (sidebar Operações → Aferições).
- Esperado: DataTable com colunas Equipamento, Patrimônio, Nº Série, Categoria, Frequência, Última Aferição, Ações; loading skeleton durante carregamento; lista populada com pendentes (dados do seed).
- Registro: confirmar presença das 7 colunas, skeleton (observar durante load), e lista populada.
- Estado vazio (Pitfall 6): verificar como CONSEQUÊNCIA do fluxo — após aferir todos os pendentes (fluxo do cenário 2), a lista esvazia e exibe "Todos os equipamentos estão em dia". Registrar as DUAS observações (populada + vazia após aferição). Se a lista vazia não puder ser alcançada sem afetar os demais cenários, registrar observação parcial como nota.

**Cenário 2 — Formulário de Aferição (campos dinâmicos de parâmetros):**
- Clicar em "Nova Aferição" (ou "Aferir") na página de pendentes.
- Esperado: Dialog renderiza um InputNumber por parâmetro do template, com label, unidade e faixa de tolerância (min–max) exibidos; Select de equipamento funcional; TextArea para observações.
- Registro: confirmar dialog abre, N parâmetros renderizados com label/unidade/faixa, select funcional, textarea presente. Salvar uma aferição dentro da tolerância → toast de sucesso e registro aparece no histórico (usar como base do cenário 3).

**Cenário 3 — Aba Aferições no EquipmentDetailPage (timeline com parâmetros expansíveis):**
- Ir para /equipments/:id (equipamento aferido no cenário 2) e clicar na aba "Aferições" (tab 3).
- Esperado: DataTable paginada com colunas Data, Operador, # Parâmetros, indicador "Fora do Intervalo"; linhas expansíveis mostram parâmetros com tags coloridas (verde dentro do intervalo, vermelho fora); botão "Aferir" no topo da aba.
- Registro: confirmar colunas, expansão de linha, tags coloridas, botão Aferir visível (admin).

**Cenário 4 — Alerta de tolerância excedida após salvar aferição:**
- No formulário de aferição, inserir um parâmetro FORA do intervalo min–max e salvar.
- Esperado: toast warn com summary "Tolerância excedida" e detail "Um ou mais parâmetros estão fora da tolerância permitida." (Pitfall 7: o texto canônico era "Tolerância excedida para o equipamento" — registrar equivalência de intenção; NÃO reprovar por redação).
- Registro: confirmar toast warn aparece, registro salvo aparece com indicador "Fora do Intervalo" (vermelho) no histórico.

**Registro (por cenário):** atualizar no 16-UAT.md o `result` do cenário (pass/fail) e acrescentar evidência: descrição do que foi executado, estado observado, e — se reprovado — erro exato (console, status HTTP, screenshot). Após cada cenário, atualizar a seção Current Test para o próximo cenário. NUNCA marcar pass sem evidência (Anti-Pattern 1).

**PARADA do executor:** após cada cenário registrado, o executor PAUSA e espera o usuário aprovar/reagir ao resultado registrado antes de seguir para o próximo. A cada cenário concluído, atualizar a Summary do 16-UAT.md (passed/pending).
</action>
<verify>
<automated># Sem comando único de automação — verificação é o registro de evidência por cenário + confirmação do usuário.
# Gate estrutural do registro (após a task): cada cenário 1-4 com result != pending
(Get-Content .planning\phases\16-verifica-o-uat\16-UAT.md | Select-String -Pattern "result: (pass|fail)").Count -ge 4</automated>
</verify>
<acceptance_criteria>
- Cenários 1-4 com `result: pass` e evidência registrada (descrição + observação) OU `result: fail` com motivo exato no 16-UAT.md.
- Cenário 4 aceita equivalência de wording (summary "Tolerância excedida" + detail "Um ou mais parâmetros estão fora da tolerância permitida.") com a equivalência anotada.
- Estado vazio do cenário 1 registrado como consequência do fluxo (ou observação parcial anotada).
- Current Test avançou para o cenário 5 após o registro do cenário 4.
</acceptance_criteria>
<done>Os cenários UAT-01-S1..S4 têm result final (pass ou fail com motivo exato) e evidência descrita no 16-UAT.md, com o Current Test apontando para o cenário 5.</done>
<post_conditions>
- 4 cenários UAT-01 registrados com resultado e evidência.
- Pelo menos 1 aferição criada dentro da tolerância e 1 fora da tolerância (dados para o cenário 3 e 4 no histórico real).
- Sessão admin aberta no browser ou re-login possível (credenciais anotadas).
</post_conditions>
<logging_monitoring>
Durante a sessão manual, observar o DevTools: (1) console sem erros JS durante as navegações/formulários; (2) Network: POST /api/v1/verifications → 201/200 e GET /api/v1/verifications/pending → 200; (3) qualquer 4xx/5xx registrado como evidência de falha. Registrar observações no 16-UAT.md.
</logging_monitoring>
</task>

<task type="auto" gate="blocking-human" checkpoint="checkpoint:human-verify">
<name>Task 3: Execução manual UAT-01 cenários 5-6 com usuários de permissão — gating da aba e do botão Aferir + fechamento do Summary</name>
<files>
- .planning/phases/16-verifica-o-uat/16-UAT.md   # atualizar result/evidência dos cenários 5-6 + Summary final
</files>
<read_first>
- @.planning/phases/16-verifica-o-uat/16-UAT.md (Current Test — cenário atual)
- @frontend/src/modules/equipment/pages/EquipmentDetailPage.vue (gating da aba 3 por hasPermission('afericoes.view'))
- @frontend/src/modules/verifications/pages/VerificationPendingPage.vue (gating do botão por hasPermission('afericoes.create'))
- @frontend/src/modules/verifications/components/VerificationHistoryTab.vue (gating do botão na aba)
- (executar) `git show ddff1a5^:.planning/phases/09-afericoes/09-UAT.md` — texto canônico dos cenários 5-6
</read_first>
<action>
Execução MANUAL human-in-the-loop dos cenários 5-6 com os usuários de permissão (tecnico e consulta). O executor guia, o usuário executa, o executor registra.

**Cenário 5 — Aba Aferições condicional à permissão afericoes.view:**
- Logout do admin; login como tecnico@uat.test / senha123 (role tecnico — SEM afericoes.view).
- Navegar para um /equipments/:id.
- Esperado: a aba "Aferições" NÃO aparece. Abas permanecem: Principal(0), Localização(1), Técnica(2), Arquivos(4), Manutenções(6), Logs(5) — numeração sem quebra após a ausência da aba 3.
- Registro: confirmar aba 3 oculta e demais abas íntegras. Opcional (reforço): chamar GET /api/v1/equipments/{id}/verifications como tecnico → 403 real (barreira de verdade do RBAC, não só gating visual) — registrar o status.
- Confirmar também que /verifications como tecnico não permite acessar o módulo (403 na API ou redirect) — registrar observação.

**Cenário 6 — Botão "Aferir" condicional à permissão afericoes.create:**
- Logout do tecnico; login como consulta@uat.test / senha123 (role consulta — COM afericoes.view, SEM afericoes.create).
- Navegar para /verifications e para /equipments/:id → aba Aferições.
- Esperado: a aba "Aferições" É visível (tem afericoes.view), mas o botão "Aferir"/"Nova Aferição" NÃO aparece (não tem afericoes.create).
- Registro: confirmar aba visível + botão oculto na página de pendentes e na aba de histórico. Opcional: POST /api/v1/verifications como consulta → 403 real (registrar status).

**Fechamento:**
- Atualizar os 2 cenários no 16-UAT.md (result + evidência).
- Atualizar a Summary: total/passed/issues/pending/skipped/blocked com os números REAIS dos 6 cenários.
- Preencher a seção Gaps com qualquer item reprovado (ou "[none yet]" se todos pass).
- Atualizar frontmatter `updated` com a data ISO atual.
- Avisar o usuário que o UAT-01 (6/6) está registrado e que a W3 (16-03 — UAT-02, 5 itens de Manutenções) é o próximo plano.
</action>
<verify>
<automated># Gate estrutural do registro (após a task): 6 cenários com result != pending e Summary coerente
(Get-Content .planning\phases\16-verifica-o-uat\16-UAT.md | Select-String -Pattern "result: (pass|fail)").Count -eq 6; Get-Content .planning\phases\16-verifica-o-uat\16-UAT.md | Select-String -Pattern "^passed:|^pending:"</automated>
</verify>
<acceptance_criteria>
- Cenários 5-6 com `result: pass` + evidência OU `result: fail` + motivo exato.
- Cenário 5 comprovado: tecnico NÃO vê a aba Aferições; numeração das abas restantes intacta.
- Cenário 6 comprovado: consulta VÊ a aba Aferições mas NÃO vê o botão Aferir (página de pendentes e aba de histórico).
- Summary final do 16-UAT.md reflete os 6 cenários (passed+issues+pending = 6); Gaps preenchido; frontmatter `updated` atualizado.
</acceptance_criteria>
<done>Os cenários UAT-01-S5 e UAT-01-S6 têm result final e evidência; o 16-UAT.md tem Summary coerente para os 6 cenários, Gaps preenchido e frontmatter `updated` atualizado — UAT-01 (6/6) documentado.</done>
<post_conditions>
- Os 6 cenários UAT-01 registrados com resultado e evidência no 16-UAT.md.
- Usuários tecnico@uat.test e consulta@uat.test existem e estão prontos para reuso em futuras fases.
- Fase pronta para a W3 (16-03 — UAT-02 Manutenções).
</post_conditions>
<logging_monitoring>
Observar no DevTools: (1) nenhum vazamento de conteúdo proibido no console/network ao navegar com tecnico (aba 3 não deve nem buscar /verifications); (2) status 403 nas chamadas de API sem permissão — registrar como evidência do enforcement; (3) erros JS registrados como evidência. Registrar observações no 16-UAT.md.
</logging_monitoring>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário→UI (:5173) | Cenários manuais executados por humano no browser; resultados dependem do que é observado |
| usuário de permissão→API | tecnico/consulta atravessam Sanctum + middleware permission — 403 esperado em cenários 5-6 |
| executor→16-UAT.md | Registro da evidência; falsa aprovação sem evidência compromete a rastreabilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Spoofing | login tecnico/consulta (:5173) | medium | mitigate | Credenciais anotadas no 16-UAT.md (senha123); se 429 (rate limit) aguardar 60s — nunca insistir com senha errada |
| T-16-06 | Elevation of Privilege | gating visual abas/botões | high | mitigate | Barreira de verdade = middleware permission → 403 (RbacRegressionTest verde); o UAT verifica que o visual reflete o enforcement — registrar o 403 observado nos cenários 5/6 |
| T-16-07 | Information Disclosure | histórico de aferições por equipamento | medium | mitigate | Rotas sob permission:afericoes.view → 403 para tecnico (cenário 5); registrar status no UAT |
| T-16-08 | Repudiation | registro de resultado no 16-UAT.md | low | accept | Convenção XX-UAT.md + evidência descrita por cenário são suficientes para verificação GSD (não há assinatura digital exigida) |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- Task 1: estrutura do 16-UAT.md + roles dos usuários via tinker + 3 filtros de apoio verdes.
- Task 2: 4 cenários com result != pending e evidência; Current Test avançou.
- Task 3: 6 cenários com result != pending; Summary coerente (passed+issues+pending = 6); Gaps preenchido.
- Suíte completa verde ao fim do wave (gate da 16-VALIDATION.md): `docker compose -f docker/docker-compose.yml exec -T php php artisan test`.
</verification>

<success_criteria>
- 6 cenários UAT-01 executados sem falhas/bloqueios E registrados aprovado/reprovado com evidência no 16-UAT.md (critérios 1 e 3 do ROADMAP para UAT-01).
- Usuários de permissão criados e usados nos cenários 5-6 (tecnico sem afericoes.view; consulta sem afericoes.create).
- Evidência automatizada de apoio registrada (VerificationUatFixTest, RbacRegressionTest, SeederIdempotencyTest).
- 16-02-SUMMARY.md criado com resumo da execução e status por cenário.
</success_criteria>

<output>
Criar `.planning/phases/16-verifica-o-uat/16-02-SUMMARY.md` ao concluir.
</output>

## Artifacts this phase produces
- `.planning/phases/16-verifica-o-uat/16-UAT.md` — registro de UAT (criado; contém os 6 cenários UAT-01 com result + evidência; os 5 itens UAT-02 serão adicionados na W3/16-03).
- Usuários de teste criados via tinker no PostgreSQL real: `tecnico@uat.test` (role tecnico, senha123) e `consulta@uat.test` (role consulta, senha123).
- Evidência automatizada registrada no 16-UAT.md: VerificationUatFixTest (5/28), RbacRegressionTest (14/23), SeederIdempotencyTest (2/14).
- Dados de execução criados no banco real durante os cenários: aferições dentro/fora da tolerância, pendentes esvaziados (consequência do fluxo).
- `.planning/phases/16-verifica-o-uat/16-02-SUMMARY.md`.
</content>
