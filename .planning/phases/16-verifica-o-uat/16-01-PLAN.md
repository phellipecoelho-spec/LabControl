---
phase: 16-verifica-o-uat
plan: 01
title: Preparação do Ambiente — commit + seed PostgreSQL + gate de suíte (W1)
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/Models/Equipment.php            # COMMIT de mudança pendente (não altera conteúdo)
  - backend/app/Services/LoanService.php        # COMMIT de mudança pendente
  - backend/app/Services/MaintenanceService.php # COMMIT de mudança pendente
  - frontend/src/modules/equipment/pages/EquipmentDetailPage.vue  # COMMIT
  - frontend/src/modules/equipment/pages/EquipmentFormPage.vue    # COMMIT
  - frontend/src/modules/equipment/pages/EquipmentListPage.vue    # COMMIT
  - frontend/src/modules/inventory/pages/InventoryItemFormPage.vue # COMMIT
autonomous: true
requirements: [UAT-01, UAT-02]
created: 2026-08-09

# Goal-backward verification
must_haves:
  truths:
    - "Working tree de código limpo — as 7 mudanças pendentes de produto estão versionadas em commit antes de qualquer evidência de UAT"
    - "PostgreSQL real populado: migrate:fresh --seed --force conclui sem erro E um db:seed --force adicional roda sem exceção nem duplicação (idempotência BUG-01)"
    - "UI acessível APENAS via dev server http://localhost:5173 respondendo 200 (nginx :80 fora de uso)"
    - "Suíte completa backend verde (165 passed / 473 assertions) antes da evidência manual"
  artifacts:
    - backend/app/Models/Equipment.php (commitado)
    - backend/app/Services/LoanService.php (commitado)
    - backend/app/Services/MaintenanceService.php (commitado)
    - frontend/src/modules/equipment/pages/EquipmentDetailPage.vue (commitado)
    - frontend/src/modules/equipment/pages/EquipmentFormPage.vue (commitado)
    - frontend/src/modules/equipment/pages/EquipmentListPage.vue (commitado)
    - frontend/src/modules/inventory/pages/InventoryItemFormPage.vue (commitado)
  key_links:
    - "dev server :5173 (proxy /api) -> nginx :80 -> php:9000 -> PostgreSQL/Redis — único caminho funcional para a UI"
    - "AdminUserSeeder -> admin@labcontrol.com / @dmin123 — credencial única para login admin"
---

<read_first>
@C:/Users/phell/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/phell/.config/opencode/gsd-core/templates/summary.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-verifica-o-uat/16-RESEARCH.md
@.planning/phases/16-verifica-o-uat/16-VALIDATION.md
@.planning/phases/16-verifica-o-uat/16-PATTERNS.md
@.planning/phases/15-corre-es-de-funcionamento/15-VERIFICATION.md
</read_first>

<objective>
Preparar o ambiente para a execução dos 11 cenários UAT (6 de Aferições — UAT-01, 5 visuais de Manutenções — UAT-02): (1) versionar as 7 mudanças de produto pendentes da working tree para que a evidência corresponda a código commitado (regra do CONTEXT.md: tudo versionado); (2) popular o PostgreSQL real com `migrate:fresh --seed` (banco vazio confirmado — operação segura) e provar a idempotência dos seeders com um `db:seed` adicional (liquida o Manual-Only pendente da Fase 15: seed 2x + login admin); (3) rodar a suíte completa como gate verde antes de qualquer evidência manual.

Purpose: Os cenários UAT só podem ser executados sobre um ambiente são — código versionado, banco populado e suíte verde. Sem este plano, os cenários da W2/W3 falhariam com "sem dados" e a evidência não corresponderia a um commit.

Output: 7 arquivos de produto commitados; PostgreSQL real populado (admin único, 6 roles, categorias); prova de idempotência do seeder; suíte completa verde registrada como evidência automatizada.
</objective>

<acceptance_criteria>
1. `git status --short` não lista nenhum dos 7 arquivos de produto como modified/untracked após o commit.
2. `docker compose -f docker/docker-compose.yml exec -T php php artisan migrate:fresh --seed --force` termina com exit 0.
3. `docker compose -f docker/docker-compose.yml exec -T php php artisan db:seed --force` (2ª execução) termina com exit 0, sem exceção de duplicação.
4. Tinker confirma no PostgreSQL real: User::count() = 1 (admin@labcontrol.com), Role::count() = 6, EquipmentCategory::count() = 5, InventoryCategory::count() = 5.
5. `curl -s -o NUL -w "%{http_code}" http://localhost:5173` retorna 200 (dev server no ar; se não estiver, iniciado via `npm run dev` em frontend/).
6. `docker compose -f docker/docker-compose.yml exec -T php php artisan test` termina com 0 failures (meta: 165 passed / 473 assertions; registrar os números reais se diferentes).
</acceptance_criteria>

<execution_context>
- Repo: `D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl`
- Containers: `docker compose -f docker/docker-compose.yml` (php, postgres, redis, nginx — 4/4 healthy)
- UI: dev server vite em http://localhost:5173 (proxy /api → :80). NÃO usar nginx :80 (retorna 500 — dist não montado/stale).
- Credenciais admin: admin@labcontrol.com / @dmin123
- Rate limit login: 5 falhas → 429/60s — usar sempre credenciais corretas.
- Suíte completa: ~277s.
</execution_context>

## Context / Estado Verificado

- **Working tree:** 7 arquivos de produto modificados (Equipment model +7, LoanService +16, MaintenanceService +18, EquipmentDetailPage +2, EquipmentFormPage +1, EquipmentListPage +3, InventoryItemFormPage -1). PLANNER.md/TASKS.md e artefatos .planning são de processo GSD — NÃO entrar neste commit.
- **Banco real:** PostgreSQL vazio (0 users/roles/equipments — confirmado via tinker pela pesquisa). Banco vazio ⇒ `migrate:fresh` sem risco de perda.
- **Seeders (Fase 15):** idempotentes (firstOrCreate em colunas UNIQUE + guards de count) — `db:seed` repetível sem exceção (BUG-01).
- **UI:** somente dev server :5173 é funcional (nginx :80 → 500; frontend/dist não montado no container e stale de 27/07).
- **Suíte:** 165 passed / 473 assertions (15-VERIFICATION.md, mesmo dia); VerificationUatFixTest (5/28) e MaintenanceVerificationTest (6/23) re-executados verdes na pesquisa.
- **Typecheck:** 7 erros pré-existentes bloqueiam `npm run build` mas NÃO o dev server — fora do escopo desta fase (Fase 19).

### Tasks

<tasks>

<task type="auto">
<name>Task 1: Commit das mudanças pendentes da working tree (7 arquivos de produto)</name>
<files>
- backend/app/Models/Equipment.php
- backend/app/Services/LoanService.php
- backend/app/Services/MaintenanceService.php
- frontend/src/modules/equipment/pages/EquipmentDetailPage.vue
- frontend/src/modules/equipment/pages/EquipmentFormPage.vue
- frontend/src/modules/equipment/pages/EquipmentListPage.vue
- frontend/src/modules/inventory/pages/InventoryItemFormPage.vue
</files>
<read_first>
- (executar) `git -C "D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl" status --short`
- (executar) `git -C "D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl" diff --stat` — conferir que as mudanças são as 7 listadas (sync de status + ajustes Vue)
- `git log --oneline -5` — conferir convenção de mensagem do repo (feat/fix/docs/test/chore + escopo)
</read_first>
<action>
Versionar as 7 mudanças de produto pendentes ANTES de registrar qualquer evidência de UAT (Pitfall 8 da 16-RESEARCH.md: evidência deve corresponder a código commitado).

1. Conferir com `git status --short` que exatamente os 7 arquivos abaixo estão modificados (Equipment.php, LoanService.php, MaintenanceService.php, EquipmentDetailPage.vue, EquipmentFormPage.vue, EquipmentListPage.vue, InventoryItemFormPage.vue). Se houver arquivos adicionais modificados além destes, NÃO commitar cegamente — parar e reportar o conteúdo extra antes de prosseguir.
2. Stage apenas os 7 arquivos de produto:
   `git add backend/app/Models/Equipment.php backend/app/Services/LoanService.php backend/app/Services/MaintenanceService.php frontend/src/modules/equipment/pages/EquipmentDetailPage.vue frontend/src/modules/equipment/pages/EquipmentFormPage.vue frontend/src/modules/equipment/pages/EquipmentListPage.vue frontend/src/modules/inventory/pages/InventoryItemFormPage.vue`
3. NÃO stage: PLANNER.md, TASKS.md, qualquer caminho `.planning/**`, nem `15-UAT.md` (artefatos de processo GSD são commitados pelos fluxos de planejamento/execução — fora deste commit de produto).
4. Commit com mensagem na convenção do repo:
   `feat: sync de status de equipamento (manutencao/emprestimo) e ajustes de frontend`
5. Conferir `git status --short` final: nenhum dos 7 arquivos de produto aparece como modified/untracked (os arquivos .planning/TASKS.md/PLANNER.md podem permanecer pendentes — esperado).
</action>
<verify>
<automated>git -C "D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl" status --short; git -C "D:\BACKUP\PROJETOS DE NEGÓCIO\LabControl" log --oneline -3</automated>
</verify>
<acceptance_criteria>
- Commit criado com os 7 arquivos de produto; `git status --short` não lista nenhum dos 7 como modified/untracked.
- A mensagem do commit segue a convenção (feat: + escopo).
- Nenhum artefato .planning, TASKS.md ou PLANNER.md dentro do commit.
</acceptance_criteria>
<done>Os 7 arquivos de produto estão versionados em um único commit (mensagem feat: com escopo); `git status --short` limpo para esses arquivos; nenhum arquivo de processo (.planning/TASKS.md/PLANNER.md) no commit.</done>
<post_conditions>
- A working tree de produto está limpa; toda evidência de UAT a partir de agora corresponde a código versionado.
- O dev server (se em execução) já serve as mudanças via HMR — não há reinício necessário.
</post_conditions>
<logging_monitoring>
N/A — task de versionamento (git), sem caminho de código user-observável alterado. Registrar o hash do commit e a saída de `git status --short` no 16-01-SUMMARY.md como evidência.
</logging_monitoring>
</task>

<task type="auto">
<name>Task 2: Seed do PostgreSQL real + prova de idempotência + probes de ambiente (UI :5173)</name>
<files>
- (nenhum arquivo de código — operação de banco/ambiente)
</files>
<read_first>
- (executar) `docker compose -f docker/docker-compose.yml ps` — 4 containers healthy
- (executar) `docker compose -f docker/docker-compose.yml exec -T php php artisan --version` — Laravel 13.20.0
- `docker/docker-compose.yml` — confirmar nome do serviço php (`php`) e caminho do compose
</read_first>
<action>
Popular o PostgreSQL real e provar o ambiente antes dos cenários (Pitfall 1: banco vazio; Pitfall 2: UI só em :5173).

1. Rodar o seed do zero (banco vazio confirmado — operação segura, sem risco de perda):
   `docker compose -f docker/docker-compose.yml exec -T php php artisan migrate:fresh --seed --force`
2. Prova de idempotência (BUG-01): repetir o seed sem fresh:
   `docker compose -f docker/docker-compose.yml exec -T php php artisan db:seed --force`
   → deve terminar com exit 0 SEM exceção de duplicação. Registrar as duas saídas.
3. Confirmar os counts no PostgreSQL real via tinker:
   `docker compose -f docker/docker-compose.yml exec -T php php artisan tinker --execute="echo 'users='.App\Models\User::count().' roles='.App\Models\Role::count().' eqcat='.App\Models\EquipmentCategory::count().' invcat='.App\Models\InventoryCategory::count();"`
   Esperado: users=1 roles=6 eqcat=5 invcat=5 (registrar valores reais).
4. Probar a UI: `curl -s -o NUL -w "%{http_code}" http://localhost:5173` → 200. Se o dev server não estiver respondendo, iniciá-lo em background a partir de `frontend/` (`npm run dev`) e re-testar o curl até 200.
5. NÃO testar http://localhost (nginx :80) — retorna 500 (dist não montado/stale); documentar o 500 apenas como confirmação de que o caminho nginx não é utilizável.
6. Login smoke via API (liquida o item Manual-Only "login admin" da Fase 15 com evidência automatizada): obter cookie CSRF em `http://localhost:5173/sanctum/csrf-cookie` (via proxy /api → :80) e POST `http://localhost:5173/api/v1/auth/login` com `{"email":"admin@labcontrol.com","password":"@dmin123"}` (Content-Type application/json, com o cookie de sessão). Esperado: 200/201 com dados do usuário (ou 422/redirect se o fluxo exigir verificação de email — registrar o status real e a resposta).
</action>
<verify>
<automated>docker compose -f docker/docker-compose.yml exec -T php php artisan tinker --execute="echo 'users='.App\Models\User::count().' roles='.App\Models\Role::count().' eqcat='.App\Models\EquipmentCategory::count().' invcat='.App\Models\InventoryCategory::count();"; curl -s -o NUL -w "%{http_code}" http://localhost:5173</automated>
</verify>
<acceptance_criteria>
- `migrate:fresh --seed --force` e `db:seed --force` (2ª vez) terminam com exit 0.
- Tinker: users=1, roles=6, eqcat=5, invcat=5 (ou valores registrados e justificados).
- `curl http://localhost:5173` → 200.
- Login smoke admin: resposta HTTP 200/201 com payload de usuário (ou status registrado e explicado no SUMMARY).
</acceptance_criteria>
<done>PostgreSQL real populado (admin único, 6 roles, 5+5 categorias); `db:seed` repetido 1x sem exceção (idempotência BUG-01 comprovada); UI respondendo 200 em :5173; login admin smoke validado — tudo registrado no SUMMARY.</done>
<post_conditions>
- PostgreSQL real populado com admin/roles/permissões/categorias — dados de base para UAT-01 e UAT-02 disponíveis.
- Idempotência dos seeders comprovada (BUG-01).
- UI acessível em http://localhost:5173.
</post_conditions>
<logging_monitoring>
Observar e registrar no SUMMARY: saída resumida de migrate:fresh/seed (exit codes), counts de tinker, código HTTP do curl :5173 e resposta do login smoke. Se `db:seed` repetido lançar qualquer exceção, registrar a mensagem EXATA como evidência de falha de idempotência (não esconder).
</logging_monitoring>
</task>

<task type="auto">
<name>Task 3: Gate — suíte completa backend verde (evidência automatizada dos cenários)</name>
<files>
- (nenhum arquivo — execução de testes)
</files>
<read_first>
- (executar) `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=VerificationUatFixTest` — apoio UAT-01 (5 tests/28 assertions)
- (executar) `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=MaintenanceVerificationTest` — apoio UAT-02 (6 tests/23 assertions)
- (executar) `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=RbacRegressionTest` — apoio RBAC (14 tests/23 assertions)
- (executar) `docker compose -f docker/docker-compose.yml exec -T php php artisan test --filter=SeederIdempotencyTest` — apoio seed/BUG-01 (2 tests/14 assertions)
</read_first>
<action>
Gate de suíte antes de qualquer evidência manual (Pattern 2 da 16-RESEARCH.md; Sampling Rate da 16-VALIDATION.md — suíte completa verde antes do /gsd-verify-work).

1. Rodar a suíte completa: `docker compose -f docker/docker-compose.yml exec -T php php artisan test`
   Meta: 165 passed / 473 assertions, 0 failures. Registrar os números REAIS de saída (linha de resumo "Tests: ... Passed ... Failed ...").
2. Rodar os 4 filtros de apoio e registrar cada resultado (suíte de apoio por módulo — evidência automatizada dos cenários que será citada no 16-UAT.md):
   - VerificationUatFixTest (UAT-01: create/tolerance/history)
   - MaintenanceVerificationTest (UAT-02: create/complete/cancel/history)
   - RbacRegressionTest (UAT-01 #5/#6, UAT-02 #5: 403 real)
   - SeederIdempotencyTest (UAT-01 #1/BUG-01: seed idempotente)
3. SE a suíte completa falhar com testes que não são os 4 filtros acima: NÃO prosseguir para as W2/W3 — reportar a falha com a saída do teste no SUMMARY e parar a fase (gate bloqueante).
4. SE algum dos 4 filtros falhar: registrar a falha exata como evidência e decidir com o usuário (bloqueante para o cenário correspondente).
5. Registrar no 16-01-SUMMARY.md: linha de resumo da suíte completa + 4 resultados dos filtros.
</action>
<verify>
<automated>docker compose -f docker/docker-compose.yml exec -T php php artisan test 2>&1 | Select-String -Pattern "Tests:|Passed|Failed" | Select-Object -Last 3</automated>
</verify>
<acceptance_criteria>
- Suíte completa termina com 0 failures (registrar counts reais; meta 165/473).
- Os 4 filtros de apoio terminam verdes (VerificationUatFixTest 5/28, MaintenanceVerificationTest 6/23, RbacRegressionTest 14/23, SeederIdempotencyTest 2/14) — registrados no SUMMARY.
- Nenhuma falha desconhecida introduzida.
</acceptance_criteria>
<done>Suíte completa verde (0 failures, meta 165 passed/473 assertions) e os 4 filtros de apoio verdes, com a linha de resumo e os resultados registrados no 16-01-SUMMARY.md como evidência automatizada.</done>
<post_conditions>
- Gate verde comprovado: todo o contrato de API por trás dos 11 cenários responde conforme esperado (evidência automatizada disponível para o 16-UAT.md).
- Ambiente pronto para a W2 (16-02, UAT-01).
</post_conditions>
<logging_monitoring>
N/A — execução de testes (sem caminho de código alterado). Registrar linha de resumo da suíte + resultados dos filtros no SUMMARY como evidência.
</logging_monitoring>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor→git | Working tree é fonte de evidência; commit incorreto (arquivos errados/secrets) contamina o histórico |
| executor→PostgreSQL real | `migrate:fresh` destrói dados — seguro hoje (banco vazio verificado), perigoso se houver dados |
| executor→API login (:5173) | Credenciais admin + rate limit governam a interação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | git commit da W1 | medium | mitigate | Conferir `git status`/`diff --stat` ANTES do add; stage explícito dos 7 arquivos; excluir .planning/TASKS.md/PLANNER.md |
| T-16-02 | Denial of Service | migrate:fresh --seed | high | mitigate | Banco vazio confirmado via tinker (User::count()=0) antes do fresh; se count > 0, parar e reportar |
| T-16-03 | Spoofing | login admin (:5173) | medium | mitigate | Usar credenciais exatas do AdminUserSeeder; nunca repetir credenciais erradas 5+ vezes (rate limit 429/60s) |
| T-16-04 | Repudiation | seed/login evidence | low | accept | Saídas de comando registradas no SUMMARY são evidência suficiente para fase de verificação (não há trilha formal de auditoria de seed esperada) |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote é instalado nesta fase (verification-only; Package Legitimacy Audit da pesquisa = N/A) |
</threat_model>

<verification>
- Após Task 1: `git status --short` limpo nos 7 arquivos de produto.
- Após Task 2: counts de tinker + curl :5173 = 200 + db:seed 2ª vez sem exceção.
- Após Task 3: suíte completa 0 failures + 4 filtros verdes.
- Verificação estrutural: nenhum caminho de código de produto alterado por este plano (git diff vazio de novo código além do commit da Task 1).
</verification>

<success_criteria>
- 7 arquivos de produto versionados em commit único.
- PostgreSQL real populado e idempotência comprovada (BUG-01 liquidado).
- UI acessível em http://localhost:5173.
- Suíte completa verde (gate) + 4 filtros de apoio verdes registrados como evidência.
- 16-01-SUMMARY.md criado com evidências (exit codes, counts, http codes, linha de resumo da suíte).
</success_criteria>

<output>
Criar `.planning/phases/16-verifica-o-uat/16-01-SUMMARY.md` ao concluir.
</output>

## Artifacts this phase produces
- Commit `feat: sync de status de equipamento (manutencao/emprestimo) e ajustes de frontend` — versiona os 7 arquivos de produto pendentes (Equipment.php, LoanService.php, MaintenanceService.php, EquipmentDetailPage.vue, EquipmentFormPage.vue, EquipmentListPage.vue, InventoryItemFormPage.vue).
- PostgreSQL real populado: 1 admin (admin@labcontrol.com), 6 roles, 5 categorias de equipamento, 5 de estoque (estado de banco, não arquivo).
- Evidência de idempotência: 2ª execução de `db:seed` sem exceção (BUG-01).
- Evidência de suíte: resultado completo de `php artisan test` + 4 filtros de apoio.
- `.planning/phases/16-verifica-o-uat/16-01-SUMMARY.md`.
</content>
